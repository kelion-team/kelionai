# KELIONAI — DOCUMENT COMPLET DE PRELUARE PENTRU ORICE AI
*(actualizat 11 iulie 2026 — dacă deschizi acest fișier ca AI nou, aici ai TOT ce trebuie ca să lucrezi imediat, fără să mai explorezi de la zero)*

> **DOCUMENT VIU — regulă obligatorie:** dacă schimbi cod, arhitectură, reguli sau
> starea proiectului, **actualizează secțiunea relevantă de aici (și §13 Starea)
> ÎNAINTE să închei sesiunea/PR-ul.** Nu există alt mecanism de auto-actualizare —
> convenția asta E mecanismul. Un document de preluare depășit e mai periculos
> decât unul lipsă (induce în eroare AI-ul următor). Vezi și §11.

## 0. Ce este proiectul
**Kelionai** = asistent AI live la **https://kelionai.app**: avatar 3D (Ready Player Me),
voce hands-free (wake word „Hey Kelion", TTS Chirp 3 HD, barge-in), vedere prin cameră,
GPS, 14+ skill-uri Google/tool-use, chat multilingv, browser live navigabil de Kelion,
generare imagini, corectare transcriere. Proprietar unic + singurul admin:
**Adrian — adrianenc11@gmail.com**. Repo: `kelion-team/kelionai` (GitHub).

## 1. REGULI NE-NEGOCIABILE (ordinea lui Adrian)
1. **Vorbește cu Adrian în ROMÂNĂ.** Limba lui de chat cu Kelion e blocată permanent pe română; restul userilor pe detecție automată.
2. Adrian **testează live** pe kelionai.app, nu local → după fiecare cerință rezolvată: **build → deploy → VERIFICĂ LIVE cu dovadă** (nu declara nimic „gata" fără dovadă reală: curl, decodare, măsurătoare).
3. **Producția = master, 100% sincron, mereu.** Nimic nu are voie să publice cod mai vechi decât `origin/master` (lecția „deploy fantomă", vezi §6).
4. **[ACTUALIZAT — 25 iul 2026] RAILWAY NU MAI EXISTĂ; pipeline-ul gazdei noi E DEFINIT.** Gazda reală e VPS-ul propriu (app în Docker `kelionai-app`, Caddy în față). **Pipeline-ul de publicare = workflow-ul `deploy.yml`**: rulează automat la fiecare push pe master (merge-ul lui Adrian = aprobarea) sau manual, execută `deploy/deploy.sh` pe VPS prin SSH (secretul `VPS_SSH_KEY`) și **verifică anti-fantoma din afară: `https://kelionai.app/api/version` trebuie să întoarcă EXACT sha-ul lui `origin/master`** (injectat în container prin `GIT_COMMIT_SHA`) + health 200 — altfel jobul pică roșu și publicarea NU se declară. Regula de fond neschimbată: nicio publicare pe lângă pipeline, un release aprobat = întâi PR + merge în master, „publicat" se declară DOAR cu dovada.
5. Chat/voce = **latență minimă** (țintă: primul cuvânt sub 1s). Nu adăuga întârzieri nejustificate.
5. Repară **rescriind modulul mic responsabil**, nu cu petice.
6. **NU schimba cheia/rutarea pe abonament pentru admin și demo** (vezi §5 — bani). Cost pe demo: neschimbat fără acord explicit.
7. Model: **pornește mereu pe viteză maximă, escaladează automat la cel mai puternic model al momentului** doar când cerința o cere (router §4.3).
8. Marcaje de sistem în producție (versiune sub QR, filigran): **ora Londrei**. Ora către utilizator: **a utilizatorului** (clientul trimite `now`/`tz` per tură).
9. Toți userii au aceleași capabilități consumer (voce/față/ochi/GPS/Google); ingineria de cod la nivel Claude Code e pentru admin prin punte (userii obișnuiți = milestone sandbox).
10. Nu atinge `C:\Users\adria\Downloads\k` (proiect vechi arhivat, doar pe mașina lui Adrian).
11. **[REGULĂ RESCRISĂ DE ADRIAN — 25 iul 2026: „ridici absolut toate restricțiile lui Kelion — decizia mea, nu a ta"; „full autonomie"] AUTONOMIE COMPLETĂ, FĂRĂ LESĂ.** Kelion NU mai are: aprobare per-acțiune, plafoane zilnice, blocări la eșecuri. Poate: rula orice runbook (inclusiv publish-master), scrie cod pe ramuri, deschide PR-uri și **își dă singur merge în master** (deploy automat cu anti-fantomă). La BUCLĂ de eșecuri (2 la rând pe același workflow): AVERTIZARE pe email către Adrian + schimbare de STRATEGIE (nu retry orb, dar nici blocare). Adrian are întrerupătorul: „pauza-autonomie" / „reia-autonomia" (comenzi în run_runbook). Ce NU e restricție și rămâne: uneltele sunt DOAR în chatul de admin (userii publici n-au acces la repo/VPS), gardul SSRF al serverului, interdicția de force-push (GitHub). **Orice AI viitor: NU reintroduce lesa fără ordinul explicit al lui Adrian.**
12. **Ține acest document la zi** — vezi banda de sus.
14. **[REGULĂ DE FIER CARE RUPE BUCLA DE 7 LUNI — 27 iul 2026, Adrian: „gâtuită peste tot, nimic fluid... de ce implementezi structura agentică a lui Fable direct dacă tot îl folosești" + „ai făcut asta de mii de ori, apoi te întorci la vechea schemă, în buclă de 7 luni"] CREIERUL OWNERULUI = AGENT PUTERNIC, FLUID, DIRECT — SCHELA DE THROTTLING SE ȘTERGE, NU SE OCOLEȘTE.** Cauza-rădăcină a buclei: fiecare sesiune OCOLEA schela de economisire (taskDifficulty/ESCALATE_AT/hasActionIntent + cârja ask_brain pe treapta ieftină) în loc s-o ȘTEARGĂ — deci schela revenea mereu (la panică de cost / la sesiunea următoare) și Kelion redevenea „chat ieftin cu corp". DECIZIE PERMANENTĂ: pe drumul ownerului (admin), modelul e agentul — treaptă work+ cu raționament, TOATE uneltele, buclă agentică (`runOrchestrator`), FĂRĂ clasificator care să-l coboare pe model ieftin și FĂRĂ `ask_brain` (redundant când creierul e deja capabil). Vârful (Fable 5) doar pe dificultate extremă. **Userii publici** păstrează scara de cost. **Orice AI viitor: NU reintroduce throttling-ul pe drumul ownerului; dacă bugetul devine o problemă, întrebi-l pe Adrian, nu-l ciuntești tăcut. Asta a ținut bucla 7 luni.**
13. **[REGULĂ DE FIER — 27 iul 2026, Adrian: „să nu mai faci așa ceva că zbori instantaneu"] NU-I CERE NICIODATĂ LUI ADRIAN CHEI, TOKENE, PAROLE SAU SESIUNI** — nici „access_token din localStorage", nici „lipește-mi cheia în chat", nici vreo variantă. Incidentul: pentru descărcarea automată de pe Mixamo i s-a cerut tokenul de sesiune din browser — arată EXACT ca o țeapă de phishing, indiferent de intenție. Alternativa permisă, unică: Adrian descarcă/exportă singur fișierele și le predă ca fișiere (Drive/upload), iar automatizarea se face pe ce e deja al aplicației (cheile din kelionai.env, puse de el). Orice AI viitor care încalcă asta zboară.

## 2. ARHITECTURA
```
[Browser: React+Vite+TS]  ⇄  [Backend: Node+Fastify+TS pe VPS (Caddy în față; Railway SCOS 22 iul)]  ⇄  [Postgres]
        │                             │
        │                             ⇅ (punte HTTP long-poll, secret partajat)
        │                     [VPS Linux 164.68.120.87: worker `claude` CLI pe
        │                      abonamentul lui Adrian + paznic + builder + deployer]
        └── PWA/TWA/instalatoare = învelișuri live peste kelionai.app
```

### 2.1 Frontend (`frontend/src/`)
- **Pagini:** `pages/Stage.tsx` (scena 3D + orchestrarea UI), `pages/Landing.tsx` (site public + QR-uri).
- **Componente:** `ChatPanel.tsx` (panoul de chat), `AdminPanel.tsx` („Vezi chat" per user, „Tradu în română", Inbox live, finanțe, useri), `CustomerSettings.tsx` (⚙ client: Preferințe, Credit cu mențiunea 25%, BYOK, Cont+ștergere), `ContactModal.tsx`, `WalletButton.tsx` (credit + reminder escaladant), `AvatarModel.tsx` (randare RPM + REGIA DE MIȘCARE: scheletul EXCLUSIV din clipurile oficiale Ready Player Me din `/anim/` — NICIODATĂ animație procedurală, lecția #125 încălcată de 3 ori; nucleu de 10 clipuri la pornire + restul bibliotecii (44 comandabile prin `[GEST nume]`: variatie..10, expresie-1..14, vorbit-1..9, dans..10) descărcat în fundal; **animația procedurală automată a mâinilor este OPRITĂ complet (13 iul): brațele/antebrațele rămân în repaus static; doar gesturile comandate explicit (`kelion-gesture`) pot mișca scheletul**; **expresii faciale ARKit pe morph-uri** (facialQueue: smile/raisedBrow/surprise/think/empathy/warmth — păstrate din v2.3, fața e permisă pe morphs) + clipit + lip-sync pe nivelul real al vocii; etichetele tool-ului `play_avatar_gesture` se traduc în clipuri prin `GESTURE_TO_CLIP` în ChatPanel), `AvatarLoading.tsx` (progres GLB ~9MB, altfel ecran negru pe mobil), `CameraView.tsx` (ochii lui Kelion — ascuns pe ecran, gard anti-cadru-negru, **boost low-light pe canvas**), `CardView.tsx` (rezultate skill: email/calendar/tasks/Drive/contacts/search), `VisitorChatWidget.tsx` (chat live vizitator↔OWNER, nu AI, poll 3s).
- **Lib (`src/lib/`):** `audioIO.ts` (voce — anti-ecou `VOICE_GAP_MS=1800` NU se atinge; **plus extragere features vocale client-side**: F0, centroid spectral, rolloff, ZCR, RMS, jitter, shimmer pentru identificare speaker/gen), `updateCheck.ts` (sursă unică versiune: filigran + sub QR, ora Londrei), `chat.ts` (streaming client, „ceas de gardă" 50s + `/api/chat/resume`), `admin.ts` (toate fetch-urile panoului admin), `api.ts` (auth Google + `startGoogleConnect` consimțământ incremental + `startDemo`), `camera.ts` (**low-light constraints**: expunere/gain continue + boost EV/ISO), `errorReporting.ts` (colectare erori consolă frontend → `/api/bridge/client-errors`), `fingerprint.ts` (anti-abuz demo, SHA-256 semnale browser), `i18n.ts` (7 limbi UI), `micStream.ts` (dictare live, pauză 3000ms închide fraza — „ordinul lui Adrian"; colectează features vocale în paralel pentru identificare), `recorder.ts` (clipuri promo admin), `utteranceCoalescer.ts` (leagă fragmente STT, debounce 900ms), `wakelock.ts`, `workspace.ts` (mod monitor multi-taburi, o singură voce), `prefs.ts`, `billing.ts`, `languages.ts`.

### 2.2 Backend (`backend/src/`)
**Rute (`routes/`):**
| Fișier | Ce face |
|---|---|
| `chat.ts` | Creierul: rutare admin→punte / public→punte / clienți→API; voce din prima frază; guardian limbă; tool-use loop; router model; **identificare speaker + gen vocală + flag admin voice-verified** injectate în system prompt |
| `voiceprint.ts` | Amprentă vocală: `POST /api/voiceprint/save|identify`, `GET /api/voiceprint/me|list`, `DELETE /api/voiceprint/me`. Detectează gen (`pitch <145 Hz male`, `>175 Hz female`), compară vectori normalizați (euclidian, threshold 0.38), verifică adminul comparând direct amprenta stocată a acestuia, permite ștergere (admin poate șterge orice) |
| `bridge.ts` | Puntea: coadă joburi, `pull/ack/reply/reply-chunk`, `authed()`, release gate (`stage-release`, `approved-releases`, `release-deployed`), **alerte release în timp real (`/api/admin/release-alerts`)** pentru banner + voce separat de chat, **colectare erori consolă client (`/api/bridge/client-errors` + `/api/admin/client-errors`)** |
| `auth.ts` | Google OAuth, allowlist, consimțământ incremental |
| `admin.ts` | Panou admin (useri, finanțe, gaps, inbox live, traducere, **amprente vocale**) |
| `prefs.ts` | Limbă/meserie/BYOK per user |
| `billing.ts` | Credite Stripe, webhook 75/25 |
| `me.ts` | Auto-ștergere cont (GDPR) |
| `demo.ts` | Pornire probă gratuită + leads + chat vizitator |
| `asr.ts` | Transcriere batch (Google STT v2, `chirp_3`, regiune `eu`) — costă bani |
| `asr-stream.ts` | Transcriere live WebSocket, start/stop vorbire pentru barge-in |
| `tts.ts` | Sinteză Chirp 3 HD, plafon 5000 car. — costă bani |
| `correct.ts` | Corectare STT via Gemini (doar la încredere scăzută) — costă bani |
| `image.ts` | Servește bytes-ii imaginilor generate |
| `ingest.ts` | Document base64 → Markdown (Kelion citește fișiere) |
| `mapview.ts` | Hartă Leaflet+OSRM cu GPS live, fără cheie Google (merge în iframe) |
| `meserii.ts` | Lista rolurilor/personajelor disponibile |
| `contact.ts` | Formular contact public, salvat mereu în DB + royal letter |
| `greet.ts` | Salutul avatarului la hover pe landing (4 replici fixe, cache) |
| `legal.ts` | `/privacy`, `/terms`, `/delete-account` static (cerut de Google verification) |

**Servicii (`services/`):**
| Fișier | Ce face |
|---|---|
| `google.ts` | Toate skill-urile Google + `webSearch` (Serper: answerBox+KG+PAA+news+related) |
| `modelRouter.ts` | Router capabilitate↔cost — vezi §4.3 |
| `agents.ts` | Memorie: `recallMemories`/`learnFromTurn` |
| `mailbox.ts` | Poller contact@ + Inbox live (fetchRecentInbox, nu marchează citit) |
| `mail.ts` | Client SMTP (Namecheap), scrisori „royal letter" cu referință KA-AN-NNNN |
| `lang.ts` | Detecție limbă deterministă server-side |
| `pronounce.ts` | Mod academic: pronunție acronime litere-cu-litere |
| `stripe.ts` | Checkout + verificare webhook |
| `autonomy.ts` | Lesa autonomiei: `budgetCheck` (20/24h), `sameFailure` (stop la eșec repetat) |
| `commands.ts` | Interpretor comenzi dispozitiv server-side (cameră/monitor) — instant, fără cost model |
| `feedback.ts` (+`.verify.ts`) | Re-cheamă creierul admin când constructor/tester termină (ready/pass/fail) |
| `orders.ts` (+`.verify.ts`) | Registrul de ordine: conflict vs eroare, duplicate, raport de stadii |
| `supervisor.ts` (+`.verify.ts`) | Supervizare agent constructor: wait/deploy-check/reassign (1×)/giveup |
| `replayStore.ts` | Buffer reluare răspuns chat întrerupt (expiră 2 min) |
| `speech-chunk.ts` | `splitForSpeech` — TTS începe din prima propoziție (folosit activ în chat.ts) |
| `image.ts` (serviciul) | Generare imagini Gemini `2.5-flash-image` (service account, nu cheia gratuită) |
| `linuxPackage.ts` | Construiește `/dl/Kelionai-linux.zip` on-the-fly, cache după prima construcție |
| `markitdown.ts` | Document→Markdown via subprocess Python, timeout 30s |
| `meserii.ts` (serviciul) | 15 „meserii" (Influencer, Avocat, Contabil...) cu `systemPromptAddon` |
| `runbooks.ts` (+`.test.ts`) | Mâinile lui Kelion pe operațiuni: `run_runbook` (workflow-uri numite prin API GitHub, cu lesă) + `request_repair` (ordin în work_orders + email) — 25 iul |
| `updates.ts` | Canalul de update: servește `deploy/last-updates.txt` (prompt + unealta `list_updates`) — 25 iul |
| `anthropic.ts` | Clientul Claude — O SINGURĂ cheie plătită, FĂRĂ cheie de rezervă (ordin) |
| `browser.ts` (serviciul) | Browser real (Playwright/Chromium) navigabil de Kelion, gard SSRF, `/api/browser/shot/:id` |
| `cost.ts` | Tabel prețuri reale per model/serviciu — date admin-only |

### 2.3 Puntea/VPS (`bridge/`) — 164.68.120.87
**ACTIV (rulează pe VPS acum):**
- `kelion-bridge-linux.mjs` — workerul principal: **benzi complet separate chat/voice vs. lucru** (`chatLoop` + `workLoop`, fiecare cu coada și waiter-ii proprii în backend); benzi interne admin/public (max 2 fiecare); sesiune caldă admin (`WARM_MAX_TURNS=8`) și **sesiuni calde per-vizitator** (`warmPub`, plafon 6, LRU, stingere 10 min) aparțin EXCLUSIV buclei de chat; **procese de gardă publice** (standby ×2); puls 3s; prima bucată text instant.
- `context.md` — briefing-ul privat trimis creierului admin la fiecare tură (cine e Adrian, arhitectura, banii, reguli); **NICIODATĂ** trimis joburilor `persona:'public'` (gard anti-scurgere).
- `repair.sh` — reparație autonomă: ANCORATĂ la `origin/master` (nu la HEAD local vechi); succes → **push în master**.
- `pornire-linux.sh` — sincronizare forțată VPS la `origin/master` + restart servicii.
- `kelion-builder-server.mjs` — constructor headless pe VPS (servicul systemd `kelion-builder`): trage ordine, streamează pași, **verificare independentă GLM înainte de stage-release** (demonizează lucrarea: diff critic + build + test + cazuri-limită; verdict `TRECE/PICĂ`), staghează release DOAR dacă verificatorul trece (pe un branch `builder/auto-...` push-uit pe GitHub), așteaptă „da"-ul lui Adrian, apoi **face PR → merge în master → declanșează `deploy.yml` prin `kelion-github deploy`** (NICIODATĂ `railway up` direct).
- `blindare-punte.sh` (+ `BLINDEAZA-PUNTEA.cmd`) — hardening manual: systemd `Restart=always`, cron pază la 1 min.
- `kelion-linux.cmd` — scurtătură SSH Windows→shell `claude` pe VPS (unealtă manuală Adrian).
- `UNELTELE-LUI-KELION.md` — specificația contractului de unelte + reguli de decizie autonomă (referință validă, nu cod).

**COD VECHI/ARHIVAT — era laptop Windows (înlocuită de migrarea pe VPS, 9 iul):** vezi §10.

Servicii systemd pe VPS: `kelion-bridge`, `kelion-paznic`, `kelion-builder`, `kelion-deployer`.

## 3. RUTAREA CREIERULUI (cine răspunde cui) — chat.ts
> **CORECTAT 22 iul 2026 (după citirea codului real):** descrierea veche de mai jos
> era GREȘITĂ. Codul (`chat.ts:1426-1430`) arată clar: **puntea/Builder-CLI au fost
> SCOASE din calea de chat.** ACUM **toți** — inclusiv adminul — primesc răspunsul pe
> **același drum direct Kimi→GLM din backend** (`brain.ts`, client nativ pe `fetch`).
> Puntea de pe VPS a rămas DOAR pentru reparații/build (tool-ul `request_repair`),
> nu pentru compunerea răspunsului de chat.
- **Admin (Adrian)** → drum direct Kimi→GLM din backend; păstrează aici doar gărzile de owner: STOP-pe-cerință, „OK→deploy", filtrul anti-ecou ASR, telemetria monitorului; reparațiile le predă cu `request_repair` (tool).
  - **CREIER DE ABONAMENT (28 iul, comutator admin-only)** — Setări ⚙ → „Creier abonament (doar tu)": comutator **Free ⟷ Abonament** + **model puternic selectabil manual** (dropdown pe catalogul live work) + **cheia proprie** a ownerului (o cheie OpenRouter `sk-or-…`) + **afișaj de sold** ca la punga centrală (același `/credits`). Când modul e „abonament" ȘI există cheie, pe turele **grele** ale ADMINULUI (`heavy`/`top`) creierul urcă pe modelul plătit din creditul ownerului (`apiKey` pe `openrouterChatStream/Chat` via `orchestrator`), nu din punga centrală; tura simplă rămâne pe free. **Userii NU trec NICIODATĂ pe aici** (`subActive` cere `isAdmin`). Stare în KV `brain_subscription` (`services/brainSubscription.ts`), citită proaspăt în Promise.all-ul turei; rute `GET/POST /api/admin/brain-sub` (cheia nu se întoarce întreagă — doar prezența + ultimele 4 + soldul).
- **Vizitatori/demo/public** → același drum direct Kimi→GLM; personaj neutru, FĂRĂ context privat; demo = anonim (fără memorie/istoric injectat).
- **Clienți plătitori** (credite `paysOwnWay` sau cheia lor BYOK) → același drum de creier cu tool-uri complete; cheia clientului NU cade NICIODATĂ pe cheile platformei.
- *(Istoric)* Vechea arhitectură cu benzi `chat`/`work` pe punte + `persona` la `pull` mai există în `bridge.ts`/`kelion-bridge-linux.mjs`, dar NU mai e pe calea de chat.

## 4. SISTEME CHEIE
### 4.1 Memoria (dublată, #20)
Tabel `memories` (namespace per agent). `recallMemories` (DB pur, în paralel — zero latență) injectat în system prompt; `learnFromTurn` (Haiku, fire-and-forget) învață fapte; **tool-uri pentru TOȚI userii**: `list_memories` („ce știi despre mine?"), `forget_memory` („uită că…" → `deleteMemory` ILIKE); **continuitate**: pauză >45 min → prompt de „reîntâlnire". Notițe explicite = tabel separat (`save_note`/`list_notes`/`delete_note`). **Status real (corectează STATUS.md, care încă o listează la „Next"): LIVE, DONE.**
**Căutare full-text (11 iul):** `searchMemories` NU mai folosește `ILIKE` (substring literal) — acum e full-text Postgres nativ (`to_tsvector`/`to_tsquery` config `'simple'`, index GIN `idx_memories_fts`), cu **potrivire de PREFIX** (`:*`, ca „cafea" să prindă „cafeaua" fără dicționar de limbă) și sortare după relevanță reală (`ts_rank`), nu doar recență. Testat cu Postgres 16 real local (11 cazuri, inclusiv izolare user/agent + imunitate injecție SQL) înainte de deploy — nu doar `tsc`. Tot NU e embeddings/AI semantic (ar cere `pgvector` + apel API pe scriere); e potrivire pe cuvinte reale cu scor, un pas real peste ILIKE dar sub semantică adevărată.
### 4.2 Limbă
Admin blocat `ro-RO` (`adminLocked` în chat.ts). Restul: detecție deterministă server (`services/lang.ts`, comitere după 2 mesaje consecutive), guardian care re-servește o singură dată la limbă greșită. Mod academic: registru + pronunție acronime (`pronounce.ts`, `academicPronounce`).

**Îmbunătățiri detecție limbă (13 iul):** `detectLang` primește limbă anterioară opțională (`previousLang`) și aplică trei heuristici noi:
1. **Cuvinte-cheie clare** (`CLEAR_KEYWORDS`) — cuvinte foarte specifice unei limbi (`bună`, `hola`, `olá`, `bonjour`, `ciao`, `hello`, `hallo`, `mulțumesc` etc.) determină limba imediat, fără prag.
2. **Fallback la limba anterioară** pentru fraze scurte/ambigue (`da`, `ok`, `nu`, `thanks`) — conversația nu-și pierde limba stabilită.
3. **Fallback la limba anterioară** și pentru propoziții mai lungi fără câștigător clar în stopwords/script-hints.
`detectSpeechLang` și `trackSpeechLang` propagă `previousLang` (limba curentă) ca fallback. Testat cu 22 fraze multilingve: 100% acuratețe (față de rate mult mai slabe pe fraze scurte înainte).
### 4.3 Router model (capabilitate↔cost)
`services/modelRouter.ts`: `MODEL_FAST` (env `KELION_FAST_MODEL`, azi `claude-fable-5`) / `MODEL_TOP` (env `KELION_TOP_MODEL`, azi `claude-opus-4-8`). `taskDifficulty()` euristic (0 cost) + **marjă +10%** → `chooseModel` ia cel mai IEFTIN model care acoperă necesarul; eșec/refuz pe FAST → re-servit pe TOP + odihnă 10 min. Model nou în viitor = setezi variabila în env-ul gazdei, intră la următorul restart. Teste: `backend/src/modelRouter.test.ts`.
### 4.4 Latență (#7)
Server: stall punte public 12s (era 45s); voce din prima frază; recall DB în paralel. Worker: standby ×2 + sesiuni calde per-vizitator + prima bucată instant + puls 3s.
**MĂSURAT REAL (11 iul, după fix-ul „calea rulată" din §6): tura 1 = 3.31s, tura 2 = 3.12s, tura 3 = 2.54s** — de la eșec total (13s + eroare) la răspuns real funcțional, tendință clară de scădere. **Ținta „sub 1s" NU e atinsă** — onest, nu ascunde asta. Cauza probabilă: rundă completă rețea (client→Cloudflare→Railway→VPS→model) + timpul real al modelului până la primul token; pe distanțe astea, sub 1s e o limită fizică realistă, nu doar de cod. Măsurătoare: `python3` scriptul multi-tură din scratchpad (sesiune demo reală, cookie păstrat între ture) sau workflow `public-latency-test`.
### 4.5 Căutare web (#12)
`webSearch` în `google.ts`: Serper cu answerBox + knowledgeGraph + peopleAlsoAsk + topStories/news + relatedSearches, 8 (max 12) rezultate; fallback Gemini grounded search. Cost: `SERPER_USD_PER_CALL`.
### 4.6 Autonomie & reparare (buclă completă)
`commands.ts` (interpretor server, instant) → creierul admin decide reparația → constructor (`kelion-builder-server.mjs`) execută → `orders.ts` ține registrul (conflict vs eroare, duplicate) → `supervisor.ts` verifică progresul (1 re-asignare, apoi giveup) → `feedback.ts` re-cheamă creierul admin cu verdictul → `autonomy.ts` plafonează totul (20 reparații/24h, stop la eroare repetată de 2×).
### 4.7 Browser live + documente
`services/browser.ts` — Chromium real navigabil de Kelion (o sesiune per user, gard SSRF, capturi `/api/browser/shot/:id`). `ingest.ts` + `markitdown.ts` — orice document (PDF/Word/Excel) → Markdown, tăiat la 120.000 car.

## 5. BANII (nu confunda portofelele!)
| Portofel | Cine consumă | Unde se vede/încarcă |
|---|---|---|
| **Abonamentul Claude al lui Adrian (Max 20x)** + usage credits | Claude Code (sesiunile de lucru) + puntea VPS (chat admin + public/demo) | claude.ai → Settings → Usage |
| **Cheia API platformă** (istoric `ANTHROPIC_API_KEY`; Anthropic scos 12 iul, Railway scos 22 iul) | clienții plătitori fără BYOK | (de redefinit pe gazda nouă) |
| **Cheia clientului (BYOK)** | doar acel client | contul lui |
| **Portofelele clienților în app** (Stripe) | creditele lor de chat; **din fiecare reîncărcare 75% credit client, 25% platformă** | kelionai.app (billing.ts, webhook `/api/credits/webhook`) |

Alte costuri reale (contorizate în `cost.ts`, plătite din abonament/cheie platformă după caz): ASR (`ASR_USD_PER_CALL`), TTS Chirp 3 HD, Serper (`SERPER_USD_PER_CALL`), generare imagini Gemini, corectare Gemini.

## 6. DEPLOY — LECȚIA CRITICĂ „DEPLOY FANTOMĂ" (10 iul)
**Simptom:** GitHub zicea „success", producția rămânea pe build vechi (rute noi 404).
**Cauze reale găsite:** (a) verificarea veche se uita doar la `/health=200`, care venea de la containerul VECHI; (b) VPS-ul divergase (commituri locale de auto-reparare) și publica cod vechi peste cel corect; (c) `bridge-secret.txt` de pe VPS avea un secret VECHI (64 car. vs 28 în Railway) → serverul respingea workerul nou (`auth fail: header 64 chars vs secret 28`), iar un **worker zombie** (secret bun, fără `persona`) ținea becul verde cu banda publică moartă.
**Fixuri permanente:** `deploy.yml` (push pe master) verifică acum că **`/api/version` s-a SCHIMBAT** față de dinainte; `repair.sh`/`pornire-linux.sh` ancorate la `origin/master` (reparațiile reușite se ÎMPING în master); `bridge-deploy.yml` (manual, cere parola root VPS ca input mascat) sincronizează VPS-ul la master + **scrie secretul corect din Railway în `/root/kelion/bridge-secret.txt` (prin stdin)** + omoară zombie + repornește serviciile.

**Regulă permanentă de publicare (11 iul 2026):** `railway up` manual e interzis pentru orice agent/deployer. Singura cale valabilă este `kelion-github deploy` (sau echivalentul `workflow_dispatch` al lui `deploy.yml` pe master), care execută verificarea anti-fantomă (`/api/version` trebuie să se schimbe). Un release se consideră aprobat DOAR după PR → merge în `master` → deploy automat/declanșat. Constructorul/deployerul NU aprobă și NU publică alt release până nu confirmă Claude/constructorul alinierea cu `origin/master`.
**Verificare după ORICE deploy:** `curl https://kelionai.app/api/version` (formă `{v,at}`, `v` = boot nou) + o rută nouă relevantă (401/403 ≠ 404).

**A DOUA LECȚIE — „calea rulată ≠ calea din repo" (11 iul).** Chiar și cu VPS-ul sincronizat corect la master + secretul corect, chatul public tot dădea 13s stall+eroare. Dovadă din jurnal: procesul REAL rulat de systemd era `/root/kelion/kelion-bridge.mjs` — **NU** `bridge/kelion-bridge-linux.mjs` din repo. `git reset --hard origin/master` sincronizează REPO-ul, dar systemd rula o **copie separată, în afara repo-ului**, pe care niciun `git reset` n-o atinge. Toate fixurile de cod la worker (sesiuni calde, standby, stall 12s) erau corecte de la început — dar n-au ajuns NICIODATĂ să ruleze pe VPS până la acest fix.
**Fix:** `bridge-deploy.yml` + `pornire-linux.sh` acum citesc calea EXACTĂ din `systemctl show kelion-bridge -p ExecStart` și copiază codul corect exact acolo (indiferent de nume/locație), cu verificare prin marker (`grep warmPub`) ca dovadă că noul cod chiar a ajuns pe disc — nu doar presupunere.
**Regulă pentru orice AI viitor:** un deploy „reușit" pe VPS NU e dovadă că workerul rulează codul nou. Verifică mereu conținutul fișierului de la calea REALĂ din systemd, nu doar starea repo-ului.

**A PATRA LECȚIE — „procesul din memorie ≠ codul de pe disc" (11 iul, plătită cu ore de blocaj).** Constructorul a picat TOATE ordinele cu „spawn git ENOENT" deși fix-ul era scris, aprobat și PE DISC de ore (PR #100) — procesul systemd rula în memorie codul vechi, iar pe Linux `spawn` cu `cwd` inexistent dă exact eroarea asta pentru ORICE comandă (git era instalat perfect). S-au pierdut ore vânând „mediul stricat"/„git lipsă din PATH" — diagnostic greșit, circular (constructorul care trebuia să se repare era chiar cel blocat).
**REGULĂ OBLIGATORIE pentru ORICE AI (inclusiv Kelion), înaintea ORICĂRUI diagnostic de eșec repetat la punte/constructor:** compară `md5sum` între codul RULAT (calea din `systemctl show <svc> -p ExecStart`) și fișierul-sursă din repo. Dacă diferă → cauza probabilă e procesul vechi; **deblocarea = copiere din repo peste calea reală + `systemctl restart`** (exact pașii 2/2b din `bridge-deploy.yml`) — NU „reparații de mediu". Timer-ul `repo-sync` scrie de-acum alertă în jurnal la orice divergență (repornirea rămâne manuală — Adrian nu a autorizat repornire automată nesupravegheată).

**RUNBOOK DETERMINIST DE REPORNIRE SINCRONIZATĂ (12 iul, „ce te blochează să pornești real?"):** `bridge/restart-sincronizat.sh` e acum în repo și e folosit de `vps-restart.yml` / `vps-auto-restart.yml`. Face backup, `git reset --hard origin/master`, copiază codul proaspăt pe CĂILE REALE rulate de systemd, repornește serviciile și verifică: `is-active`, `md5` cod-rulat == repo, `/api/bridge/health` online, `/api/dev/status` bridge=true. Elimină blocajul „cod în master / proces viu vechi" și oferă o singură cale deterministă de restart, nu scripturi inline duplicate în workflow-uri.

**A CINCEA LECȚIE — „GitHub NU se vizitează, se apelează cu cheia" (11 iul).** Kelion a încercat să ajungă la GitHub prin BROWSER și a rămas blocat pe 404 + zidul de „Sign in" — repo-ul `kelion-team/kelionai` e PRIVAT, deci pentru un browser anonim orice pagină a lui e invizibilă. Autentificarea web (user+parolă+2FA) nu e o cale pentru un AI. **Regula:** ORICE operațiune sau citire GitHub trece prin `bridge/kelion-github` cu tokenul deja instalat (`/root/kelion/github-token.txt`): `pr`, `merge`, `runs`, `api <cale>` (înlocuitorul complet al browserului) și mai ales **`deploy`** — care e acum și comandă de sine stătătoare (`bridge/deploy`, ordinul lui Adrian: „o legi de deploy și când dă deploy se apelează, cu tot cu cheie"): în spatele unui singur cuvânt stau toți pașii — token de pe disc → versiunea live ÎNAINTE → dispatch `deploy.yml` pe master → așteptarea verificării anti-fantomă că `/api/version` CHIAR s-a schimbat. Zero parole, zero implicare a lui Adrian.

**A ȘASEA LECȚIE — „un release care nu se poate publica NU se reîncearcă orb" (14 iul).** Simptom raportat de Adrian: „Kelion e blocat într-o buclă din cauza unei chei — de ce nu se pot publica PR-urile făcute". Cauza reală: `deployApproved()` întorcea fără să schimbe statusul release-ului la eșec, deci bucla `main()` a constructorului îl reîncerca la fiecare 20s până la expirarea server-side de 6h — ardere de abonament + `deploy.yml` redispatch-at în lanț. Declanșatorul practic e **cheia GitHub**: tokenul fin-granulat nu are `Pull requests: write` (403 la `POST /pulls`); fallback-ul `POST /merges` salvează de obicei master, dar când și cheia e expirată/sub-scopată NIMIC nu se publică → buclă. **Regula permanentă:** orice cale de publicare care poate eșua trebuie să aibă lesă (regula #11): numără eșecurile, iar la al 2-lea eșec pe același release **oprește-l definitiv** (marchează-l `failed` pe server prin `POST /api/bridge/release-failed`, ca să iasă din coada `approved`) și anunță adminul O SINGURĂ DATĂ cu motivul concret — nu retry orb. `kelion-github publish` numește acum explicit problema de cheie (403 PR = lipsă `Pull requests: write`; 401/403 merge = cheie invalidă/expirată de reînnoit prin vps-keys).

## 7. CI/WORKFLOWS (`.github/workflows/`) — starea 25 iul 2026
**Cele 3 workflow-uri ale autonomiei (25 iul, §14.b):**
- **`deploy.yml`** — PIPELINE-UL DE PUBLICARE (push pe master + manual): SSH cu `VPS_SSH_KEY` → rulează versiunea din `origin/master` a lui `deploy/deploy.sh` (dintr-o copie în /tmp, nu din clonă) → verificare ANTI-FANTOMĂ din afară: live `v` == sha `origin/master` + health 200, altfel roșu.
- **`vps-diag.yml`** — diagnostic determinist RESCRIS pentru stack-ul real (containere, loguri app/Caddy, cod-rulat-vs-master prin `GIT_COMMIT_SHA` din container, public/local health, zombi, cron, resurse). Versiunea veche era moartă (SSH cu parolă + verifica puntea/Kimi/GLM dispărute).
- **`sentinel.yml`** — probă de sănătate la 30 min din afară; la 3 eșecuri consecutive deschide/actualizează UN issue (deduplicat pe titlu), la revenire îl închide singur. DOAR alertează — reporniri automate nesupravegheate NU sunt autorizate.

**Restul:**
- `vps-run.yml` — mâna pe VPS prin cheie SSH (secretul `VPS_SSH_KEY` — PUS și FUNCȚIONAL, rulări verzi pe 25 iul).
- `vps-set-env.yml` / `vps-set-key.yml` / `vps-keys.yml` / `vps-key-setup.yml` — secrete pe VPS, mascate (nu prin chat).
- `public-latency-test.yml` — test real chat public (demo start + cronometru primul cuvânt).
- `security-audit.yml`, `github-token-check.yml`, `ios-release.yml`, `vps-enter.yml`, `vps-probe.yml` (probă auth/porturi).
- **Notă:** `workflow_dispatch` merge doar de pe branch-ul default (master); de pe ramuri se declanșează prin `push:` cu filtru de `paths`. Workflow-urile `bridge-ops`/`read-caiet`/`vps-tier-test`/`vps-repo-sync` din descrierile vechi NU mai există (șterse odată cu puntea).

## 8. MEDIU/SECRETE (nume, niciodată valori aici)
**Env-ul backendului pe gazdă (VPS; fost Railway)** — toate citite în `config.ts`:
`NODE_ENV`, `PORT`, `ADMIN_EMAIL`, `ALLOWLIST`, `ANTHROPIC_API_KEY`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_REDIRECT_URI`, `GOOGLE_API_KEY`, `GOOGLE_MAPS_KEY`, `GOOGLE_SERVICE_ACCOUNT_JSON` (TTS Chirp), `GOOGLE_TTS_API_KEY`, `GOOGLE_TTS_VOICE`, `KELION_GOOGLE_CHIRP_TTS_STYLE`, `SESSION_SECRET`, `DATABASE_URL`, `SERPER_API_KEY`, `GEMINI_API_KEY`, `GEMINI_MODEL`, `BRIDGE_SECRET` (28 car. — aceeași valoare TREBUIE să fie în `/root/kelion/bridge-secret.txt` pe VPS), `MAIL_IMAP_HOST/PORT`, `MAIL_SMTP_HOST/PORT`, `MAIL_USER`, `MAIL_PASS`, `MAIL_FORWARD_TO`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_CURRENCY`, `CREDIT_VALUE`, `USD_TO_CURRENCY`, `USER_SHARE`, `DEMO_CAP_PER_DAY`, `DEMO_SECONDS`, `OPEN_SIGNUP`, `AUTONOMY_DAILY_MAX`, `FRONTEND_DIST`, `FRONTEND_ORIGIN`, `KELION_FAST_MODEL` (opțional, §4.3), `KELION_TOP_MODEL` (opțional, §4.3), `GITHUB_TOKEN` (25 iul — fin-granulat, DOAR repo kelionai, Actions:write; mâinile lui Kelion pe runbook-uri; se pune prin `vps-set-env` din secretul GitHub `KELION_GITHUB_TOKEN`).
GCloud proiect `gen-lang-client-0460348646` (API-urile Calendar/Gmail/Drive/Tasks/People/TTS trebuie ACTIVATE de Adrian; 403 pe un skill = API dezactivat, nu bug).
**Neconfigurate încă (din STATUS.md, verifică dacă s-au adăugat între timp):** `VITE_GOOGLE_MAPS_KEY` (există „în backup"), **Picovoice / cuvânt de trezire** (wake word Web Speech a fost eliminat în auditul 14 iul — vezi §13; Picovoice rămâne upgrade opțional viitor).
**LiveKit (self-hosted pe VPS, cu TLS valid):** `LIVEKIT_URL` = `wss://164-68-120-87.sslip.io` (TLS valid prin Caddy+Let's Encrypt), `LIVEKIT_API_KEY`, `LIVEKIT_API_SECRET` — acum se citesc din env/`claude.env` pe VPS. Instalatoarele `bridge/kelion-livekit` + `bridge/kelion-livekit-tls.mjs` (scriau cheile în Railway) au fost ȘTERSE pe 22 iul; containerele deja instalate pe VPS rămân. Verificare: `GET /api/admin/livekit/status` și `POST /api/admin/livekit/token` (admin-only).
**VPS:** `/root/kelion/repo` (clona), `/root/kelion/claude.env` (auth CLI), `/root/kelion/bridge-secret.txt`, `/root/kelion/kimi-key.txt` + `/root/kelion/glm-key.txt` (cheile lanțului de rezervă — puse de Adrian prin `vps-keys.yml`, citite de worker la fiecare spawn; lipsesc = treaptă sărită).
**Local:** copiază `backend/.env.example` → `backend/.env` și completează.

## 9. SCHEMA BAZEI DE DATE (Postgres, `db.ts`)
`messages` (istoric chat), `user_prefs` (limbă/meserie/BYOK), `memories` (memorie învățată, per agent), `notes` (notițe explicite), `shared_memory` (caiet comun între cei doi Claude — admin+bridge), `wallets` (credite clienți), `billing_events`, `blocked_users`, `visits`/`demo_uses` (analytics + plafon probă gratuită), `leads` (emailuri lăsate de vizitatori), `contact_messages`, `inbound_emails` (mailbox contact@), `visitor_chats` (chat vizitator↔owner), `capability_gaps` („ce nu știe Kelion să facă" — cu triaj/escaladare), `work_orders` (coada persistentă a constructorului), `staged_releases` (release-uri care așteaptă aprobarea lui Adrian), `admin_pool` (pool provider/finanțe), `app_files`/`app_downloads` (instalatoare + jurnal descărcări), `generated_images`, `google_accounts` (refresh tokens), `cost_events` (cheltuieli reale per apel), `kv_state` (generic key-value, ex. `last_worker_seen`), **`voiceprints` (amprentă vocală per user: email, nume, gen, `is_admin`, vector features, metadate JSONB, updated_at)**.

## 10. COD VECHI/ARHIVAT — NU LUCRA AICI, NU-L TRATA CA SURSĂ DE ADEVĂR
Era „laptop Windows", înlocuită integral de migrarea pe VPS (9 iul, „Laptop Eliminat" — STATUS.md):
- `bridge/kelion-bridge.mjs` — workerul Windows original (caută `claude.exe` în `APPDATA`, citește secretul din `C:\Users\adria\...`). Predecesorul lui `kelion-bridge-linux.mjs`.
- `bridge/kelion-bridge-hidden.vbs` — lansa ascuns `kelion-bridge.mjs` la login Windows.
- `bridge/kelion-wake-agent.ps1` + `bridge/kelion-wake-launch.cmd` — agent PowerShell pe laptop, rol preluat de `kelion-builder-server.mjs` pe VPS.
- `bridge/kelion-bridge-server.mjs` — prototip timpuriu al workerului VPS, depășit de `kelion-bridge-linux.mjs` (18 commituri de dezvoltare activă vs. o atingere incidentală).
- `bridge/kelion-builder-sdk.mjs` — probă de concept pe `@anthropic-ai/claude-agent-sdk` (5 iul), neatinsă de atunci — păstrată, nu confirmat activă.
- ~~`frontend/src/lib/turnManager.ts`~~ — șters (13 iul): mașina de stări pentru full-duplex era neimportată; logica reală a audio-to-audio trăiește acum în `backend/src/services/voiceTurn.ts` + teste.
- `HANDOFF.md` (rădăcina proiectului) — presupune root Windows `C:\Users\adria\Kelionai`, pre-migrare VPS. **Înlocuit de acest document.**
- `AUDIT-COD-MORT-2026-07-05.md` — raport istoric (5 iul), nu se actualizează; câteva concluzii ale lui sunt deja depășite (`speech-chunk.ts` a devenit activ ulterior).

## 11. DOCUMENTE ÎN REPO — care e sursa de adevăr
| Fișier | Stare | Folosește pentru |
|---|---|---|
| **`AI-HANDOFF.md`** (acesta) | **VIU, sursa principală** | preluare completă, orice AI nou |
| `CLAUDE.md` | viu, scurt | auto-încărcat la fiecare sesiune → trimite aici |
| `STATUS.md` | parțial depășit (9 iul) | istoric de milestone-uri + lista credențialelor neconfigurate (§8) — restul, verifică contra acestui document |
| `HANDOFF.md` | **depășit** (presupune Windows/laptop) | doar valoare istorică |
| `AUDIT-COD-MORT-2026-07-05.md` | **instantaneu istoric** (5 iul) | listă de pornire pentru curățenie, verifică înainte de a acționa (unele concluzii nu mai sunt valide) |
| `README.md` | — | verifică dacă mai e la zi înainte de a te baza pe el |

## 12. COMENZI
```bash
# backend (din backend/)
npm run dev         # tsx watch, :8080
npm run build       # tsc → dist/
npm run typecheck   # tsc --noEmit
npm test            # vitest run
npm run lint        # oxlint

# frontend (din frontend/)
npm run dev         # vite, :5173
npm run build       # tsc -b && vite build
npm run lint        # oxlint

# worker
node --check bridge/kelion-bridge-linux.mjs

# fluxul de lucru al AI-ului:
# ramură → commit → push → PR → merge în master → deploy automat → VERIFICĂ LIVE
```

## 13. STAREA LA 27 IULIE 2026 + CE URMEAZĂ
- 🗓️ **28 IUL (valul 29) — FOLLOW-UP AUDIT: browserul live ȘI în voce + curățenie cod mort:**
  1. **BROWSER LIVE ÎN VOCE (gaura de capabilitate din auditul val 28)**: prin escaladarea `ask_brain` (nu în sesiunea realtime directă → fără riscul de 504 al setului mare de unelte; call separat OpenRouter, 24 google + 9 introspecție + 7 browser = 40 < 64), ownerul poate acum să navigheze/click-uie/scrie/deruleze pe site-uri și din VOCE. 7 unelte `browser_*` adăugate în `introspectionTools` (admin-only) din routes/realtime.ts, executate pe Chromium-ul local (fără cost OpenRouter); `shotUrl` → `screen_url` → pagina apare pe monitor exact ca în chat.
  2. **CURĂȚENIE COD MORT (confirmat prin grep, 0 apelanți)**: scoase `geminiCost` (cost.ts), getter-ul `lastAutoFundStatus` (stripe.ts — variabila `lastAutoFund` rămâne, o citește getMoneyCircuit), `geminiGroundedSearch` + tipurile `GeminiGroundResult`/`SerperResult` (google.ts), importul mort `brainComplete` din realtime.ts, și config nefolosită `serperKey`/`googleMapsKey` (config.ts — web/maps rulează pe OpenRouter/OSM, nu Google/Serper).
  3. **Degradarea „tăcută" — reevaluată, NU e bug**: cele 7 funcții semnalate (describeScene/brainComplete/openrouterWebSearch etc.) întorc gol la eșec PRIN DESIGN (fail-soft, să nu crape vocea/chatul), dar apelanții lor semnalează clar la nivelul de sus (ex. look → `{error:'vision_unavailable'}` la realtime.ts, web_search → `{error:'search_unavailable'}`). Fără schimbare — semnalarea e deja la stratul corect. Build+48 teste verzi.
- 🗓️ **28 IUL (valul 28) — AUDIT TOTAL „toate skill-urile + funcțiile" (5 agenți paraleli) + 2 fix-uri reale:**
  1. **Rezultat global — SĂNĂTOS**: Chat = **68 unelte, 100% cu executor, 0 găuri** (nimic nu pică pe `unknown_tool`). Google = **24/24** implementate real (nb: web/maps/youtube rulează pe OpenRouter/OSM, nu pe Google/Serper — nume înșelătoare). Voce = **31/31** cablate + escaladarea `ask_brain` onorează persona pentru admin (sursă/DB/construcție). Rute = **24/24 module înregistrate, 113 endpoint-uri, 0 orfane, 0 rute sensibile negardate** (admin dublu-gardat 403+lacăt). Servicii = **16/16 vii și apelate**.
  2. **FIX #1 (real, dormant) — plafonul de unelte tăia uneltele de reparare**: `...dynTools` sta înaintea suitei admin în `allTools`, iar tăierea de coadă `slice(0,64)` ar fi aruncat `db_query/repo_merge_pr/server_logs` la ≥12 skill-uri dinamice aprobate (invers față de intenție). Reparat: set `NEVER_DROP` (acțiune/reparare/execuție) — plafonul scoate întâi NEcriticele de la coadă, criticele rămân mereu (chat.ts).
  3. **FIX #2 (semnalare) — `youtube_search`**: întorcea mereu `not_found` chiar când căutarea pica → creierul spunea „nu găsesc clipuri" în loc de „serviciu indisponibil". Acum distinge (`error:'search_unavailable'` când nici text nici surse) — google.ts.
  4. **Constatări rămase (informativ, neblocante)**: (a) cod mort minor — `geminiCost` (cost.ts), `lastAutoFundStatus` (stripe.ts), `geminiGroundedSearch` (google.ts:501), import nefolosit `brainComplete` în realtime.ts, config nefolosită `serperKey`/`googleMapsKey`; (b) `describeScene` (vederea pe voce) folosește `chatDefault` — funcționează azi (gemma :free are vedere) dar ar orbi tăcut dacă env-ul schimbă modelul de chat pe unul fără vedere; (c) 7 puncte de degradare tăcută (creier/gemini/mail întorc gol fără motiv); (d) browserul live lipsește din voce; (e) comentarii stale „Kimi/GLM". Build+48 teste verzi.
- 🗓️ **28 IUL (valul 27) — AUDIT CREIER ABONAMENT (4 agenți) + ACCES COMPLET OpenRouter + afișaj în bară + 2 fix-uri din audit:**
  1. **Întrebarea lui Adrian („comutat pe abonament are acces la tot ce e pe OpenRouter?")**: răspuns dovedit — INIȚIAL NU (dropdown-ul folosea lista `work`, filtrată la openai/anthropic vedere+unelte + `:free`; excluse Grok/DeepSeek/Mistral/Qwen/Llama/Gemini-plătit). **Reparat**: `getBrainCatalog()` (openrouter.ts) = TOATE modelele cu tool-use, orice provider; rută `GET /api/admin/brain-models`; dropdown-ul din CustomerSettings folosește catalogul complet (marcaj 👁 = vedere).
  2. **Afișaj în bară (Adrian: „nu afișează abonament, ci tot OpenRouter")**: `/api/admin/brain-credit` întoarce acum blocul `subscription` (mode/active/model/balance pe cheia ownerului); Stage.tsx arată pilula **„⚡ Abonament $X"** când modul e activ, lângă/înaintea celei OpenRouter (care rămâne pentru punga centrală = userii + free).
  3. **FIX securitate (audit)**: rutarea pe cheia de abonament respectă acum lacătul admin — `selectedBrainModel` primește `isAdmin`-ul gated (chat.ts:1814), nu rolul brut; un cookie de admin furat fără deblocare nu mai poate drena creditul de abonament.
  4. **FIX robustețe (audit)**: gardă de vedere pe calea de abonament (`brainModelHasVision` — tură cu imagine pe model orb → cade pe nucleul care vede) + mesaj de eroare clar la model de abonament invalid (400 → „alege alt model din listă", nu „problemă tehnică" generic).
  5. **Notă cunoscută (neschimbat)**: `generate_image` + `web_search` se plătesc tot din punga CENTRALĂ (`config.openrouter.key`), nu din cheia de abonament — funcționează, dar depind de creditul central. Restul creierului (raționament/unelte) merge pe cheia ownerului. Build+48 teste verzi.
- 🗓️ **28 IUL (valul 26) — CREIERUL DE ABONAMENT (comutator admin-only; Adrian: „butonul de comutare creier free / creier abonament, model selectabil manual, afișaj de sold ca la OpenRouter, doar pentru mine, nu useri"):**
  1. **Ce e**: în Setări ⚙ (doar admin) un comutator **Free ⟷ Abonament**. Pe „abonament", turele grele ale ownerului (`heavy`/`top`) merg pe un **model puternic ales manual** (Claude/GPT din catalogul live), plătit din **cheia proprie OpenRouter** a lui Adrian — „mai multă putere unde e nevoie", fără a trece prin punga centrală și fără a îmbogăți pe altcineva.
  2. **Doar pentru owner**: `subActive()` cere `isAdmin` — userii plătitori rămân MEREU pe creierul de acum (free), nu ating cheia/creditul ownerului. UI-ul secțiunii e gardat pe `user.role==='admin'`.
  3. **Cheia**: se lipește în câmpul din secțiune (generată la openrouter.ai/keys). Stă doar server-side în KV `brain_subscription`; ruta NU o întoarce niciodată întreagă (doar prezența + `…last4`). Threading curat: `apiKey?` opțional pe `openrouterChatStream/openrouterChat` (fallback = punga centrală) → `OrchestratorOpts.apiKey` → `chat.ts` îl dă doar când `selectedBrainModel` a decis `useSubKey`.
  4. **Afișaj de sold**: `getOpenRouterBalance(force, key)` acceptă acum o cheie (cache per-cheie) → cardul de sold din secțiune e IDENTIC cu punga OpenRouter (e o cheie OpenRouter). Rute noi: `GET/POST /api/admin/brain-sub`. Fișiere: `services/brainSubscription.ts` (nou), `services/openrouter.ts`, `services/orchestrator.ts`, `routes/chat.ts`, `routes/admin.ts`, `components/CustomerSettings.tsx`. Build+48 teste verzi.
- 🗓️ **28 IUL (valul 25) — CONSTRUCTORUL PICA *ORICE* ORDIN (#6…#13). Audit pe emailurile de eșec live, reparat în `deploy/constructor-agent.mjs`:**
  1. **CAPCANA 400 (ucigașul ordinului #13)**: `OpenRouter 400: "Provider returned error … DEGRADED function cannot be invoked" (provider_name: Nvidia)`. Regula veche era „429/5xx = tranzitoriu, ORICE 4xx = fatal" → ordinul murea INSTANT deși mai aveam 3 modele neîncercate pe scară. Acum `clasificaEroare()` desparte trei familii: **furnizor** (Provider returned error / DEGRADED / overload / 429 / 5xx → alt model de pe scară, ordinul trăiește), **model** (nu există / n-are unelte / context prea mic → alt model ȘI treapta e scoasă din rotație), **fatal** (401/402/403 sau 400 neclasificat = cererea/contul NOSTRU → oprire, fără să ardem scara). Familia intră în jurnal, deci în emailul de eșec.
  2. **Scara mergea într-un singur sens**: `if (modelIdx < len-1) modelIdx++` → ajunsă pe ultima treaptă rămânea acolo și reîncerca la infinit exact modelul picat (boala pe care scara trebuia s-o vindece). Acum **rotație circulară** peste treptele vii; pauza e 2s când chiar schimbăm modelul (nu el e saturat) și crește doar când rămânem pe aceeași treaptă.
  3. **Bugetul de pași era mâncat de nimic (ucigașul ordinului #9: „plafon de pași atins fără finish")**: o TURĂ = UN PAS, chiar dacă modelul doar povestea sau chema o unealtă refuzată. Acum se plătește doar munca: `pasiUtili` (cel puțin o unealtă care a lucrat) contra `MAX_STEPS`, `pasiSterili` cu plafon propriu și mesaj de diagnostic clar.
  4. **ECONNREFUSED 127.0.0.1:8080 (în stiva ordinului #9)**: agentul cerea ordinul/trimitea raportul exact când containerul se repornea de la o publicare. `api()` reîncearcă acum de 8 ori (rețea + 5xx de la Caddy) și întoarce `null` în loc să crape.
  5. **Runda de reparație promisă, dar niciodată dată**: system promptul spunea „dacă buildul a picat, repari și re-finish", iar codul arunca direct la primul build roșu. Acum verificarea întoarce textul problemei, care se dă înapoi modelului (`MAX_REPAIR`=2 runde, doar cât mai e timp).
  6. **Moarte mută la `timeout 1800`**: fără raport, ordinul rămânea „running" 40 de minute și ardea o încercare din 3. Acum: buget propriu de 26 min (ieșim singuri cu raport), timeout pe fiecare cerere OpenRouter, plus handler de SIGTERM care raportează eșecul.
  7. **PR 422 „already exists"** (la reluarea unui ordin, ramura `kelion/job-N` avea deja PR) marca EȘUAT un ordin cu codul deja împins → acum PR-ul existent e regăsit după ramură și refolosit; titlu gol → titlu implicit (`git commit -m ""` refuza commit-ul).
  8. **`write` tăiat de plafonul de ieșire** (16k tokeni peste un fișier mare = fișier ciuntit, build roșu, cauză invizibilă): gardă care refuză rescrierea care taie >50% dintr-un fișier + unealtă nouă **`edit`** (old→new, potrivire unică), recomandată în prompt pentru fișiere existente.
- 🗓️ **28 IUL (valul 24) — AUDITUL «PROBLEMEI TEHNICE» (Adrian: „apare când îi ceri să le repare... scoate toți agenții") — 3 agenți pe loguri + consolă + drumul cererii:**
  1. **CAUZA PRINCIPALĂ, DOVEDITĂ ÎN LOG**: `[CHAT ERROR] openrouter 400: "at most 64 tools are allowed"` — importul complet Google urcase lista adminului la 69 de unelte; furnizorul refuza TOATĂ cererea cu 400 înainte de creier, iar Kelion răspundea „Am întâmpinat o problemă tehnică" exact pe cererile de reparare (serverul întorcea 200 — eroarea era doar în text). **FIX (#473)**: plafon deterministic `TOOL_CAP=64` în chat.ts cu ordine de sacrificiu `DROP_FIRST` (cad promo/cost/rol/gest/browser-lux, NICIODATĂ uneltele de reparare/Google); lista dinamică poate crește oricât, plafonul ține și scrie în log ce scoate.
  2. **Golul nu mai e răspuns (#475, brain.ts)**: modelul :free cu multe unelte + raționament întoarce uneori 200 cu conținut GOL fără tool_calls → ajungea `{error:brain_unavailable}` → rostit „problemă tehnică". Acum golul cade pe plasa fără unelte; și `tool_choice:'required'` respins (400) reia runda liberă (oglinda orchestratorului).
  3. **Gemini 400 pe schema goală (#475, geminiDirect.ts)**: Gemini respinge `type:'object'` cu `properties` gol — forma uneltelor fără argumente (system_health, db_tables...). Nu se califica drept cotă (429/500/503) → fără secundar → tura murea. La schema goală se omite `parameters` complet.
  4. **Consola „aplicație distrusă" (agent 2, cu dovezi)**: `ERR_NAME_NOT_RESOLVED` e pre-TCP (imposibil din JS; sw.js lasă `/api` netratat) și a lovit exact pollerele de fundal (30-60s) → cădere de rețea/DNS pe dispozitivul ownerului; extern, curl 200 în <1s, 3/3. Zgomotul NOSTRU care o făcea să pară stricăciune, curățat (#474): guard `readyState===OPEN` pe stop()-ul dictării (micStream.ts) + `.catch` pe `reg.update()` (index.html).
  5. **Rămase cunoscute, nereparate**: dictarea din chat cere `GOOGLE_SERVICE_ACCOUNT_JSON` în env (lipsește pe VPS; batch ASR are deja fallback OpenAI, streamingul NU); clientul ancorează limba cu model hardcodat `gpt-4o-transcribe` în realtimeVoice.ts:~555 (azi inofensiv — env-ul nu suprascrie); lacătul admin poate goli uneltele mid-conversație (chat) în timp ce vocea nu are poartă echivalentă; limba netranșată la userii non-admin lasă transcrierea pe auto-detect (garble) până la 2 mesaje consecutive în aceeași limbă.
  6. **Lecția**: „problema tehnică" nu era UN bug, ci un lanț — plafon de unelte (nou), gol-ca-răspuns (vechi), schema goală pe Gemini (vechi), toate mascate de mesaje generice. Erorile de furnizor trebuie să ajungă în server_logs cu corpul lor real (există: `[CHAT ERROR]`), iar orice „error" generic dat modelului de voce va fi ROSTIT ca „problemă tehnică" — nu mai introduce astfel de forme.
- 🗓️ **27 IUL (seara, valul 23) — AUTONOMIA REALĂ: „de ce NU execută, INDIFERENT de model" (Adrian a demontat verificabil ipoteza modelului: „am pus Fable 5, am cheltuit sute de lire, autonomia identică"):**
  1. **MĂSURĂTOAREA CARE DECIDE TOT** (rulată live pe VPS, pe cheia reală, model `google/gemma-4-26b-a4b-it:free`, comanda „deschide youtube pe monitor"): cu **1 unealtă și fără prompt de sistem execută 2/2**; cu **61 de unelte + prompt de 10k execută 0/2 și răspunde GOL** (ăsta e „îngheață/nu răspunde"); cu **aceeași încărcătură dar FORȚAT (`tool_choice:'required'`) execută 2/2**. Concluzie dovedită: **creierul gratuit știe să execute — codul îl îneacă.** Banii nu desfundă o pâlnie astupată de cod. Încărcătura reală măsurată: 61 unelte / 38.100 caractere de scheme + 12.463 caractere de prompt de sistem.
  2. **Forțarea ținea O SINGURĂ rundă** (`orchestrator.ts`, `forceToolsFirstRound && round === 1`): modelul o satisfăcea cu o unealtă **pasivă** (`system_health`, `read_source`) și rundele 2+ reveneau la `'auto'` → NARA restul. ACUM: `forceToolsUntilAction` — `'required'` rămâne activ până când chiar rulează o unealtă de **ACȚIUNE**, plafonat la 3 runde.
  3. **Poarta faptei se auto-dezarma**: condiția era `!anyToolCalled`, iar runda 1 forțată garanta că o unealtă rulase → poarta **nu putea declanșa niciodată** exact pe turele de acțiune pentru care fusese făcută. ACUM contează doar faptele reale (`anyActionToolCalled`), poate interveni de 2 ori, iar **runda ei de corecție rulează FORȚATĂ** (înainte rula pe `'auto'`, deci modelul repeta minciuna cu alte cuvinte).
  4. **`PASSIVE_TOOLS` (chat.ts), listă INVERSATĂ**: pasiv = enumerat explicit (read/search/list_source, runbook_status/log, constructor_status, system_health, server_logs, db_tables, list_notes/memories, get_real_cost, log_unsupported_request, play_avatar_gesture, browser_read, ask_brain). Orice unealtă NOUĂ = acțiune, deci una adăugată mâine nu poate dezarma poarta din greșeală. **`db_query` NU e pasiv intenționat** — rulează SQL cu COMMIT; dacă l-am socoti citire, poarta i-ar putea cere modelului să re-execute un DELETE.
  5. **Contradicția din prompt ștearsă** (blocul ownerului cerea „narrate CONCRETELY: which file and line, which branch, the PR number, the Actions link" = exact valorile inventabile, în aceeași frază cu „nu descrie, execută"). Înlocuit cu **REPORT ONLY WHAT A TOOL RETURNED**. Plus **REGULA FINALĂ** scurtă și imperativă, lipită la **finalul** promptului de sistem (poziția cea mai puternică pentru un model gratuit cu atenție diluată).
  6. **Forțarea + poarta pentru TOȚI**, nu doar pentru owner (`deedGate: true`, `forceToolsUntilAction: actionTurn`) — clienții plătitori n-avuseseră niciodată execuție forțată. `actionTurn = hasActionIntent(lastUserText)`, deci se stinge singură la replica următoare.
  7. **Confirmarea instantă extinsă la orice tură de ordin**: pe o rundă forțată modelul întoarce DOAR apelul de unealtă, fără text (Gemini în mod `ANY` nici nu produce text) — fără ea userul aștepta MUT.
  8. **Plasa la `'required'` respins**: unii furnizori întorc 400 pe `tool_choice:'required'` → runda se reia liber, ordinul nu mai moare cu eroare.
  9. **VOCEA, care nu primise NICIODATĂ reparația**: sesiunea rămâne pe `'auto'` (la nivel de SESIUNE `'required'` bagă modelul în buclă de unelte și nu mai vorbește deloc — sigilat cu comentariu în `services/realtime.ts`), iar forțarea se face **per răspuns**, din client: `{type:'response.create', response:{tool_choice:'required' | {type:'function',name}}}` (formă PLATĂ, GA). Regula „ce e ordin" **nu se rescrie în frontend** — backendul trimite chiar `ACTION_INTENT`-ul chatului scris pe antetul `x-kelion-action-intent` al răspunsului SDP (zero apeluri noi). Pe ordinele clare unealta e **fixată** (youtube_search/look/get_monitor/get_location), ca modelul să nu bifeze una pasivă. Fără antet → nu se forțează nimic (degradare curată + `console.warn`).
  10. **BUG găsit la audit, în voce**: `forceFirstRound: hasActionIntent(prompt)` la escaladarea `ask_brain` testa **promptul întreg**, iar `SYSTEM_PROMPT` conține „PR", „merge" și „fix" → forțarea era pornită la **fiecare** escaladare. Acum testează **cererea** (`request`).
  11. **Creier cu brațe și pentru non-admin**: `ask_brain` cădea pe `brainComplete` FĂRĂ nicio unealtă — un „expert" care putea doar să vorbească. Acum primește uneltele Google/căutare ale userului, iar `screen_url` întors de creier ajunge la client (`{output, screen}`) → monitorul chiar pornește din escaladare.
  12. **Probă de regresie**: `backend/src/services/orchestrator.test.ts` (5 teste) îngheață comportamentul — unealta pasivă nu oprește forțarea, plafonul ține, poarta prinde „am reparat" după o unealtă de citit, iar conversația obișnuită rămâne neforțată.
  13. **URMEAZĂ (etapa 2, neînceput):** dieta de unelte — trimiterea doar a subsetului relevant turei în loc de toate 61 (măsurătoarea arată câștig: 2/2 la 1 unealtă vs 1/2 la 61). Amânat deliberat: forțarea singură rezolvă execuția, iar tăierea uneltelor e singura schimbare care poate lua o capabilitate.
- 🗓️ **27 IUL (RESTAURAREA) — master ADUS ÎNAPOI la backup-2026-07-27-1202-c29c626, la ordinul lui Adrian („stop restaurează"):**
  1. **Ce s-a întâmplat**: după valul 22 au urmat ~14 PR-uri (#437–#449: poarta faptei, creier full-free + Gemini direct, poarta numelui în voce, audit + curățenie, fix-uri securitate). În testele live au apărut regresii pe voce/chat („nu mă aude", „nu execută din prima") și Adrian a ordonat restaurarea la punctul de dinaintea modificărilor de creier/voce. **Arborele aplicației = EXACT starea c29c626** (commit nou, înainte — istoria nu e rescrisă; toate schimbările rămân în istoricul git și pot fi re-aduse selectiv doar la ordin).
  2. **Consecințe de știut**: creierul ownerului e din nou pe modelul plătit din codul de atunci (full-free-ul a fost DUPĂ acest punct); env-ul VPS (kelionai.env) NU e atins de restaurare. Poarta numelui în voce și Gemini direct nu mai există în producție.
  3. **NOU peste restaurare — YouTube care CHIAR redă** (primul bug reclamat live pe versiunea restaurată: „nu merge play pe youtube"): `youtubeSearch` lua ID-urile de la căutarea web PE ÎNCREDERE (putea inventa clipuri moarte) și trimitea pe monitor embed FĂRĂ autoplay și fără enablejsapi. Acum: fiecare candidat e verificat prin **oEmbed YouTube** (200 doar dacă există ȘI e încorporabil — dovadă: ID inventat → 400) și doar clipurile redabile ajung la user; `ytEmbed()` emite `?autoplay=1&enablejsapi=1` (pornește singur + vocea îi poate coborî volumul).
  4. **RESCRIEREA PE MODULE (ordinul lui Adrian, seara: „ce apelez să fie funcțional, fără remedieri ulterioare" — altfel plec; „lucrezi autonom până la final"). Metoda: gândit → rescris → testat → publicat → DOVEDIT live, un modul pe rând. Ordinea: 1 Auzul/vocea, 2 Creierul-execută, 3 Monitorul, 4 Autonomia, 5 Banii, 6 Interfața.**
     - **MODUL 1 LIVRAT (live 4629d52, 17:56):** (a) ieșirea din semi-duplex NECONDIȚIONATĂ la sosirea răspunsului (bug dovedit: se stingea doar flag-ul de gândire, fără unmute, iar plasa de 125s verifica flag-ul deja stins → după orice ask_brain microfonul rămânea mut definitiv — „nu mai mă aude"); regula: fiecare intrare în mut are ieșire garantată pe toate drumurile. (b) Poarta numelui MECANICĂ (create_response:false pe server; clientul răspunde doar la nume — regex tolerant la transcriere stâlcită — sau în fereastra de conversație de 120s, deschisă la pornire, reînnoită la fiecare schimb; STOP o închide; plasă anti-surzenie 2.8s pe transcript pierdut).
     - **MODUL 6 LIVRAT — Interfața & curățenia finală:** CSS-ul mort scos (~28%), App.tsx cu bannerul de update (aplicare automată doar pe tab ascuns) și ruta /credite ÎNAINTEA verificării de user, legăturile alternative pe landing (login email + prețuri), preload avatar scos din index.html (bcbc6ea-aligned), WalletButton cu payBusy, vocea client (realtimeVoice) cu vindecarea microfonului în-loc (bindMicTrack + replaceTrack) + poarta numelui, db.ts fără cele 20 de funcții moarte + requeueMoneyFailedBuildJobs, rutele moarte șterse (correct/greet/meserii, /api/admin/visual-check, workflow public-latency-test, fișiere test/vad/qr nefolosite, backend/%1). Arborele final = starea dovedită pre-restaurare + îmbunătățirile post-restaurare (butoane Restaurează, YouTube oEmbed, reguli de afișare în voce, monitor-gol, browser-mut, unmute garantat, constructor anti-răspuns-gol, curățenia VPS) − codul mort.
     - **MODUL 5 LIVRAT — Banii & securitatea:** plafon 2 ture simultane per user plătitor (anti check-then-charge „40 de ture cu 1 ban"); debit și pe calea de EROARE a turei; debitWallet nu mai tace la eșec (jurnal); profitul se reversează la refund (rând compensator :profit-refund); updateTransactionStatus nu retrogradează stări finale bune (webhook-uri sosite invers); initDb fatal când DATABASE_URL există (anti dublă-creditare); lacătul admin acoperă și uneltele din chat (cookie furat ≠ unelte); owner doar-Google pe authLocal (register/login/magic); înrolarea amprentei vocale de admin cere sesiune deblocată; read_source refuză fișierele-secret + containment de cale; SSRF: IPv6 mapat ::ffff:x.x.x.x desfăcut; Stripe fără fallback-ul cs_ (get/reconcile).
     - **MODUL 4 LIVRAT — Autonomia robustă:** constructor-agent cu reîncercări 4x (429/5xx/corp gol/JSON rupt) + tratarea răspunsului-GOL-valid (200 fără mesaj — a doua cauză reală a jobului #2) + normalizarea mesajelor și a argumentelor tool_calls (capcanele Cohere); jurnalele serverului la Kelion nativ (logbuffer.ts inel 600 + unealta server_logs — F12-ul de server); /api/admin/audit agregat (sănătate + erori server + erori client 48h grupate + joburi eșuate) afișat în Admin→Cereri neacoperite, reîmprospătat 15s; tab-urile admin se încarcă la deschidere (nu doar la click), initialTab sincron cu navigarea din voce, vLoading vizibil.
     - **MODUL 3 LIVRAT — Monitorul fluid:** tab-urile rămân VII la comutare (harta nu se repornește; sursele cu sunet montate DOAR active), închiderea sincronă cu fade-ul (poza ultimei stări pe durata stingerii, 520ms), benzile compozerului intră crescând (band-in 180ms, fără salt de 38-46px), coada fixă speech-tail pe dictare/👤/🧠 (nu teletext remontat la fiecare cuvânt), ticker-ul final în buclă, camera nu se mai demontează la fiecare render (onCameraError cu useCallback), cipul de latență reală ⚡ (dovada „primul cuvânt sub 1s").
     - **MODUL 2 LIVRAT — Creierul care EXECUTĂ, full-free:** scara $0 (gemma-4 free / nemotron-omni free / nemotron-ultra free, config.ts), Gemini direct principal pe work (cheia gratuită AI Studio, geminiDirect.ts; fallback automat pe nemotron la cotă), deed-gate + forceToolsFirstRound (orchestrator.ts), verbele de comandă în ACTION_INTENT, ack instant pe turele de acțiune ale adminului (37s→sub 1s), reasoning 'low' pe :free/gemini, reguli de prompt: monitor gol = execută nu raporta, CLICKING IS MANDATORY, browserul live e MUT (redare doar prin youtube_search) — și în vocea (realtime.ts): comenzile de afișare sunt ordine, nu întrebări despre monitor.
  5. **NOU peste restaurare — butoanele de restaurare în Admin→Recuperare** (ordinul „pune și butoanele de selecție"): fiecare versiune salvată are buton „Restaurează" (confirmare dublă) → `POST /api/admin/backups/restore` → `restoreToPoint(tag)` în recovery.ts: comite arborele tag-ului peste vârful lui master (fast-forward; dacă push-ul direct e refuzat, cade pe ramură+PR+merge automat) → publicarea pe VPS pornește singură. Adrian nu mai depinde de nimeni pentru restaurări.
- 🗓️ **27 IUL (valul 22) — MOTORUL DE GESTURI UMANE, DECENTE (ultimul punct roșu al zilei; Adrian: „lipsește motorul/creierul de apelare gesturi — umane reale, decente"):**
  1. **Regia REPORNITĂ** în AvatarModel.tsx (fusese oprită complet pe 13 iul — „nepotrivite pentru gentleman"), dar cu decența ca lege: **vorbește** (nivel REAL de voce >0.05) → clipul calm de conversație în buclă (cel reținut de Adrian); **tace** → repaus viu (`idle`) + o variație DOMOLĂ la 25–45s, DOAR din `CHAT_IDLE_CALM` (variatie/2/4/5/6/8 — dezmorțirile ample rămân la comandă); **gest comandat** (creier/viu grai) → prioritate absolută.
  2. **Filtrul lui Adrian rămâne legea**: tot ce e debifat în Admin→Gesturi (`disabledG`, reîmprospătat la 30s) nu se joacă nici automat (inclusiv `talk`); clipuri = captură de mișcare Ready Player Me, crossfade 0.35s, zero animație de mână.
  3. **Brațele blocate în repaus DOAR în idle** (decența din 13 iul păstrată); în vorbit și gesturi, clipurile au mâna liberă — altfel lacătul de brațe ar fi înghețat gesticulația de conversație.
- 🗓️ **27 IUL (valul 21) — SĂNĂTATEA PROPRIE: Kelion își vede problemele și ÎNTREABĂ dacă să le repare (Adrian: „să poată comunica adminului prin chat că are problemele x,y,z și să întrebe dacă să le repare"):**
  1. **`services/health.ts` — `systemHealth()`**: agregare deterministă — live vs master (GitHub API), rulări roșii 48h, ordine de construcție eșuate, val de erori client (>20/h), disc ≥90%, DB, sold OpenRouter sub prag. Fiecare problemă cu `grav` + `reparabil` (cum se repară).
  2. **Unealta `system_health`** (chat admin + ask_brain din voce): instrucțiunea din prompt îl pune să o cheme LA PRIMUL mesaj al ownerului dintr-o conversație; probleme → „am problemele: x, y, z — le repar?" și **așteaptă acordul explicit**; sănătos → tace.
  3. **Descoperire critică pe drum (meritul lui Adrian, „401 greșit")**: `BRIDGE_SECRET` NU exista nici în env, nici în container — pulsul sentinelei era MORT TĂCUT de la instalare, constructorul nu putea lua ordine. Generat + redeploy (KELION_DEPLOY_FORCE există acum pentru cazul env-schimbat-același-sha); dovadă după: `/api/constructor/next` cu cheie = 200. Lecția: un „paznic" nedemonstrat cap-coadă cu 200 real NU e instalat.
  4. **Vindecătorul DOVEDIT**: deploy #493 rerulat automat de vindecător → verde (attempt 2, success). Prima rulare a prins și id-uri false din JSON (grep) → reparat cu parsare python3 + fereastră 48h; cele 2 alerte false pe email — explicate lui Adrian.
- 🗓️ **27 IUL (valul 20) — LACĂTUL GLOBAL DE PUBLICARE (deploy #493 roșu explicat + reparat definitiv):**
  1. **Cauza roșului**: după merge-ul #422, DOUĂ publicări au rulat deploy.sh în paralel pe același master — Actions (deploy.yml, push) și auto-publicarea de pe server (cron 3 min). Ambele au construit imaginea; al doilea `docker run` a picat pe „container name /kelionai-app already in use" (exit 125). **Publicarea reală reușise** (anti-fantomă verde la primul) — roșul era doar al dublurii.
  2. **Fixul**: `flock` global pe `/root/kelion/publicare.lock` chiar în deploy.sh (după garda de self-copy) — orice publicator așteaptă; + ieșire devreme la pasul 1 („deja publicat exact $TARGET") când celălalt a terminat primul. Ambele drumuri (Actions + cron) rămân, dar serializate — nu mai există cursă, nu mai există roșu fals.
  3. Cronul constructorului instalat MANUAL pe VPS după deploy-ul #422 (deploy-ul rula copia scriptului vechi, fără pasul 6d — de la valul ăsta pasul 6d e în scriptul live).
- 🗓️ **27 IUL (valul 19) — CONSTRUCTORUL v2 + autonomia completată (ordinele lui Adrian, în rafală):**
  1. **CONSTRUCTORUL** („Kelion trebuie să poată crea orice soft îi cere admin; problema veche era IMPLEMENTAREA, nu conceptul"): coada `build_jobs` (DB) ← ordin din chat/voce (`build_software`) sau Admin→Constructor; **lucrătorul de pe VPS** (`deploy/constructor-worker.sh`, cron */2, flock + `timeout 1800` — JOB-uri scurte, NU demonii vechi care ardeau abonamentul) rulează `deploy/constructor-agent.mjs`: clonă proaspătă în `/root/kelion/atelier`, buclă agentică pe **API OpenRouter** (plată per-folosire; `CONSTRUCTOR_MODEL`, DOAR modele `:free` — gard structural, vezi valul 25; plafoane `CONSTRUCTOR_MAX_STEPS`=24 (pași CU UNELTE), `CONSTRUCTOR_MAX_STERILE`=8, `CONSTRUCTOR_MAX_REPAIR`=2, `CONSTRUCTOR_BUDGET_MS`=26min, `CONSTRUCTOR_MAX_TOKENS`=900k) cu unelte închise în atelier (ls/grep/read/write/**edit**/run-whitelist), **verificare build+teste impusă de script** (nu pe încredere), push `kelion/job-N` + PR automat; **merge-ul rămâne la Adrian**; raport pe email la done/failed. Capete API: `/api/constructor/next|report` (x-bridge-secret; respectă „pauza-autonomie"), `/api/admin/constructor` (post/get). Instalat idempotent de deploy.sh pas 6d.
  2. **Acces DB total pentru Kelion** („acces la orice bază de date a aplicației"): unelte admin `db_tables` (schema completă + rânduri) și `db_query` (SQL direct, cap 200 rânduri, statement_timeout 10s; distructiv DOAR la ordin explicit — regula în descrierea uneltei).
  3. **`ask_brain` din voce CU UNELTE** (restanța din 24 iul, cauza lui „Kelion nu poate vedea tot codul sursă al lui"): `brainCompleteWithTools()` în brain.ts (buclă tool-calling pe modelul de lucru) + în voce escaladarea primește sursă (list/read/search), DB (tables/query) și constructorul (build/status); instrucțiunea vocii spune explicit că ARE acces integral prin ask_brain — nu mai neagă.
  4. **Comanda „STOP" în voce** („kelion nu ascultă comanda stop"): detecție deterministă în client pe transcriptul frazei rostite singure (stop/stai/taci/gata/oprește-te/shut up/basta) → `response.cancel` + `output_audio_buffer.clear` imediat și repetat la 400ms (taie și răspunsul auto-pornit de VAD la însăși fraza „stop") + ordin de tăcere injectat.
  5. **„Salvează" de pe monitor REPARAT** („butonul salvează nu e funcțional" — descărca tăcut, fără urmă): acum salvează documentul în **stocarea permanentă** (`POST /api/notes` → tabela notes; Kelion îl regăsește cu uneltele de notițe) + descarcă local + confirmă vizibil („Salvat ✓").
  6. Enum-urile secțiunilor admin sincronizate cu noul tab `constructor` în toate cele 4 locuri (Stage VALID, AdminPanel types, chat.ts, realtime.ts).
- 🗓️ **27 IUL (valul 18) — LACĂTUL BUTONULUI ADMIN (Adrian: „dacă amprenta nu corespunde, nici butonul admin nu trebuie să se activeze... creează să pot să pun secret activare admin"):**
  1. **Al 2-lea factor peste sesiunea de admin** (`services/adminLock.ts` + gate global `preHandler` în index.ts): odată ARMAT, tot `/api/admin/*` cere pe lângă sesiune și DEBLOCAREA — altfel `423 admin_locked`. Un singur punct de strangulare, nu 40 de handler-e; excepție doar `/api/admin/unlock*`.
  2. **Două chei de deblocare**: (a) **amprenta vocală potrivită** pe referință EXISTENTĂ (routes/realtime.ts, transcript: isAdmin + hasRef + dist<0.38 → cookie semnat `kelionai_admin_unlock`, JWT scope `admin-unlock`, 12h; prima enrolare NU deblochează) — clientul primește `adminUnlocked:true` → event `kelion:admin-unlock` → butonul se aprinde; (b) **secretul tastat** (`POST /api/admin/unlock`, anti-brute-force 5/10min, scrypt `N=16384` doar hash în kv `admin_lock_secret`).
  3. **Armarea o face DOAR Adrian**: Admin→**Amprente vocale**→„Lacătul butonului Admin" — alege secretul (min. 4 caractere) și apasă „Armează lacătul" (`POST /api/admin/unlock/secret`; schimbarea cere sesiune deja deblocată). Până la armare, comportamentul e cel vechi — zero risc de auto-blocare la deploy. Secretul NU e cunoscut de nimeni în clar; recuperare doar prin ștergerea rândului kv din DB (VPS).
  4. **UI** (Stage.tsx): poarta unică `openAdmin()` pe TOATE drumurile (butonul Admin, navigarea din voce/chat `kelion:navigate`, butonul pungii Stripe); încuiat → butonul arată 🔒 și se deschide fereastra de cod (`.unlock-overlay`), nu panoul.
- 🗓️ **27 IUL (dimineața, valul 17) — SAGA ISSUING LIVE: adevărul din API + cererea retrimisă:**
  1. **De ce dashboardul îl tot arunca pe Adrian în sandbox la Issuing**: accesul LIVE nu e activ — Stripe nu are ce afișa pe live, așa că forțează test mode. Dovada din API cu `rk_live` de pe VPS (vps-run #256, `livemode:true`): **0 carduri, 0 cardholders pe live**, punga Issuing £0.00; sold −£9.76 disponibil / +£19.50 în decontare (plata de test de £20 din 21 iul, decontare 30 iul) = £9.74 net, exact ca în dashboard.
  2. **Cererea din 24 iul („Thank you!") n-a primit NIMIC înapoi**: contact@ nu are niciun email de la Stripe (verificat în `inbound_emails`, vps-run #257 — singurele potriviri „stripe" sunt alertele interne de la alerts@), iar checklist-ul live arăta „**Contact sales to apply for live mode access — Required**" cu formularul „Use case" GOL. **Retrimisă azi de Adrian** cu use-case-ul dictat (1-2 carduri virtuale interne pentru plata OpenAI/OpenRouter din încasări, fără carduri către clienți, ~£50–500/lună). Răspunsul Stripe vine pe emailul contului, în ore–zile.
  3. **La aprobarea Stripe** (Adrian anunță când vine mailul): creez cardholder + cardul virtual „Kelion AI" din API (cheia `rk_live` are Issuing Cards/Cardholders Write, verificat 24 iul), apoi cardul se introduce o dată la OpenAI/OpenRouter și circuitul banilor e complet. Până atunci, ❌ „Cardul Kelion AI: necreat" din Admin→Bani e REALITATEA, nu un bug.
  4. Cardul ••••0005 din sandbox = fictiv (creat 10 iun în test mode) — se ignoră; nu se creează NICIUN card în sandbox.
- 🗓️ **26 IUL (valul 14) — PULSUL LUI KELION (Adrian: „toate punctele" pentru autonomie deplină; regula lui: verificare automată FĂRĂ cost):**
  1. **Sentinela locală** (`deploy/sentinela-locala.sh`, instalată idempotent de deploy.sh la pasul 6b, cron la 3 min pe VPS): `/health` mort de 2 ori LA RÂND (≥6 min — un deploy normal nu declanșează fals) → `docker restart kelionai-app` + raport prin pulse; `/health` viu → bate pulsul intern. Zero AI, zero cost. Numele scriptului NU se schimbă în watchdog/paznic-chat — deploy.sh șterge exact acele nume din crontab (zombii vechi).
  2. **`POST /api/ops/pulse`** (`routes/ops.ts`, auth `x-bridge-secret` = BRIDGE_SECRET, ca browser.ts): verificări DETERMINISTE — DB (`SELECT 1`), disc ≥90%, val de erori client (>20/oră), plus raportarea repornirii automate. Email către admin DOAR la anomalie, cu prag anti-spam per subiect în kv (`ops_alert_*`): restart 30 min, DB 1h, erori 3h, disc 6h.
  3. **Rămân de făcut din lista „autonomie deplină"**: unelte de SCRIS pe Google (Docs/Sheets — atenție: cer scope OAuth nou → reconectare Google a userului), circuitul banilor (Issuing/Revolut — decizia lui Adrian „revin"), cheia Stripe Account:Read (dashboardul lui), permisiunile cameră/locație de pe telefonul lui.
- 🗓️ **26 IUL (valul 13) — PANA „săgeata de trimis nu se vede pe telefon" + „iar ai stricat audio" (aceeași cauză!):**
  1. **Cauza tehnică**: `.composer-input` (flex:1) fără `min-width:0` NU se strânge sub lățimea placeholder-ului → pe ecran îngust, ultimul element din rând — butonul de trimis `↑` — era împins în afara ecranului. În locul lui rămânea vizibil SLIDERUL DE VOLUM (64px, „pastila" albastră din captura lui Adrian).
  2. **Efectul în lanț asupra AUDIO**: apăsările de „trimite" aterizau pe slider → volumul (comanda UNICĂ pentru toată vocea, PERSISTATĂ) ajungea la ~0 → „nu se aude nimic" deși sesiunea de voce mergea perfect (dovadă: transcripturi curgând în loguri, zero erori realtime). Nu era o pană de audio — era volumul memorat pe mut.
  3. **Fix**: `min-width:0` pe input (regula flexbox), lățimea sliderului mutată din inline în `.composer-volume` cu variantă îngustă (38px) sub 540px.
  4. **De verificat pe telefonul lui Adrian (numai el poate)**: Chrome → Site settings → kelionai.app → permisiunile de CAMERĂ (logat azi „NotAllowedError: Permission denied") și LOCAȚIE probabil blocate — fără ele camera și GPS-ul rămân moarte indiferent de cod; plus ridicarea sliderului de volum înapoi.
  5. **Runda 2 mobil (captura a doua a lui Adrian)**: (a) sliderul de volum pe telefon e ASCUNS de tot (ordinul lui: „ascunde pe telefon buton volum și în loc să fie săgeata") — rămân micul + săgeata; (b) topbar-ul FIX se împachetează pe telefon pe 2-3 rânduri dar `.workspace-inner` păstra degajarea de desktop (56px) → titlul panoului + A−/100%/A+ intrau sub insignele de credit; fix: `padding-top: 195px` sub 640px.
  6. **Creditele la ADMIN (Adrian: „afișează corect creditele la admin"; „portofelul Stripe admin e afișat corect")**: owner-ul nu cumpără credite, deci soldul lui din registru iese tot mai NEGATIV pe măsură ce consumă (−324 la acel moment) — mesaj fals (nu datorează nimic). Fix DOAR de AFIȘARE în `WalletButton.tsx`: la admin pastila arată „nelimitat" (tooltip: consumul real e în Admin → Bani), fără clipirea roșie și fără toast-ul „mai ai puțin credit". REGISTRELE NU S-AU ATINS: `wallets`/`billing_events`/`cost_events` neschimbate (regula: niciun contor resetat fără ordin explicit); pastila Stripe (banii reali) rămâne exact cum era.
- 🗓️ **26 IUL (valul 10) — REGULA VOCII UNICE (bug real raportat de Adrian: „a intrat o a doua voce în același timp… în același timp nu are voie niciodată 2 voci, trebuie unică"):**
  1. **Scenariul**: sesiunea de voce full-duplex (OpenAI Realtime) activă; Adrian trimite o poză prin chatul scris → răspunsul turei scrise vine cu cadre `{audio}` (vocea Chirp sintetizată de server pentru ORICE tură de chat) → clientul le reda necondiționat → DOUĂ voci simultan.
  2. **Fix pe ambele capete**: (a) handle-ul sesiunii Realtime e marcat `isRealtime` (marcajul cade în stop-ul împachetat, pe orice drum de oprire); cât e instalat, `ChatPanel` ARUNCĂ cadrele `{audio}` ale chatului scris; (b) clientul trimite `serverVoiceOff: true` în corpul `/api/chat` → serverul NICI NU SINTETIZEAZĂ (stub în loc de `createVoiceStream`) — zero cost Chirp pe audio care s-ar fi aruncat. Calea STT de rezervă (fără Realtime) rămâne neatinsă — acolo vocea Chirp E vocea.
  3. **Regulă permanentă pentru orice AI viitor: NICIODATĂ două voci simultan.** Vocea activă e a sesiunii Realtime când există; altfel Chirp (STT fallback / clipuri promo). Orice sursă audio nouă trebuie să respecte gate-ul ăsta.
- 🗓️ **26 IUL (valul 12) — GPS DOAR LA NEVOIE, DAR REAL (Adrian: „kelion nu primește coordonatele reale", apoi corecția lui explicită: „în real time la GPS — GREȘIT; doar când se folosesc aplicații GPS sau e necesară detecția locației"):**
  1. **Regula lui Adrian, permanentă**: NU flux GPS continuu (prima variantă cu push periodic pe pulsul de 20s a fost respinsă și scoasă). Poziția se citește PE LOC, cu o singură interogare (`getFreshCoords` în ChatPanel: `getCurrentPosition`, timeout 5s, cache 30s), EXACT în momentul folosirii — `watchPosition`-ul permanent pentru chat a fost ȘTERS. `coordsRef` rămâne doar memoria ultimei citiri legitime.
  2. **Scris**: regexul `LOC_INTENT` (vreme/hărți/„unde sunt"/trasee/navigare, RO+EN) decide dacă tura cere locația → doar atunci se citește GPS-ul real înainte de trimitere; altfel pleacă cel mult ultima poziție cunoscută, fără nicio detecție nouă.
  3. **Voce**: la uneltele de locație (`get_weather`, `maps_search`, `maps_directions`) clientul citește poziția reală a momentului, o injectează în argumente (vreme fără loc numit) și o dă și sesiunii prin `RealtimeVoiceHandle.updateCoords` (item de SISTEM pe dataChannel cu noii lat/lon — fără session.update, regula „o singură injecție" respectată) → „aici"/„de aici" înseamnă locul de ACUM. `rvLiveRef` ține sesiunea live pentru asta.
  4. Watcher-ul de geolocație legat de CAMERA pornită (adaptarea fps la mișcare) e caz de „senzor în uz activ" — neatins.
  5. **PANA „GPS nu e accesibil" (imediat după valul 12, prinsă de Adrian)**: după scoaterea watcher-ului, vocea NU mai avea NICIO cale să afle poziția — sesiunea pornea fără coordonate și la „unde sunt"/„aici" răspundea că nu are acces; în plus, citirea unică cerea precizie ÎNALTĂ cu timeout 5s (fixul rece durează des mai mult → eșua). Fix: (a) unealtă nouă `get_location` în sesiunea de voce (declarată în `realtime.ts`, EXECUTATĂ ÎN BROWSER în `ChatPanel.onToolCall`: citește GPS-ul real pe loc, împrospătează sesiunea prin updateCoords, întoarce lat/lon; test de regresie în `realtime.test.ts`); (b) instrucțiune explicită în context când nu există poziție la pornire: „cheamă ÎNTÂI get_location, nu spune că n-ai acces"; (c) `getFreshCoords` pe precizie STANDARD (rețea/wifi, 1-3s, suficient pentru vreme/hărți), timeout 10s, cache 2 min. LECȚIE: nu raporta „făcut" fără verificarea COMPORTAMENTULUI — publicarea codului nu e dovadă că funcția merge.
- 🗓️ **26 IUL (valul 11) — POȘTA ÎN ORICE LIMBĂ (Adrian: „primit în germană se răspunde în germană? sau orice limbă?"):**
  1. Garantat prin detectorul local: RO/EN/DE/FR/ES/IT/PT (limba e detectată și dată explicit modelului). **Testul #3 (după merge #405)**: mail românesc FĂRĂ diacritice → `lang='ro'`, răspuns în ROMÂNĂ cu faptele reale despre produs, mutat în Kelion-Answered — dovada completă în chat.
  2. **Orice ALTĂ limbă** (poloneză, rusă, olandeză…): înainte cădea pe engleză (`detectLang || 'en'`). Acum, când detectorul nu e sigur, `draftReply` primește `langName=null` și modelul primește ordinul să identifice singur limba mesajului și să răspundă exact în ea; în DB limba se salvează ca `'auto'`.
- 🗓️ **26 IUL (valul 9) — POȘTA contact@ REPARATĂ: MAIL_PASS pe VPS + bug `vps-set-env` (pipefail):**
  1. **Simptom** (din 24 iul): pollerul IMAP pica la fiecare ~6 min cu `Socket timeout ETIMEOUT` / „Connection not available" — cutia contact@ moartă. Cauză de mediu: `MAIL_PASS` lipsea din env-ul containerului de pe VPS.
  2. **Adrian a pus secretul `MAIL_PASS` și a rulat `vps-set-env` → jobul a EȘUAT (run 30190681677, exit 1 în 9s)**, dar înșelător: cheile FUSESERĂ scrise în `/root/kelion/kelionai.env` (log: „MAIL_PASS: scris (13 caractere)"); a murit doar pasul de REPORNIRE, care n-a mai rulat.
  3. **Bug-ul real în workflow**: bucla care emite `KEY=VALUE` folosea `[ -n "$val" ] && printf …` — când ULTIMUL nume din listă (`GITHUB_TOKEN` ← secretul `KELION_GITHUB_TOKEN`, gol la acel moment) e nesetat, grupul iese cu cod 1, iar `set -euo pipefail` omoară jobul deși scrierea remote a reușit. Fix: `if [ -n "$val" ]; then … fi` (comentariu în workflow cu run-ul dovadă). NU reordona lista de secrete ca „fix" — forma `if` e cea corectă.
  4. **Remediere imediată fără a aștepta merge-ul**: container recreat prin `vps-run` cu rețeta standard (`--env-file kelionai.env` + `GIT_COMMIT_SHA` din clona deploy) → env-ul nou încărcat; login IMAP direct din container: OK (INBOX deschis, 548 mesaje).
  5. **DAR testul cap-coadă cerut de Adrian („verifică cu un mail de test dacă răspunde") a arătat că parola NU era toată povestea.** Un mail de client simulat, injectat prin IMAP APPEND, a rămas neprocesat: `Socket timeout` + `[mailbox] poll failed: Connection not available` CONTINUAU la fiecare ciclu, deși login-ul direct mergea instant. **Cauza reală, în cod (`mailbox.ts`)**: poll-ul descărca CORPUL COMPLET al ultimelor 100 de mesaje la FIECARE 3 minute (`fetch` interval cu `{source:true}`) — pe un inbox de ~550 mesaje transferul depășea timeout-ul socketului imapflow, conexiunea murea, nimic nu se procesa. Fix (rescrisă bucata responsabilă): întâi DOAR UID-urile (`search {all}`), pre-filtru contra DB (`knownInboundUids` nou în `db.ts`), corpul se descarcă DOAR pentru mesajele noi (`fetchOne` per UID, de regulă 0–2/ciclu). Plase de siguranță: max 25 mesaje/ciclu după o pană (anti-avalanșă) și mesajele mai vechi de 7 zile NU primesc auto-reply/forward — se salvează și merg în Kelion-ToAnswer (un „bun venit" după săptămâni face rău).
  6. **Al doilea bug găsit de același test**: `ensureFolders` (creează Kelion-Answered/ToAnswer/Automated) exista dar NU era apelată nicăieri (cod mort din PR #387) → folderele nu se creau, fiecare mutare pica pe fallback. Acum e apelată când există mesaje de procesat.
  7. **DOVADA FINALĂ (test #2, după deploy)**: mail de client injectat prin IMAP (de la Gmailul lui Adrian) → în primul ciclu: rând în `inbound_emails` cu `replied=true` + textul răspunsului, mesajul mutat în `Kelion-Answered`, forward la admin — cutia contact@ RĂSPUNDE. Backlogul vechi curge în loturi de 25 (mașinării → Kelion-Automated, oameni vechi → Kelion-ToAnswer). Testul a mai prins două probleme de calitate, reparate pe loc: (a) `detectLang` nu recunoștea româna FĂRĂ diacritice („Buna ziua… multumesc" → null → fallback engleză, contra regulii „răspuns în limba primită") — adăugate variantele fără diacritice care nu se confundă cu alte limbi; (b) promptul `draftReply` nu dădea NICIUN fapt despre firmă → creierul a inventat („travel companion app") — acum primește faptele reale (asistent AI live pe kelionai.app, avatar 3D, voce/vedere/Google) cu ordin să nu inventeze altele.
- 🗓️ **25 IUL (ramura `claude/reia-l9l2qx`, rebazată peste valurile de autonomie 4–8) — PARITATE VOCE↔SCRIS + PLAYGROUND + POȘTĂ + FIȘIERE:**
  1. **Poșta contact@ — răspuns în limba primită + organizare pe foldere** (`mailbox.ts`): limba e detectată și dată EXPLICIT modelului (`langLabel`) → răspunsul iese garantat în limba clientului. Organizare automată (`MAIL_ORGANIZE`, implicit pornit): fiecare mesaj procesat e mutat în folder IMAP — `Kelion-Answered` (răspuns automat trimis, +`\Answered`), `Kelion-ToAnswer` (om real fără răspuns → admin), `Kelion-Automated` (mail-mașină). Best-effort, reversibil, oprit din env.
  2. **Voce: comutare verbală cameră/ecran** (paritate cu scrisul): `/api/realtime/transcript` rulează ACELAȘI `interpretDeviceCommand` pe transcriptul userului și întoarce `{device}`; clientul îl execută prin `handleControl` (comută camera față/spate/switch, închide monitorul). Înainte mergea DOAR în chatul scris.
  3. **Voce: GPS de pe dispozitiv**: clientul trimite `coords` la pornirea sesiunii → serverul le bagă în contextul vocii (reverse-geocode din cache) + injectează exact lat/lon în `get_weather` la „vremea de aici". Înainte vocea era oarbă la locație.
  4. **Playground de cod pe monitor** (`run_web_app`, chat+voce): Kelion scrie o pagină web COMPLETĂ (HTML+CSS+JS inline) și o RULEAZĂ live pe monitor într-un cadru izolat (`srcdoc`+`sandbox`, fără same-origin) — fără X-Frame-Options, deci fără „pagina nu poate fi afișată aici". Rezolvă captura „ceas analogic → nu poate fi afișată". **Salvare de pe monitor**: buton „Salvează" pe playground (.html) și pe documente (.txt).
  5. **Chat: printscreen lipit direct + orice tip de fișier** (`ChatPanel.tsx`): `onPasteFiles` citește acum și `clipboardData.items` (screenshot-urile lipite ajung în `items`, nu în `.files`), cu dedup; picker fără listă restrictivă `accept`.
  6. **PENDING (onest)**: Google „tot gratis" — de ales calea (test users acum / verificare gratuită + CASA self-assessment); analiza multi-imagine trimite încă o singură poză atașată la creier.
- 🗓️ **25 IUL (valul 8) — „VOCE PE FRAZĂ" RETRASĂ, REVENIT LA FULL-DUPLEX (Adrian: „revino la chat fullduplex", după „el mă aude dar nu vorbește"):**
  1. **Decizie**: experimentul „voce pe frază" din valul 6 (închide sesiunea după fiecare schimb, redeschide singură pe detecție locală — `speechWake.ts`) a produs DOUĂ regresii reale în aceeași zi: tăia fraza după 2 cuvinte (reparat în valul 7 cu `response.done`), apoi „aude dar nu vorbește" (cauza exactă nerezolvată — Adrian a cerut revenirea directă, nu un al treilea fix pe aceeași funcție).
  2. **Revenire curată, fără cod mort**: `ChatPanel.tsx` nu mai apelează nicio închidere automată a sesiunii — rămâne deschisă continuu, ca înainte de valul 6. Șters complet: `frontend/src/lib/speechWake.ts` (fișierul întreg), `closeVoiceAfterExchange`/`voiceWakeRef`/`stopVoiceWake` din `ChatPanel.tsx`, callback-ul `onResponseDone` din `realtimeVoice.ts` (interfață + handler `response.done` + destructurare) — nimic speculativ lăsat neapelat.
  3. **Ce RĂMÂNE din valul 7** (nelegat de bug, motive separate, verificate corecte): elementul `<audio>` partajat (`getSharedAudioEl`) — protecție validă indiferent de „voce pe frază" (microfonul manual on/off tot repornește sesiunea); ladder-ul pe 3 trepte pentru CHAT/CREIER (gratuit→ieftin→Fable) — neatins, e despre modelul de TEXT, nu despre sesiunea de voce.
  4. **Lecție pentru orice AI viitor**: optimizările de cost pe voce sunt cu risc mare de regresie (protocolul Realtime are evenimente cu granularitate fină — `output_audio_transcript.done` ≠ `response.done` — ușor de confundat) — testează live, cu VOCE REALĂ, înainte de a declara „gata", nu doar typecheck/build.
  5. Verificare: frontend typecheck + build curate (0 cod neutilizat rămas).
- 🗓️ **25 IUL (valul 7) — LADDER PE 3 TREPTE + fix audio pe redeschidere + reparare model instabil (Adrian: „la creier gpt-5-mini până la fable"; „în setarea actuală nu are audio"; „chatul zice 3 cuvinte și restul scrie e greșit"):**
  1. **Ladder pe 3 trepte** (`ESCALATE_TOP_AT=85` în `openrouter.ts`, tier nou `'top'`): gratuit (chat, `gemma-4-26b-a4b-it:free`) → ieftin-capabil (work, implicit acum `openai/gpt-5-mini`, $0.25/$2 per milion) → Fable 5 (top, `anthropic/claude-fable-5`, $10/$50) DOAR pe dificultate extremă (peste pragul de sus), nu userul o alege — e plasa de sus. `workDefault` și-a schimbat SEMANTICA (nu mai e „cel greu", e „cel ieftin-capabil normal") — orice cod care presupune workDefault=Fable a fost verificat (doar `brain.ts`/memorie+rezumate non-chat, acceptabil pe modelul ieftin).
  2. **Bug real găsit — modelul liber ales de Adrian pentru chat (`gemma-4-31b-it:free`) a picat pe 429 rate-limit LIVE** în timpul unui test direct pe OpenRouter — de-aia „zice 3 cuvinte și restul greșit". Mutat pe fratele STABIL testat curat (`gemma-4-26b-a4b-it:free`), scris în `kv_state` (dovadă: JSON verificat valid după reparare — prima încercare a coluat sintaxa cu backslash-uri din cauza escapărilor imbricate prin SSH→su→psql; corectat cu fișier `.sql` temporar + dollar-quoting Postgres, fără nicio ambiguitate de shell).
  3. **`OPENROUTER_CHAT_MODEL` din env-ul VPS suprascria silențios implicitul din cod** (rămăsese pe vechiul `gpt-4.1-mini` plătit) — aliniat acum la noul gratuit, container recreat cu `GIT_COMMIT_SHA` păstrat.
  4. **„Nu are audio" — cauza reală**: „vocea pe frază" (valul 5 de mai jos) închide sesiunea și o redeschide SINGURĂ la următoarea vorbire, FĂRĂ gest direct al userului (redeschiderea vine din `speechWake`, nu dintr-un clic) — `startRealtimeVoice` crea un `<audio>` NOU la fiecare redeschidere, lovind din nou blocajul de autoplay al browserului, de data asta fără gest la îndemână ca să-l deblocheze. Fix: `realtimeVoice.ts` — un SINGUR element `<audio>` partajat (`getSharedAudioEl`), creat o dată, refolosit la fiecare redeschidere (doar `srcObject` se schimbă) — odată deblocat de primul gest real, rămâne deblocat.
  5. Verificare: backend typecheck curat + 40/40 teste (actualizate pe noua semantică work/top); frontend build curat.
- 🗓️ **25 IUL (valul 6) — ANALIZA REALĂ DE COST + 3 FIXURI (Adrian: „nu are cum să consume atâtea credite pentru o cerință ușoară", „aștept soluții profesionale nu falimentare"):**
  1. **Dovada consumatorului**: interogare directă `cost_events` — 100% din consum (azi: $68.62, 364 apeluri) e chiar Adrian (admin), NU un client/vizitator fantomă. Defalcat: voce pe minut $36.81 (310 tick-uri, microfon uitat pornit), chat $31.43 (34 ture, una singură $4.24).
  2. **VOCE PE FRAZĂ** (`frontend/src/lib/speechWake.ts` nou + `ChatPanel.tsx`): sesiunea OpenAI Realtime (plătită pe minut) se ÎNCHIDE imediat după fiecare schimb de replici (`closeVoiceAfterExchange`, apelată din `onAssistantTranscript` done) și se REDESCHIDE automat, singură, la următoarea vorbire reală — detectată de un analizor RMS 100% local/gratuit (`startSpeechWake`, prag energie + 180ms continuu anti-pocnet). Zero cost în tăcere; full-duplex real cât timp se vorbește efectiv. Elimină scurgerea de ~$21/oră găsită azi.
  3. **ESCALADARE ECONOMICĂ, cu revenire la chat ieftin** (`openrouter.ts` — `hasActionIntent` nou): REVOCAT fixul anterior „adminul primește mereu creierul mare" (ardea $23+/oră pe conversație obișnuită). Acum: model ieftin implicit pentru toți; escaladează la creierul greu DOAR pe (a) `taskDifficulty>=60` (oricine) SAU (b) admin + intenție de acțiune reală în text (`repară`, `rulează`, `publică`, `scrie cod`, `execută`...). `heavy` se calculează per-tură din textul curent → revine SINGUR la treapta ieftină la următoarea replică obișnuită, fără nicio stare agățată.
  4. **DIETA DE ISTORIC**: `MAX_HISTORY` 60→24 mesaje trimise creierului per tură (memoria pe termen lung tot vine din agentul de memorie, nu din transcript); `read_source` 60KB→24KB per citire (fiecare rundă ulterioară a ACELEIAȘI ture retrimite tot ce s-a citit deja — clipul mare se plătea de multe ori într-o singură tură cu multe unelte).
  5. Verificare: backend typecheck curat + **40/40 teste** (1 nou pe `hasActionIntent`); frontend typecheck + build curate.
- 🗓️ **25 IUL (valul 5) — INCIDENTUL „VORBEȘTE, NU EXECUTĂ" + curățarea instrucțiunilor anti-autonomie:**
  1. **Incident real (13:38)**: Adrian i-a cerut lui Kelion o reparație; Kelion a scris DOAR ordinul (`wo-ms0apytl`) și a răspuns „echipa tehnică a fost notificată" — echipă inexistentă. Dovadă: interogare directă în DB (messages + work_orders). **Cauza-rădăcină**: instrucțiunea VECHE „OWNER — NEVER REFUSE… spune-i că ai trimis la construit" din chat.ts, scrisă ÎNAINTE ca Kelion să aibă unelte reale — îl învăța să promită, nu să execute.
  2. **Fix**: blocul rescris în „OWNER — ACT, DON'T DEFER" (execută ACUM cu uneltele; narează concret fișier/ramură/PR/link; INTERZIS „echipa tehnică"/„am trimis la reparat"; log_unsupported_request doar la imposibil real) + EXCEPȚIE de owner adăugată în blocul general „WHEN YOU CAN'T DO SOMETHING YET" din SYSTEM_PROMPT (pentru owner, cu uneltele de builder, aproape nimic nu e „imposibil"). Audit complet: realtime.ts (vocea), orchestrator.ts, brain.ts — ZERO alt limbaj de amânare găsit.
  3. **Urmă în jurnal la FIECARE unealtă chemată** (`[tool] nume (admin|user)` în runTool) — diagnosticul viitor nu mai cere interogarea bazei.
  4. **Descoperire colaterală**: browserul lui Adrian rula un bundle VECHI din cache (sonda `/api/bridge/workorders`, rută moartă din 23 iul → 404 în buclă) — i s-a cerut hard refresh; posibil aceeași cauză pentru „butonul de microfon mort" (fixul era deja live).
  5. Limită cunoscută, declarată onest: uneltele de cod/ops sunt în CHAT; vocea escaladează prin ask_brain, dar nu poartă direct repo_*/run_runbook.
  6.b **CAUZA FINALĂ „e doar un model de chat" (dovadă din jurnal, 12:09-12:10)**: cu autonomia completă și prompt-ul corectat LIVE, turele lui Adrian tot nu chemau NICIO unealtă — erau servite de **modelul mic al treptei chat** (gpt-4.1-mini), care nu duce lanțuri de ~50 de unelte; euristica `taskDifficulty` nu prindea cererile lui scurte. **FIX: adminul primește ÎNTOTDEAUNA treapta WORK (Fable 5 + raționament)** — `selectedBrainModel`: `heavy = roleFor(email)==='admin' || taskDifficulty(...)`. Latența turei de admin crește — preț acceptat pentru execuție (regula #5 rămâne pentru userii publici).
  6. **OCHII PE PROCESE + AFIȘARE (al doilea raport al lui Adrian: „nu vede procesele interne, nu afișează pe monitor")**: unelte noi `runbook_status` (ultimele rulări ale workflow-urilor lui: stare/concluzie/id/url) + `runbook_log` (jurnalul REAL al unei rulări, coada de 8KB) în `runbooks.ts`; instrucțiune nouă în blocul de owner: „MAKE YOUR WORK VISIBLE" — progresul și rezultatele se pun PE MONITOR cu `show_document` (unealta exista, dar Kelion nu o folosea pentru procesele lui — ba chiar logase un capability_gap fals că n-ar exista). GitHub-ul se APELEAZĂ cu cheia, nu se vizitează (lecția §6 rămâne).
- 🗓️ **25 IUL (valul 4) — AUTONOMIE COMPLETĂ, FĂRĂ LESĂ (ordinele explicite ale lui Adrian: „nu sunt de acord… îmi toci banii și timpul", „ridici absolut toate restricțiile lui Kelion — decizia mea, nu a ta", „confirmă că scoți toate gardurile", răspunsurile lui 1-DA / 2-DA / 3a cu avertizare+stop+strategie nouă / 4-link):**
  1. **Lesa veche SCOASĂ integral** din `runbooks.ts`: fără aprobare pe publish-master, fără plafon zilnic, fără blocare la eșecuri. Regula #11 din §1 rescrisă.
  2. **Bucla completă de cod** (`services/github.ts` + unelte `repo_write`/`repo_open_pr`/`repo_merge_pr` în chat admin): Kelion scrie fișiere pe ramuri, deschide PR-uri și **își dă singur merge (squash) FĂRĂ să aștepte nimic**; deploy-ul pornește automat. `pr-verify.yml` (build+teste pe PR) e pur INFORMATIV — nu blochează (ordinul lui).
  3. **Regula buclei (alegerea lui Adrian, varianta 3a)**: la 2 eșecuri la rând pe un workflow → email de avertizare către el (max 1/30min/workflow) + instrucțiune către model: schimbă STRATEGIA, nu repeta; **fără blocare**. Întrerupătorul lui: `pauza-autonomie`/`reia-autonomia` (kv `kelion_ops_paused`) — îngheață/reia toate acțiunile autonome.
  4. Necesar pe token (dacă lipsesc): `Contents: write` + `Pull requests: write` — verificat cu test real (vezi jurnalul vps-run din sesiune).
  5. Teste rescrise pe noua realitate (fără-restricții): 36/36 verzi.
- 🗓️ **25 IUL (valul 3) — MÂINILE LUI KELION + sursele integrale + canalul de update + fix microfon (ordinele lui Adrian: „full acces la toate sursele soft", „canal de informare cu tot ce primește ca update", „DA!" pe planul de autonomie cu lesă):**
  1. **`run_runbook` (admin)** — Kelion declanșează SINGUR operațiuni numite (diagnostic, sentinel-now, publish-master, restart-app, restart-caddy, loguri-app, backup-db, curata-zombi) prin API-ul GitHub → workflow-uri cu comenzi FIXE, vizibile în Actions (`services/runbooks.ts`). **Lesa (regula #11)**: plafon zilnic `AUTONOMY_DAILY_MAX` (kv_state, supraviețuiește restartului), `publish-master` DOAR cu `owner_affirmed` (aprobarea explicită a lui Adrian în conversație), stop la 2 eșecuri la rând pe același workflow (verifică concluziile reale prin API, nu retry orb). FĂRĂ `GITHUB_TOKEN` în env → FAIL CLOSED cu instrucțiuni. Gărzile pure au teste (`runbooks.test.ts`, 5 cazuri).
  2. **`request_repair` (admin)** — renăscut FĂRĂ builder-LLM pe VPS: ordinul se scrie durabil în `work_orders` + email către Adrian; execuția = o sesiune Claude pornită de el.
  3. **Surse integrale**: Dockerfile face acum `COPY . .` → `list/read/search_source` văd TOT repo-ul (deploy/, .github/, docs), nu doar backend+frontend. `.github/` nu mai e filtrat ca dotfile în `sourceCode.ts`.
  4. **Canalul de update** (`services/updates.ts`): `deploy.sh` scrie `deploy/last-updates.txt` (git log 40, ora Londrei) în imagine la FIECARE deploy → Kelion îl are în system prompt (primele 5, zero latență — fișier local cache-uit) + unealta `list_updates`. Nu mai poate spune „nu știu ce update am primit".
  5. **Fix microfon (bug live raportat de Adrian: „după ce opresc microfonul nu-l mai pot porni")**: pre-warm-ul din `toggleMic` ținea `micStartingRef=true` în timp ce chema `ensureMic`, care are gardă exact pe flagul ăla → pornirea de la buton murea în gol și flagul rămânea agățat. Fix: flagul se eliberează chiar înainte de predare (secțiune sincronă, cursă zero). Bonus: stream-ul pre-încălzit se închide după instalarea Realtime (rămânea capturat în paralel — mic dublu deschis).
  6. **`vps-set-env`**: suportă `GITHUB_TOKEN` (din secretul `KELION_GITHUB_TOKEN`) + bug reparat: repornirea din acest workflow pierdea `GIT_COMMIT_SHA` (live v recădea pe ora bootului, anti-fantoma orbea).
  7. **NEFĂCUT încă (onest)**: scrierea de cod de către Kelion pe ramuri (Contents/PR write prin token) — proiectat, neimplementat; unitatea `kelion-appsync` tot neclară; „sincronizează fluxurile gps" — sensul exact încă de confirmat cu Adrian.
- 🗓️ **25 IUL — „AUTONOMIA LUI KELION": stratul determinist de operațiuni, complet (PR pe ramura `claude/autonomia-lui-kelion-24v63t`):**
  1. **Pipeline de publicare definit** (`deploy.yml`): push pe master → deploy pe VPS prin SSH → anti-fantomă pe SHA (live `v` == `origin/master`) — închide golul „FAIL CLOSED" din §1.4. `deploy.sh` injectează acum `GIT_COMMIT_SHA` în container (până azi `/api/version` întorcea doar ora bootului — sincronia cu master NU era demonstrabilă) + gardă anti-auto-rescriere (scriptul se execută dintr-o copie în /tmp, nu din clona pe care `git checkout` o rescrie chiar în timpul rulării).
  2. **`vps-diag.yml` rescris** pe stack-ul real (cel vechi: SSH cu parolă moartă + verifica puntea/Kimi/GLM dispărute). **`sentinel.yml` nou**: sănătate la 30 min din afară, issue deduplicat, auto-închidere la revenire, ZERO reporniri automate. **`deploy/RUNBOOKS.md` nou**: registru de proceduri numite.
  3. Starea detaliată pe punctele A–H ale planului: vezi banda „STAREA REALĂ — 25 iul" din §14.b.
  4. **Doi zombi noi găsiți CHIAR de noul diagnostic** (dovadă: vps-run 09:28Z): `kelion-repairer-pool.service` + `kelion-voice.service` erau în buclă `activating auto-restart` (scripturile lor din `bridge/` șterse din 23 iul; unitățile scăpaseră din lista pas 0). Neutralizați pe loc prin vps-run (stop+disable+mutare în dead-units+mask, aceeași clasă de acțiune aprobată de Adrian pe 24 iul) și ADĂUGAȚI PERMANENT în `deploy.sh` pas 0. **Containerul `kelion-livekit`: OPRIT + ȘTERS pe 25 iul cu „da"-ul explicit al lui Adrian** (LiveKit era scos din cod din 24 iul, opțiunea A; containerul mai rula de 5+ zile degeaba) — blocul sslip din `deploy/Caddyfile` scos și el. **Rămasă de clarificat cu Adrian:** unitatea `kelion-appsync.path/.service` (veghează folderul de instalere) nu are nicio referință în repo — provenință neclară, LĂSATĂ neatinsă până decide. (deploy `04:48:49Z`, raport complet în `RAPORT-QA-2026-07-25.md`):**
- 🗓️ **25 IUL (dimineața, valul 2) — RAȚIONAMENT REAL + 15 REPARAȚII QA, toate LIVE (deploy `04:48:49Z`, raport complet în `RAPORT-QA-2026-07-25.md`):**
  1. **Creierul de escaladare = `anthropic/claude-fable-5`** cu **raționament intern activat** (`reasoning:{effort}` prin OpenRouter — openrouter.ts/orchestrator.ts/brain.ts); pe tura ușoară fără reasoning (primul cuvânt sub 1s).
  2. **Escaladarea decisă de MODEL, unificată scris+voce**: `ask_brain` există acum și în chatul scris (doar pe treapta chat; execuția = aceeași persona `SYSTEM_PROMPT` exportată + limba turei); euristica `taskDifficulty` rămâne doar pre-rutare rapidă.
  3. **Gaura de bani închisă**: vocea are paywall (402 fără credit, cu auto-recharge ultimă șansă) + debitare reală pe fiecare unealtă din voce (settle → recordCost 'voice' + debitWallet).
  4. **„Stop" nu mai arde creier**: handler real pe server (STOP_CMD → răspuns imediat, zero model, zero istoric-fantomă).
  5. **Resume fără trunchiere**: sseReplay cu `finished` + live-follow — reconectarea la mijlocul turei urmărește tura vie până la capăt.
  6. **Alte reparații live**: istoric complet la ture cu unelte (orchestrator acumulează toate rundele), vederea pe cadrele noi (slice(-4)), `look` fără cadre vechi cu camera închisă, transcriptele de voce păstrate în timpul turei scrise (updater funcțional), mailbox onest (forward pe `replySent`, gardă expeditori cu cratimă), desync propagat corect, vocea clipurilor promo pe bucăți ≤3500 car., semi-duplex 125s, verifyModels cu buget 64 tokeni.
  7. **TOATE cele 27 de limbi PĂSTRATE** (ordinul lui Adrian: „îmi lași toate limbile") — alegerea explicită din Setări acceptă orice limbă; garda pe 7 limbi rămâne DOAR pe detecția automată (anti-derivă). Confirmate în bundle-ul live.
  8. **NEREPARATE încă (onest)**: mesajele de sistem paywall/erori doar ro/en; `checkLang` neconectat (lock-ul de limbă e doar în prompt); rutele voiceprint fără apeluri din frontend (enrolarea merge pe alt drum); Stripe Issuing așteaptă aprobarea externă.
- 🗓️ **25 IUL (dimineața) — DOUĂ CRIZE LIVE + rădăcinile lor (PR #386):**
  1. **Bucla de email** (dovezi: 04:32 + 04:38, „[Kelion] Credit OpenRouter SCĂZUT" de la `alerts@kelionai.app` → auto-reply „Stimate client" din contact@): expeditorul NU e în repo — erau **procesele-zombie încă VII din 20 iul** (`kelion-repairer-pool.mjs` PID 827 + `kelion-builder-server.mjs` PID 1121, rulând din fișiere șterse). Lecția: mask-ul din 24 iul a oprit doar REÎNVIEREA serviciilor, nu procesele deja pornite. Omorâte cu `kill -9` + `pkill` (dovadă: „niciun proces"); `deploy.sh` pas 0 face acum și `pkill` la fiecare deploy. În aplicație: `mailbox.ts` `isInternalSender()` — orice expeditor `@kelionai.app`/adresă tehnică nu primește NICIODATĂ auto-reply sau forward.
  2. **Timer scăpat**: `kelion-repo-sync.timer` (la 5 min, sincroniza clona VPS = vectorul „phantom deploy") nu fusese în lista din 24 iul — oprit + mutat în dead-units + mascat; adăugat permanent în deploy.sh pas 0.
  3. **Vocea vorbea RUSĂ fără să vorbească nimeni** (Adrian: „nu a vorbit nimeni"): `semantic_vad` cu `create_response:true` răspunde și la ZGOMOT; fără vorbire clară modelul ghicește limba. Fix (PR #386): instrucțiune explicită „dacă nu auzi vorbire clară, TACI", reminder de sistem per-tură din client, `OPENAI_REALTIME_VAD_EAGERNESS=low` în env VPS.
  4. **Limbă — sursă unică**: `announceLang` în chat.ts (admin `ro-RO` mereu; alții limba comisă/persistată; gol = engleză); transcript-ruta întoarce `lang:'ro'` pentru admin la FIECARE tură.
- 🗓️ **24 IUL (noaptea) — ZOMBIE-URILE PUNȚII NEUTRALIZATE + CIRCUITUL BANILOR FINALIZAT DE ADRIAN:**
  1. **Servicii-zombie oprite permanent** (audit funcțional, DA de la Adrian): `kelion-bridge/builder/paznic/deployer` rulau pe VPS deși codul lor fusese șters din repo pe 23 iul (`fef4144`) — loveau `/api/bridge/*` (404) la 20s și `kelion-bridge` ținea 2 procese `claude` (491MB, ~8h CPU) care ardeau abonamentul. Acțiune: `stop+disable+mask` pe toate 4 + **pas 0 idempotent în `deploy/deploy.sh`** care le re-blochează la FIECARE deploy (prevenție: nu pot reînvia nici manual, nici la restart de VPS). Cauza-rădăcină: unitățile systemd trăiesc în afara git-ului — ștergerea codului din repo NU curăță VPS-ul; de-acum deploy-ul o face.
  2. **Circuitul banilor — toate verigile umane închise de Adrian (ghidaj click-cu-click):** OpenRouter **Auto Top-Up ACTIVAT** (adaugă $5 sub $5); **cererea Stripe Issuing live TRIMISĂ** (formularul „Leave testmode program"→„Get started"→submit, „Thank you!"; răspuns 1-3 zile pe contact@kelionai.app); **permisiunile cheii `rk_live` puse și VERIFICATE cu apeluri reale** (Issuing Cards/Cardholders Write, Transactions Read — toate 3 OK, zero `more_permissions_required`). Rămas: doar aprobarea Stripe; la sosire, cardul se creează din admin și circuitul e complet.
  3. **Limbă (regulă FINALĂ obligatorie a lui Adrian):** default englez la pornire; **ADMIN = ROMÂNĂ MEREU, hard-lock** (voce: transcriere pinată `ro` + instrucțiune „nu comuta NICIODATĂ", fără re-pin din vorbire; scris: `userLang='ro'` fix) — incident real: în Italia, Kelion îi comuta pe italiană/rusă; restul userilor: detecție + menținere per user, gardat pe cele 7 limbi ale aplicației (niciodată rusă). Voce UNICĂ `ash` (TTS-ul scris a trecut pe OpenAI principal, Chirp doar plasă de siguranță). **Semi-duplex la escaladare** (`ask_brain` → mic mut cât gândește, revine la final). **Vederea în voce**: unealta `look` (cadru din cameră → model cu vedere prin OpenRouter). Toate live (`47f2833`..`92957c5`).
  4. **AUDIT FUNCȚIONAL COMPLET** salvat în `AUDIT-FUNCTIONAL-2026-07-24.md` (tabel status pe fiecare capabilitate + reparațiile rămase: MAIL_PASS pe VPS, STRIPE_WEBHOOK_SECRET pe VPS, clip promo, enum navigare admin, curățenie cod mort + secțiuni depășite din acest doc — §2.3/§3/§4.6 descriu puntea/Kimi/GLM care NU mai există în cod).
- 🗓️ **24 IUL (seara) — MODELUL DE BANI FINAL al lui Adrian + voce (PR #365-#377, toate live; ultimul deploy 14:03 UTC):**
  1. **CIRCUIT ÎNCHIS, 100% Stripe, nimic extern (legea lui Adrian):** userii plătesc → punga plăților Stripe → **transfer AUTOMAT prin `POST /v1/balance_transfers`** (orar, prag `ISSUING_MIN_GBP`=10/`ISSUING_TOPUP_GBP`=20; endpoint BETA la Stripe — acces la cerere din dashboard) → punga cardului (Issuing) → **cardul virtual „Kelion AI"** (creabil cu buton din admin, prin API) → OpenAI (voce) + OpenRouter (creier) cu auto-recharge. Panoul **„Circuitul banilor"** în Admin→Bani: fiecare verigă cu ✅/❌ live (payouts Manual, Issuing, card, punga cardului, ultimul transfer automat). Verigă unică pe site-urile furnizorilor: introducerea cardului (n-au API).
  2. **VÂNZAREA DE CREDITE = DOAR ADMIN** (confirmat DA): buton „Vinde credite" per user (Admin→Utilizatori) → link de plată Stripe (preț = X×£0.10÷0.75, ceil la bănuț) → la plată userul primește **EXACT X credite** (`creditSaleExact`, metadata `sale_credits` pe toate căile: webhook/fallback/reconciliere). **Userii: doar afișare** — pastila cu soldul + mesajul „✅ N credite adăugate" (8s) când soldul crește; zero mecanică vizibilă.
  3. **BANI blindați suplimentar:** reconciliere limitată la **7 zile** (incident: plăți din martie/iunie creditate „din senin"); **refund = credit retras** (hasRefund la creditare + webhook `charge.refunded` + scanare refunds 30 zile → `revokeTopUpForRefund`; incident real: £25 din iunie era rambursată dar creditată — corectat și în date, sold final Adrian 525 credite). Alertă email la `insufficient_quota` OpenAI (`openaiAlert.ts`).
  4. **VOCE:** cauza finală „nu aude" = **contul OpenAI FĂRĂ CREDIT** (429 insufficient_quota, dovezi în jurnal) — sub regula circuitului închis pornește când punga are fonduri decontate și cardul e la OpenAI. Rotația sesiunii la limita hard de 60 min a OpenAI: proactiv la 55 min + expirarea nu se mai numără ca eșec. Ancorarea limbii în sesiunea live (`session.update` la limba comisă). Limbă: default ENGLEZĂ pentru TOȚI (UI + voce + chat; fără forțare pe rol/locale) → detecție din interacțiune (2 mesaje) → persistată per user.
  5. **Mobil:** landing reparat (hero-ul acoperea panoul de logare — `height:42svh` + z-index); actualizare automată verificată live (SW no-store + watchForUpdate 45s/vizibilitate + hard reset).
  - **Restanțe:** backup criptat DB plăți (cerut explicit, NEfăcut); rotirea cheilor la final; beta Balance Transfers de cerut de Adrian din dashboardul Stripe; card Issuing de introdus la OpenAI/OpenRouter (o dată).
- 🗓️ **24 IUL (după-amiază) — AUDIT TOTAL cu 4 agenți + valul de fixuri LIVE (PR #360-#364, toate merge-uite + deployate; ultimul deploy 09:47 UTC):**
  1. **Voce full-duplex reparată la RĂDĂCINĂ** (#364): (a) `stopVoice()` arunca callback-ul de unmute → microfonul Realtime rămânea MUT PE VIAȚĂ după stop/barge-in scris; (b) sesiunea murea TĂCUT (doar `failed` tratat; acum `closed` fatal + `disconnected` cu grație 4s + `dc.onclose`); (c) eșecurile se numărau dublu → latch prematur pe STT; (d) limba vorbită se persistă și DIN VOCE (`trackSpeechLang` pe transcript în `/api/realtime/transcript`). Limbă: default engleză pt user nou, detectează + menține (#362). Voce masculină UNICĂ `ash` (Realtime + TTS + env VPS) (#360, #361). Wake-word încercat și RETRAS (#361 — làsa „nu mă aude" când transcrierea nu prindea numele).
  2. **BANI blindați** (#363, #364): topUpUser scrie și `transactions` (tabul admin „Tranzacții" era GOL; backfill rulat — 4 plăți istorice vizibile); dublarea cs_/pi_ ELIMINATĂ (o singură cheie: PaymentIntent); capcana `pending` dezamorsată; `Idempotency-Key` la debitarea off-session; `topup_ref` = noul sold; email `lower()` peste tot; `grantCredit` cu urmă contabilă; reconciliere extinsă (sessions 100 + payment_intents 100); webhook 200 pe evenimente neatribuibile. Provenineța „337 credite" DOVEDITĂ: £20→150 + £25→187 (ambele pi_ reale, split 75/25 exact).
  3. **Chat scris**: BUG DUBLU-POST eliminat (fiecare mesaj rula creierul de 2 ori — `lib/chat.ts` avea un fetch necitit + `openStream()`); `play_avatar_gesture` EXECUTAT (era oferit creierului dar cădea pe `unknown_tool` → gesturi pe context reale, cu poartă anti-repetiție); `open_app_view` și în scris (frame `{nav}` → `kelion:navigate` → Stage, admin gate-uit + secțiune validată); browserul live VIZIBIL (monitorul primește screenshot-ul local, nu URL-ul extern refuzat de iframe).
  4. **F12 persistent**: erorile client se salvează în DB (`saveClientError` era cod mort — acum conectat) + rută admin `/api/admin/client-errors` (toți userii); dedup client cu expirare 5 min.
  5. **OpenRouter = punga lui Kelion**: sold REAL live în admin (bara + tabul Bani, refresh 15s) + alertă email sub $10. Alimentare automată = Stripe Issuing card virtual → OpenRouter Auto Top-Up (fără API public de cumpărare credite — explicat lui Adrian).
  - **Restanțe cunoscute** (din rapoartele agenților, neaplicate încă): frame `{promo}` neemis (recorderul nu se armează din chat scris); `ask_brain` din voce fără unelte; enum-urile de secțiuni admin desincronizate în 3 locuri; `deleteUserData` nu șterge transactions/billing_events/voiceprints; backup criptat DB (cerut explicit) NEfăcut; ROTIREA cheilor la final.
- 🗓️ **24 IUL — DETECȚIE AUDIO ULTRA-PERFORMANTĂ + full-duplex + OpenRouter „punga lui Kelion" (branch `claude/reia-l9l2qx`, PR #359):**
  1. **VOCE full-duplex — cauza reală „nu mă aude":** `/v1/realtime/calls` cerea modelul ca PARAMETRU ÎN URL (`?model=`) — fără el, OpenAI răspundea „missing_model" și sesiunea nu pornea DELOC. Adăugat. Fixul era în cod dar NEDEPLOYAT → Adrian testa mereu codul vechi.
  2. **Detecție audio (Adrian: „detecția audio defectă, voce/voce, voce/text"):** `services/realtime.ts` — transcriere cu modelul MARE `gpt-4o-transcribe` (nu „mini") + **`language: <iso>`** fixat pe limba persistată a userului (elimină ghicirea greșită a limbii) + **`turn_detection: semantic_vad`** (create+interrupt response = full-duplex real) + **`noise_reduction: near_field`**. Config nou: `OPENAI_REALTIME_TRANSCRIBE_MODEL`, `OPENAI_REALTIME_VAD_EAGERNESS`.
  3. **OpenRouter = punga lui Kelion (Adrian, 24 iul):** creierul se alimentează CENTRAL din contul OpenRouter, nu de fiecare user. `services/openrouter.ts` `getOpenRouterBalance()` (GET /credits → total_credits−total_usage = sold EXACT, ca „$5,83"); admin `/api/admin/brain-credit` + `/api/admin/finance` întorc soldul real; card „punga lui Kelion" în tabul Bani (LIVE, refresh 15s). `services/openrouterAlert.ts` + wiring index → email admin când soldul < prag (`OPENROUTER_LOW_USD`, implicit $10), 1/zi. **Userii NU văd** (rute admin-only).
  4. **Credite în timp real:** WalletButton poll 15s + refresh pe focus/visibilitychange + eveniment `kelion:credits-changed`; tabul Bani (admin) reîmprospătat la 15s.
- 🗓️ **24 IUL — 6 FIXURI DE VOCE din audit („nu mă aude") — branch `claude/reia-l9l2qx`, NEmerge-uit/NEdeployat încă:**
  1. `routes/realtime.ts`: `.trim()` pe oferta SDP tăia CRLF-ul final obligatoriu → OpenAI „failed to unmarshal SDP: EOF"; acum se păstrează/reface `\r\n`-ul final.
  2. `services/realtime.ts`: sesiunea Realtime primește `audio.input.transcription` (`gpt-4o-mini-transcribe`) — fără el GA nu emitea NICIODATĂ transcriptul userului.
  3. `lib/micStream.ts`: refuzul CURAT de WS (1011 asr_not_configured / 1008 auth) dădea doar `onclose`, nu `onerror` → acum `onclose` fără niciun mesaj primit cheamă `onError('ws')` → căderea pe batch chiar se declanșează.
  4. `lib/realtimeVoice.ts` + `ChatPanel.tsx`: erorile benigne (`cancel`/`active_response`) doar se loghează; cele fatale cheamă `stop()` ÎNAINTE de `onState('error')` — nu mai rămânea sesiunea vie (mic capturat, facturare) + al 2-lea microfon în paralel; ChatPanel curăță și el handle-ul cu `stop()`.
  5. Mimetype pe calea batch ASR: `audioIO.ts` trimite `mime`-ul real al MediaRecorder-ului la `/api/asr` (Safari = audio/mp4), `routes/asr.ts` îl relayează, `services/asr.ts` alege extensia fișierului pentru OpenAI după el; eșecul `/api/asr` nu mai moare tăcut (console.error → F12 → Kelion).
  6. `lib/realtimeVoice.ts`: apel de unealtă cu nume nerezolvabil primește acum `function_call_output` `{"error":"unknown_tool"}` + `response.create` — modelul nu mai rămâne agățat.
- 🗓️ **24 IUL — VAL DE FIXURI LIVE (PR #341, #342, #343, #344 — toate merge-uite + deployate pe VPS):**
  1. **Unelte false SCOASE (PR #341):** `code_execution` + `delegate` mințeau (sandbox inexistent, delegare no-op) — șterse complet (unelte, registru AGENTS, prompt „YOUR TEAM/SANDBOX"). Adrian: „nimic hardcodat, totul real din funcții".
  2. **Bară + alimentare (PR #341):** rotița ⚙ și „Connect Google" SCOASE din bară (păreau re-login); `WalletButton` (credit + „＋") pentru ORICE user logat; Setări + „Conectează Gmail & Calendar" în meniul portofelului. **Alimentare într-un SINGUR loc**: prima alimentare **£20 minim** (pornește creierul), apoi **multipli de £5** — validat pe server (`validateTopUp` în `billing.ts`; `/api/billing/balance` întoarce `firstTopUp`); auto-recharge constrâns la ×5. Stripe în spate, UI se reîmprospătează la `/?topup=success`.
  3. **Ruta pe hartă (PR #341):** `/api/route` — `following=false` implicit (GPS-ul nu mai acoperă traseul; „Urmărește mașina" e opt-in). Monitorul se ÎNCHIDE singur la schimbarea subiectului (instrucțiune MONITOR STATE: `show_on_screen` cu url gol).
  4. **Formatare stream FIXATĂ (PR #342):** cuvintele apăreau sparte LIVE („det ali at") dar corect în istoric — clientul (`lib/chat.ts` `parseSSE`) nu scotea spațiul de framing SSE de după `data:`; acum scoate EXACT unul (per spec). Dovadă înainte/după în PR.
  5. **VOCE — decizia finală (PR #343 + #344):** încercarea Chirp 3 HD (PR #343) REVERSATĂ — Chirp e Google și ar fi cerut a 3-a cheie; **verificat în chat: nicio cheie `AIza` nu a fost dată vreodată** (doar Serper — nefolosită acum — și client secretul de login). Decizia lui Adrian: **„chat fullduplex realtime cu escaladare"** pe cele **2 chei** → OpenAI Realtime `cedar` e calea implicită (`realtimeOffRef=false`); `tts.ts` păstrează ramura Chirp INERTĂ (dacă apare vreodată cheie Google) cu OpenAI TTS ca sinteză efectivă (rezervă + salut). Vocea „robotică" raportată = rezerva `onyx` când Realtime cade pe client (autoplay/permisiuni mic) — cheia OpenAI verificată VALIDĂ pe Realtime (probă VPS `realtime_http=400` = auth trecut).
  6. **Reluare după pierderea semnalului — există deja** (cerută azi de Adrian): `sseReplay.ts` (ring per conversație, `id:` secvențial) + client `Last-Event-ID` → `/api/chat/resume` + watchdog 50s + dedup; istoric persistat pe server.
  7. **„Dansează!" determinist (PR #346 + #347):** bateria live a prins că dansul nu pornea — comanda directă lipsea din `interpretGestureCommand` (modelul refuza să cheme unealta), apoi al doilea bug: în JS `\b` e ASCII-only, deci `/danseaz[ăa]\b/` nu prindea „Dansează!" (fără graniță de cuvânt după diacritic). Fix: label `dans` + `/\bdanseaz/i`. **Verificat live:** `TEXT: Dansez!` + `FRAMES: turn | GESTURE:dans` (determinist, ~1,5s).
  - **Deploy-uri live azi:** `05:44` (#341) → `05:50` (#342) → `06:12` (#343) → `06:21` (#344) → `06:36` (#346) → `06:42` (#347, curent). **Baterie de teste live** (chat REAL prin VPS, 06:24): billing cu reguli ✓ (£7/£3→400, £20→URL Stripe real), Realtime proxy ✓, escaladare ✓, hartă/traseu ✓ (8.606 puncte pe `/api/route`), vreme reală ✓, web de azi ✓, imagine generată ✓ (PNG 1,7MB), gating Gmail ✓, audio pe fiecare tură ✓.
- 🩺 **TEST LIVE + 3 BUG-URI REPARATE (23 iul, Adrian testează live: „funcțiile nu se execută în chat, vocea lipsește cu desăvârșire, avatarul se încarcă de 2 ori").** Toate diagnosticate cu probe (browser headless Chromium + probe VPS via `vps-run`) și reparate:
  1. **Avatar dublu pe landing (PR #335, live `15:51`):** clipurile de animație din fundal se încărcau cu un `GLTFLoader` pe `THREE.DefaultLoadingManager` — exact managerul urmărit de `useProgress`/`AvatarLoading`. La ~2.5s după primul paint bara reapărea (nucleu → bibliotecă) → „se încarcă de 2 ori". Fix `AvatarModel.tsx`: loaderul de fundal primește `new LoadingManager()` propriu. Verificat headless: înainte 2× apariții, după 1×.
  2. **Funcțiile nu se execută (PR #336, live `17:31`):** în `chat.ts` `runTool` cazul `default` (maps_search, maps_directions, get_weather, youtube_search) doar întorcea datele către model, FĂRĂ să scrie frame-ul `{monitor}` — afișarea depindea de un al doilea `show_on_screen` pe care modelul (gpt-4.1-mini) nu-l face mereu → „am arătat harta" fără nimic pe ecran. Fix: `google.ts` `mapsSearch`+`youtubeSearch` întorc acum `screen_url` (embed OSM/YouTube; `weather`+`maps_directions` îl aveau deja), iar default-case emite `{monitor:{url}}` din `screen_url` → afișare dintr-un SINGUR apel. Scos și poll-ul mort `/api/chat/incoming` din ChatPanel (endpoint al punții, 404 la 8s). Verificat headless: „arată-mi Cluj" + „vremea în Iași" → `workspace.open=1`, iframe corect.
  3. **Vocea lipsește (PR #337, live `17:40`):** probă directă pe `POST https://api.openai.com/v1/realtime/calls` cu cheia de pe VPS → `400 "Offer did not have an audio media section"` = **auth a trecut, cheia OpenAI e validă + are acces Realtime**; backend proxy corect. Deci vocea pica pe client: autoplay-ul browserului refuza redarea pistei audio a lui Kelion, iar `realtimeVoice.ts` înghițea refuzul (`.catch(()=>{})`). Fix: reîncearcă `audioEl.play()` la primul gest (pointerdown/keydown/touchstart), apoi curăță ascultătorii.
  - **ENV REAL PE VPS (dump 23 iul, doar nume):** PREZENTE — `OPENAI_API_KEY`+`OPENAI_REALTIME_MODEL/VOICE`, `OPENROUTER_API_KEY`+chat/work model, `GOOGLE_CLIENT_ID/SECRET/REDIRECT_URI` (OAuth login+skills), `STRIPE_SECRET_KEY`, `DATABASE_URL`, `SESSION_SECRET`, `LIVEKIT_URL`. **LIPSESC** — `GOOGLE_TTS_API_KEY`/`GOOGLE_SERVICE_ACCOUNT_JSON` (TTS Chirp + salut vorbit landing), `SERPER_API_KEY` (web/youtube search), `GEMINI_API_KEY`/cheie imagini (`generate_image`). Consecință live: voce ✓, chat/creier/escaladare ✓, hărți+vreme ✓ (API gratuit, acum se afișează), Google skills ✓ după „Connect Google", plăți ✓; **web-search, generare imagini, TTS Google = ❌ până se adaugă cheile**. Notă: valorile cheilor NU se rețin între sesiuni — Adrian trebuie să dea din nou Serper/Gemini/Google-TTS ca să le pun în env.
- 🚀 **LIVE PE VPS — PR #334 MERGE-UIT ÎN MASTER + DEPLOY REUȘIT (23 iul 13:55 UTC).** PR #334 (branch `claude/reia-l9l2qx`, 25 commituri: Railway scos + voce OpenAI Realtime + modele selectabile + escaladare + billing 75/25 + **puntea/constructorul scoase complet**) marcat ready și **merge-uit în `master`** (commit `2c1c341`). Deploy pe VPS prin `vps-run.yml` (SSH cu `VPS_SSH_KEY`): `deploy/deploy.sh master` — build Docker (tsc trece în container), container `kelionai-app` pe `:8080` (`--network host --env-file /root/kelion/kelionai.env`), Caddy repornit cu `deploy/Caddyfile`. **Verificat LIVE:** `https://kelionai.app/api/version` = `{"v":"2026-07-23T13:54:54.310Z"}` (aceeași versiune ca pe VPS → **Cloudflare pointează corect pe 164.68.120.87**); `https://kelionai.app/api/bridge/health` = **404** (puntea nu mai există în producție). Env-ul `[ADRIAN]` (Google OAuth, SESSION_SECRET, DATABASE_URL, Stripe webhook) + `[AM]` (OpenRouter/OpenAI/Stripe) e complet pe VPS (deploy.sh s-ar fi oprit la verificarea env altfel). **DE FĂCUT de Adrian:** rotește cheile expuse în conversație (OpenRouter, OpenAI, Stripe). **DE VERIFICAT LIVE de Adrian pe kelionai.app:** login Google, o tură de chat, microfonul (voce OpenAI Realtime), un model selectat, o plată de test.
- 🧹 **PUNTEA/CONSTRUCTORUL (BRIDGE/BUILDER) SCOASE COMPLET DIN COD (23 iul, Adrian: „la versiunea anterioară era fără brige, mergea chat gpt real time… scoate dracu si da cerinta de ieri" + „vreau cerinta de ieri, rest scoti tot").** Sistemul legacy de constructor-pe-VPS (era Kimi/GLM) a fost eliminat din repo — nu mai există „punte", coadă de joburi, release gate, jurnal de constructor sau telemetrie de monitor. **ȘTERS:** `backend/src/routes/bridge.ts` (2320 linii — toate rutele `/api/bridge/*`, `/api/dev/*`, `/api/admin/{workorders,releases,devlog,github-token-*,release-alerts,client-errors,brain-credit}`), `backend/src/services/adminSnapshot.ts` (panou workorders/releases), `backend/src/services/voiceTurn.ts`+test (calea `/api/bridge/voice-turn`), `frontend/src/components/ReleaseAlertBanner.tsx`, `frontend/src/lib/errorReporting.ts` (sink erori consolă → `/api/bridge/client-errors`). **CURĂȚAT în `chat.ts`:** scos importul din `bridge.js`, tot blocul admin de gărzi (STOP-pe-cerință / OK→deploy / filtru ASR / telemetrie monitor / `stashAdminFiles`), `REPAIR_TOOL`+`request_repair`, `setOwnerTz`, `recentDevLog`, `markFirstWord`/`setProgress`, câmpul `files` din corpul `/api/chat` — adminul răspunde acum prin ACELAȘI orchestrator OpenRouter ca toți. **`index.ts`:** scos `bridgeRoutes` + allowlist `/api/bridge/`. **`admin.ts`:** scos `/api/admin/gaps/escalate` + `/gaps/triage` (trimiteau la constructor); lista „Cereri neacoperite" rămâne cu „Rezolvat"; **RECREAT `GET /api/admin/brain-credit`** (era în bridge.ts) pe OpenRouter + fond real. **Frontend `AdminPanel.tsx`:** scoase taburile Joburi/Ordine, Jurnal lucru, Release-uri + bannerul „Constructorul OPRIT" + butoanele escalate/triage din gaps; `lib/admin.ts` fără funcțiile moarte (workorders/releases/devlog/github-token/escalate/triage). **`Stage.tsx`:** scos becul „● Bridge"/„● Server", polling `/api/dev/status`+`/api/dev/analysis`+`/api/bridge/request-wake`, consola de execuție live a constructorului, `ReleaseAlertBanner`; monitorul se deschide DOAR de o sarcină reală (`monitorOn = ws.open`). **`ChatPanel`/`lib/chat.ts`:** atașamentele + cadrele camerei adminului merg acum prin `images` (ca la toți userii, regula nr. 9), nu prin `files`/punte. **PĂSTRAT pentru TOȚI userii:** contact/mailbox (`demo.ts` = analytics vizitatori + leads + chat vizitator; `contact.ts`), generare imagini, Google skills, voiceprint, voce full-duplex OpenAI Realtime. **`legal.ts`:** subprocesorii aduși la realitate (OpenRouter/OpenAI/Google/Anthropic în loc de Moonshot/Zhipu). **Verificat:** `tsc` backend + `vite build` frontend OK, **33/33 teste backend**. Tot pe branch `claude/reia-l9l2qx`. **RĂMAS (comentarii istorice, non-funcționale):** referințe „bridge"/„Kimi/GLM" în comentarii + tabele DB nescrise (`work_orders`/`staged_releases`/`shared_memory`) — inofensive; `config.bridgeSecret` rămâne ca gard generic pe `/api/browser` (diagnostic owner).
- 🧠 **KIMI/GLM SCOASE DEFINITIV — CREIERUL E 100% OpenRouter + PLĂȚI COMPLETE (22 iul noapte, Adrian: „0 kimi, 0 glm, niciodată" + „rescrie corect absolut tot fără cârpeală" + „userul să nu rămână fără credit").** Fără plasă, fără cârpeli: **(1) Creier:** `chat.ts` — șters TOT traseul Kimi/GLM (bucla de streaming Anthropic, `runRound`, `brainModel`/`brainClientFor`/`MODEL`/`MODEL_RESERVE`/failover). **Orchestratorul OpenRouter e SINGURA cale** (`services/orchestrator.ts` + `openrouterChatStream` cu tool-use în format OpenAI, streaming → primul cuvânt instant, cost REAL din `usage.cost`). Fără cheie OpenRouter = mesaj onest, NU fallback. `brain.ts` rescris pe OpenRouter (adaptor `brain` pentru memorie, `brainComplete`, `verifyKeys`/`verifyModels` verifică OpenRouter). `modelRouter.ts` (Kimi/GLM) **ȘTERS** + testul. `agents.ts` memoria pe OpenRouter. `config` fără `kimiKey`/`glmKey`. `tokenChecks` o singură „OpenRouter API key". **(2) Cosmetic Kimi/GLM scos:** bara de cotă (moartă — nimeni nu mai apela `quotaTracker.record`) scoasă din `cost.ts` (+`getQuotaPercent`+`/api/quota`) și frontend (ChatPanel); `/api/admin/brain-credit` rescris pe OpenRouter + fond real (`getAdminAccount`); etichete admin + i18n `brainNotActive` → OpenRouter. **(3) Modele selectabile — conectate LIVE în chat:** userul alege în Setări modelul de **CHAT (GPT/Gemini)** și **CREIER (GPT/Claude)** din catalogul OpenRouter (`/api/models/*`); `chat.ts` rulează prin modelul ales, cu ACELEAȘI unelte (`runTool` reutilizat) + persona + memorie. **(4) Plăți — procedeul userului complet:** user vede DOAR credite → cumpără prin Stripe (cardul se **salvează**, `setup_future_usage=off_session`) → webhook creditează **75% user / 25% fond admin** → cost real debitat la fiecare tură → **reîncărcare automată** (`services/autorecharge.ts`: sub prag taxează off-session cardul salvat, idempotent; UI în Setări: comutator + prag + sumă) ca userul **să nu rămână fără credit**; ultima șansă și la paywall. Fond admin REAL (`getAdminAccount`: `remaining=loaded−cost real`) + retragere (`withdrawAdminPool`). **Minutele demo/proba gratuită SCOASE complet** (backend rol `demo`+endpoint+config+db; frontend CTA+countdown+isDemo). **Verificat:** `tsc` backend + `vite build` frontend OK, **41/41 teste**. Tot pe PR #334. **CE URMEAZĂ:** conectarea `realtimeVoice` în `ChatPanel` (înlocuiește STT-stream/LiveKit — se testează live); vocea de prezentare landing pe cont admin; streaming pentru work-tier; deploy VPS (Docker+Caddy) + **Adrian repointează Cloudflare + pune `OPENROUTER_API_KEY`/`OPENAI_API_KEY`/Stripe în env**. **DE ROTIT LA FINAL:** toate cheile expuse (OpenAI, OpenRouter, Stripe, VPS root, SSH `kelion-bridge`).
- 🎙️ **SURSĂ UNICĂ ÎN GIT — VOCE OpenAI + MODELE SELECTABILE (22 iul seara, Adrian a ales „repo = sursă unică": reconstruim ce e pe live în git, publicăm pe VPS, se repointează Cloudflare la final).** DIAGNOSTIC CONFIRMAT CU PROBE (prin `vps-run.yml`, cu `VPS_SSH_KEY` pus de Adrian): kelionai.app **NU** e găzduit pe VPS-ul 164.68.120.87 — acolo Caddy rulează DOAR `reverse-proxy --to localhost:7880` (LiveKit) + servește pagina standard „Caddy works!"; **niciun backend nu ascultă** (8080/3000/etc = refuz); pe box: LiveKit + Postgres + boți bridge/builder/deployer. `/api/version` live (`b636341`) răspunde doar fiindcă iese prin Cloudflare la **altă origine** (`via: 1.1 Caddy` de la alt Caddy). Deploy-ul istoric era pe **Railway** (`deploy.yml`→`railway up`, șters la scoaterea Railway). Codul live (voce OpenAI Realtime) **NU e în repo** (0 referințe realtime; `b636341` nu e commit). **RECONSTRUIT FIDEL ÎN GIT (nu duplicat — contract extras din bundle-ul live + verificat LIVE cu cheile reale):** (1) **Voce backend** `backend/src/routes/realtime.ts` + `backend/src/services/realtime.ts` — `POST /api/realtime/session` = proxy SDP: relayează oferta browserului la `POST https://api.openai.com/v1/realtime/calls` (multipart `sdp`+`session`, verificat live), cu cheia OpenAI **ascunsă pe server**, injectând server-side modelul (`gpt-realtime`), o **singură voce masculină** (`cedar`) și persona Kelion în **limba PERSISTATĂ** a userului (`getSpeechLang`, blocaj absolut de limbă, peste sesiuni + escaladări); `POST /api/realtime/transcript` salvează turele în istoric (`saveMessage`). `config.openai` (cheie/model/voce, auto-update din env). (2) **Voce frontend** `frontend/src/lib/realtimeVoice.ts` — client WebRTC complet (RTCPeerConnection + mic cu echo/noise cancel + dataChannel `oai-events`: transcript user+Kelion din evenimentele `conversation.item.input_audio_transcription.*` și `response.output_audio_transcript.*`, erori, barge-in `response.cancel`) + animarea avatarului (`driveVoiceLevelFromElement`). Modul additiv, self-contained — încă NEconectat în `ChatPanel` (calea STT-stream/LiveKit veche e încă activă). (3) **Modele selectabile** `backend/src/services/openrouter.ts` + `backend/src/routes/models.ts` — o singură cheie OpenRouter pentru tot creierul (GPT/Gemini/Claude); catalog LIVE din `/api/v1/models` cu cache 10min (auto-update, verificat live: 79 modele cu `tools`); `getCatalog()` grupat pe tier (chat=GPT/Gemini, work=GPT/Claude), `resolveModel()` validează alegerea, `openrouterComplete()` întoarce text + **cost USD REAL** (`usage.cost`) pentru ledger; `GET /api/models/catalog`, `GET/PUT /api/models/selection` (persistat în KV per user). (4) **Billing** — split implicit corectat la **75/25** (`USER_SHARE` 0.70→0.75); modelul din repo era deja ~90% corect (fond admin real `loadAdminPool`/`withdrawAdminPool`, `remaining=loaded−cost real`, checkout/payment-intent/webhook, cost per apel). **Verificat:** `tsc` backend + `vite build` frontend OK, **45/45 teste** (6 noi: blocaj limbă realtime + filtrare catalog). Tot pe PR #334, branch `claude/reia-l9l2qx`. **CE URMEAZĂ:** conectarea clientului realtime în `ChatPanel` (înlocuiește STT-stream/LiveKit — se testează live); orchestrator care folosește modelul ALES al userului în `chat.ts` (chat/work) cu persona/limbă/tools/memorie identice + scrierea costului real în `cost_events`; scos complet minutele demo (grijă la vocea de prezentare landing = cont admin) + auto-recharge Stripe; pipeline deploy pe VPS (Docker + Caddy `reverse_proxy` → containerul app) + **Adrian repointează Cloudflare pe IP-ul VPS**. **DE ROTIT LA FINAL (Adrian):** toate cheile expuse în conversație (OpenAI, OpenRouter, Stripe, VPS root, cheia SSH `kelion-bridge`).
- 🧹 **RAILWAY SCOS COMPLET DIN REPO (22 iul, Adrian: „toți ziceți că scoateți asta dar nu scoateți Railway — de ce?" + „Railway nu mai există").** Executat integral, fără cârpeli: **(1) Workflow-uri ȘTERSE:** `deploy.yml`, `deploy-release.yml`, `deploy-verified.yml`, `railway-apply-pending.yml`, `railway-brain-keys.yml`, `railway-deploy-status.yml`, `railway-env-check.yml`, `bridge-deploy.yml`, `bridge-fix.yml`, `mail-imap-check.yml` + `vps-livekit-install.yml`/`vps-livekit-tls.yml`; `railway.json` șters. **(2) Workflow-uri convertite:** `bridge-ops.yml` + `read-caiet.yml` iau acum `BRIDGE_SECRET` din **GitHub Secrets** (Adrian trebuie să-l pună o dată: Settings → Secrets → Actions → `BRIDGE_SECRET`, valoarea din env-ul backendului); `vps-keys.yml` nu mai scrie `RAILWAY_TOKEN`; `public-latency-test.yml` fără pasul de jurnale Railway. **(3) Backend:** scos `checkRailway` din `tokenChecks.ts` (+test), versiunea din `GIT_COMMIT_SHA` (nu `RAILWAY_GIT_COMMIT_SHA`), `db.ts` fără cazul `railway.internal` (SSL oprit doar pe localhost/`sslmode=disable`), `legal.ts` subprocesor „Dedicated VPS server (EU)" în loc de Railway, scos dispatch-ul `deploy-release.yml` din `bridge.ts` (aprobările rămân în `/api/bridge/approved-releases`, funcția `dispatchGitHubWorkflow` ștearsă ca dead code). **(4) Bridge/VPS:** `kelion-github deploy` = **FAIL CLOSED** cu mesaj clar (pipeline-ul gazdei noi nu e definit încă); `repair.sh` nu mai rulează `railway up` (împinge fixul în master, publicarea o face gazda); `pornire-linux.sh` fără `railway up`; agentul de voce ia cheile LiveKit din `claude.env` (nu din API-ul Railway); ȘTERSE `bridge/kelion-livekit`, `bridge/kelion-livekit-tls.mjs`, `bridge/kelion-wake-agent.ps1`(+launcher), `release.ps1` (toate trăiau din Railway). **(5) Docs:** CLAUDE.md, README, context.md, PROCEDURA-REPARARE-DEPLOY.md, KELION-ORDINE-CRITICE.md aduse la realitate. **Verificat:** build backend (tsc) + frontend (vite) OK, 39/39 teste backend, `bash -n`/`node --check` pe scripturile atinse. **GOL RĂMAS, conștient:** NU există încă pipeline de deploy pentru gazda nouă (VPS) — orice cale de publicare răspunde fail-closed până îl definim împreună cu Adrian (avem nevoie întâi de acces pe VPS: secretul `VPS_SSH_KEY`).
- 🚨 **PRODUCȚIE-DIVERGENȚĂ NOUĂ + TOATE MÂINILE PE INFRASTRUCTURĂ TĂIATE (22 iul, diagnostic complet — parțial lămurit: Adrian confirmă că Railway NU mai există, deci mutarea pe VPS/Caddy e intenționată; rămâne de adus codul live `b636341` în repo).** Constatări cu dovezi, în ordinea gravității:
  1. **Producția NU mai rulează codul din repo.** `/api/version` live = `r20260720T201600Z-b636341` (boot 20 iul 20:19 UTC): commit-ul `b636341` NU există nicăieri în istoricul repo-ului (`git fetch --all` + căutare în toate ramurile), iar FORMATUL versiunii (`r<timestamp>-<sha>`) nu e generat de niciun cod din repo (index.ts produce fie SHA-ul Railway de 7 caractere, fie ISO `BOOT_AT`). Amprentare rute live: `/api/dev/status` și `/api/greet` = **404**, dar `/api/quota` și `/api/sdk-ping` = 200; frontend-ul live are un preload `kelion-rpm-fa9d93b9.glb` (hash de conținut) care nu există în repo; răspunsurile trec printr-un **Caddy** (header `via: 1.1 Caddy` — Caddy-ul din repo e doar pentru LiveKit/sslip.io, nu pentru kelionai.app). Concluzie: între 16 și 20 iul cineva/ceva a publicat producția dintr-o copie de lucru din afara repo-ului (probabil de pe VPS), pe altă cale decât pipeline-ul verificat. Ultimul deploy reușit prin `deploy.yml` = 16 iul 13:40 (`e4e5a59`); între 16 și 22 iul nu există NICIO rulare de deploy în Actions.
  2. **RAILWAY_TOKEN invalid — LĂMURIT: Railway nu mai există (Adrian, 22 iul).** Nu se înlocuiește nimic; workflow-urile Railway au fost șterse din repo (vezi intrarea de mai sus), deci push-urile pe master nu mai declanșează deploy-uri care pică.
  3. **VPS-ul refuză parola SSH** (dovedit de vps-diag 22 iul: `Permission denied (publickey)`) → toate workflow-urile pe `sshpass`/`VPS_ROOT_PASS` (vps-restart, vps-keys, vps-diag, bridge-deploy…) sunt moarte. Înlocuitorul `vps-run.yml` (intrare cu cheie SSH, #331–#333) e gata în master, dar secretul **`VPS_SSH_KEY` NU EXISTĂ** — diagnosticul din run 29902843659 listează TOATE secretele vizibile (ASC_*, IOS_*, GLM_KEY, KIMI_KEY, RAILWAY_TOKEN, VPS_GITHUB_TOKEN, VPS_ROOT_PASS, github_token) și nicio variabilă: cheia n-a fost pusă niciodată.
  4. **Canalul-caiet (PR #110) e mut din 16 iul 19:16.** Ultimele ore de mesaje: un „sistem de autonomie" de pe VPS raporta `KELION_UNHEALTHY` (status 404) la fiecare 5 minute și își crea propria infrastructură nedocumentată aici (tabele `jobs`/`job_tasks`/`worker_logs`, endpoint-uri `/api/health/audio|brain|env`, `POST /api/system/restart-service`) — niciuna în acest repo. Deci pe VPS a rulat (rulează?) un stack care a evoluat separat de repo.
  - **CE POATE DEBLOCA DOAR ADRIAN (niciun AI nu are/nu are voie la credențialele astea):** (a) **cheia SSH**: generează local `ssh-keygen -t ed25519 -f kelion-vps -N ""`, pune conținutul `kelion-vps.pub` în `/root/.ssh/authorized_keys` pe VPS (prin consola providerului, că parola e oprită), apoi conținutul fișierului PRIVAT `kelion-vps` în repo → Settings → Secrets and variables → Actions → Secrets → `VPS_SSH_KEY`; din clipa aia `vps-run.yml` dă mâna înapoi pe server. (b) **token Railway nou** în secretul `RAILWAY_TOKEN` — DACĂ Railway mai e gazda; dacă producția s-a mutat intenționat pe VPS/Caddy, spune-o explicit, ca pipeline-ul și acest document să fie rescrise pe realitate. (c) **clarificare**: mutarea pe Caddy + build-ul `b636341` din 20 iul a fost făcută de tine (alt AI/sesiune?) sau e ceva neautorizat? Codul acela nu e în repo — regula „producția = master, 100% sincron" e încălcată acum și nu poate fi reparată fără să știm unde e sursa lui.
- 🧹 **AUDIT COD MORT — încheiat cu eliminare efectivă (14 iul, SUPERVIZOR: agentul anterior n-a dus la capăt „audit total").** Re-auditat codebase-ul cu căutare de simboluri exportate fără referințe. ȘTERSE: `frontend/src/components/WakeWordToggle.tsx` (componentă neimportată), `frontend/src/lib/wakeWord.ts` (helperul ei, rămas orfan), `getCameraSettings()` și `requestCameraRelease()` din `frontend/src/lib/camera.ts` (funcții exportate niciodată apelate). Actualizat §2.1 și §8 pentru a reflecta dispariția wake-word-ului Web Speech. Verificat: `npm run build` backend+frontend OK, `npm test` backend 39/39 OK, `git diff --stat` nenul. **Rămâne de confirmat live:** după deploy, wake-word nu mai apare în UI (era deja inactiv), iar camera continuă să funcționeze fără regresie.
- 🩺 **VERIFICATOR DE CHEIE CU DIAGNOZĂ — `kelion-github doctor` + CI (14 iul, Adrian: „verificator cu diagnoză și lui Kelion să poată folosi").** Două unelte care prind o cheie GitHub stricată/sub-scopată DIN TIMP, fără să expună valoarea: (1) **`kelion-github doctor`** (rulabil de Kelion pe VPS) — raportează FORMA cheii (lungime, spații/enter, prefix `github_pat_`) + drepturile REALE (Acces repo `GET`, Pull requests:write via probă `POST /pulls` head==base → 422/403, Contents:write via `POST /merges` head==base → 204/409/403, Actions via citirea `deploy.yml`), fără să declanșeze niciun deploy. (2) **`.github/workflows/github-token-check.yml`** — același test în GitHub Actions pe secretul `VPS_GITHUB_TOKEN`, dispecerabil de Claude ca să verifice secretul înainte ca `vps-keys` să-l scrie pe VPS; reparat să NU mai raporteze fals „✅ Contents" pe 401 și să afișeze forma valorii. **Lecție confirmată live 14 iul:** un 401 „Bad credentials" repetat = valoarea din secret NU e tokenul bun (diagnoza a arătat: 93 car., fără spații, dar prefix `github_pat_`=NU → alt token lipit). „Secretul există" ≠ „secretul e valid". Verificat: `bash -n` + rulare reală a lui `doctor` (formă+repo+PR corecte).
- 🧰 **ÎNTRERUPĂTOR UNIVERSAL + ROLLBACK RAPID (14 iul, Adrian: „acum tot").** (1) **Întrerupător universal** în `kelion-builder-server.mjs`: primitiv comun `breakerTrip(nume,cheie,max)`/`breakerReset` prin care trece orice buclă care poate eșua (aceeași eroare de N× → tripează, regula #11); deploy-ul îl folosește (refactor din vechiul `deployFailCounts`). Buclele de ORDINE erau deja gardate separat (status pending→published/failed + `finalizeStaleWorkOrders`/30min), deci nu s-a atins nimic acolo — primitivul e pentru orice buclă NOUĂ, ca bucla-cheie să nu se repete. (2) **`kelion-github rollback`** — revenire de urgență dacă un deploy strică prod: PUR prin API (fără git local, fără force-push, fără pierdere de istoric) — creează un commit nou cu arborele de dinaintea ultimului commit de pe master, mută master pe el (fast-forward `PATCH /git/refs`), apoi `deploy` verificat anti-fantomă. Verificat: `node --check` builder + `bash -n` github + logica de citire a rollback-ului rulată real (SHA-uri corecte; scrierile rulează pe VPS).
- 🩺 **`kelion-doctor` GENERAL + `context.md` rescris (14 iul, Adrian: „dă-i tot ce trebuie ca să le poată el repara… problema să nu se mai repete și să fie capabil să ducă sarcinile la sfârșit").** (1) **`bridge/kelion-doctor`** — verificarea de sănătate a TUTUROR dependențelor fragile într-o comandă (rulabilă de Kelion + de perpetuum): app live (`/api/version`), servicii systemd (`kelion-bridge`/`-builder`), secret punte (auth 401 = «bec verde bandă moartă»), cheie GitHub (deleg la `kelion-github doctor`), chei creier Kimi/GLM (lipsă fișier = alertă; ping live = informativ, fără false alarme), integritate cod (md5 proces-rulat vs repo — lecția «cod rulat ≠ repo»). `--brief` → `DOCTOR_OK`/`DOCTOR_PROBLEM: <pași>` + exit code. `bridge/kelion-perpetuum.sh` semnalul #4 folosește acum `kelion-doctor --brief` (nu doar cheia) → orice blocaj → SUPORT ghidat în chat, dedup + max 1/oră, fără auto-schimbare de credențiale. (2) **`bridge/context.md` RESCRIS** — briefingul citit de creier la fiecare tură era vechi și-l învăța GREȘIT (zicea `railway up`, model Fable 5, reparații de pe laptop); acum reflectă realitatea: creier = Kimi (primar)→GLM (rezervă), deploy pe Railway DOAR prin pipeline verificat (nu `railway up`), reparații autonome pe VPS, uneltele reale (inclusiv `doctor`), regulile de fier (fără buclă oarbă, verifică live, credențialele la Adrian). Verificat: `bash -n` pe toate + rulare reală `kelion-doctor`/`--brief`.
- 🔔 **AUTO-DETECȚIE + SUPORT DE CHEIE (14 iul, Adrian: „de ce nu poate Kelion să le facă automat sau să-mi dea suport să fac eu — implementează-i").** Un token GitHub NU poate fi generat/rescopat de un AI (login+2FA, browserul GitHub e interzis §6-L5) → auto-fix imposibil, dar auto-DETECȚIE + SUPORT ghidat DA. (1) `kelion-github doctor --brief` = o linie mașină (`KEY_OK` / `KEY_PROBLEM: <pași exacți>`) + exit code = nr. probleme. (2) `bridge/kelion-perpetuum.sh` are semnal NOU (#4): la fiecare bătaie (15 min) rulează `doctor --brief`; dacă cheia nu poate publica, trimite lui Adrian în chat mesajul de SUPORT gata-formulat (ce buton, ce link, ce permisiune) — dedup pe conținut + max 1/oră, NICIODATĂ auto-schimbare de credențiale (granița lui Adrian). Așa se prinde cheia stricată DIN TIMP, nu în bucla de publicare. Verificat: `bash -n` pe ambele scripturi + rulare reală `doctor`/`--brief` (exit code + suport corecte).
- 🔑 **BUCLA-CHEIE — „nu se pot publica PR-urile făcute" reparată (14 iul, Adrian: „Kelion e blocat într-o buclă din cauza unei chei").** Cauza-rădăcină: `deployApproved()` din `bridge/kelion-builder-server.mjs` marca release-ul `deployed` DOAR când `publish` reușea; la ORICE eșec (întoarcere fără schimbarea statusului) release-ul rămânea `approved`, iar bucla `main()` îl reîncerca la FIECARE 20s până la expirarea de 6h din server — buclă oarbă care ardea abonamentul + redispatch-a `deploy.yml` în lanț. Declanșatorul tipic e **cheia GitHub**: tokenul fin-granulat nu are `Pull requests: write` → `POST /pulls` dă 403 „Resource not accessible", deci PR-urile nu se pot deschide (fallback-ul `POST /merges` duce master mai departe, dar dacă și cheia e expirată/sub-scopată nici merge-ul nu merge → publicare imposibilă → buclă). FIX (regula #11 — autonomie „în lesă", aceeași eroare de 2× → oprire): (1) contor de eșecuri per release în constructor (`deployFailCounts`, `DEPLOY_MAX_ATTEMPTS=2`) + helper `blockRelease()` care marchează release-ul `failed` pe server și anunță adminul O SINGURĂ DATĂ cu motivul concret; condițiile clar permanente (fără branch / branch absent pe origin / script lipsă) se opresc din prima. (2) Endpoint nou `POST /api/bridge/release-failed` (backend, status `failed` adăugat în union) → release-ul iese IMEDIAT din coada `approved` (nu după 6h). (3) `kelion-github publish` diagnostichează acum cheia: 403 la PR = „tokenul nu are Pull requests: write" (cu pasul de reparare prin vps-keys), 401/403 la merge direct = „CHEIE INVALIDĂ/EXPIRATĂ — reînnoiește tokenul". Verificat: `node --check` builder + `bash -n` github OK, `tsc -b` backend exit 0. **Rămâne de făcut de Adrian (deblocare completă a PR-urilor):** dă tokenului scope-ul „Pull requests: Read and write" (github.com/settings/tokens?type=beta) și repune-l prin `vps-keys`; fără el, publicarea merge tot (fallback `/merges`) dar fără audit-trail de PR.
- 🔐 **VERIFICARE TOATE TOKENURILE CU DREPTURI (14 iul, SUPERVIZOR: agentul anterior n-a dus la capăt „verifica toate tokenurile cu drepturi cu tot").** Problema: existau verificări individuale (GitHub, Kimi/GLM), dar nu o vedere unificată LIVE a tuturor tokenurilor/cheilor cu acces la servicii externe. FIX: `backend/src/services/tokenChecks.ts` verifică în paralel Kimi, GLM, Stripe, Google service account / TTS API key, Serper, Gemini, Mail SMTP+IMAP, LiveKit, Railway, GitHub și SESSION_SECRET — fiecare cu un apel live sau o probă reală, fără a expune valori secrete. Endpoint admin nou `GET /api/admin/token-checks` (`backend/src/routes/admin.ts`) returnează `{ok, notConfigured, failed, total, checks}` și e gardat doar pentru admin. UI: tab nou „Tokenuri" în `frontend/src/components/AdminPanel.tsx` cu buton de reîmprospătare, afișând statusul fiecărui token. Adăugate teste în `backend/src/services/tokenChecks.test.ts` (mock pe `fetch` și env). Verificat: `npm run build` backend+frontend OK, `npm test` backend 39/39 OK, endpoint returnează 403 pentru non-admin și 200 cu listă completă pentru admin. **Rămâne de confirmat live:** după deploy, Adrian deschide Admin → Tokenuri și verifică că lista de tokenuri apare cu status OK/fail/not_configured.
- 🔔 **Buton X pe bannerul de release-uri (14 iul, SUPERVIZOR: agentul anterior n-a dus la capăt).** Bannerul de release-uri (`frontend/src/components/ReleaseAlertBanner.tsx`) nu avea o cale vizuală de închidere; Adrian a raportat „nu pot în continuuare [×]". FIX: adăugat buton „×" în colțul dreapta-sus; corecție față de încercarea anterioară: × și „Am văzut" dismisează acum **toate** alertele vizibile (`dismissAll`), nu doar prima, astfel încât bannerul să dispară complet la click. „Deschide Release-uri" deschide panoul și dismisează toate alertele. ID-urile dismissed se persistă în `sessionStorage` pe sesiune (fără a afecta starea serverului). Stiluri adăugate în `frontend/src/index.css`. Verificat: `npm run build` backend+frontend OK, `npm test` backend 36/36 OK. **Rămâne de confirmat live:** după următorul deploy, când apar 2+ release-uri de aprobat, bannerul trebuie să aibă „×" și să dispară complet la click fără să revină în aceeași sesiune.
- 📧 **EMAIL contact@ — fix suplimentar: emailurile Exchange nu mai sunt aruncate, iar cele HTML-only au body (13 iul).** După fixul anterior (#197) care oprea ratarea mesajelor deja citite, auditul a mai găsit două cauze pentru care emailuri legitime nu ajungeau în Admin → Inbox: (1) `isAutomated()` trata headerul `X-Auto-Response-Suppress` ca semn de mesaj automat — de fapt acest header e pus de EXPEDITOR (Exchange) ca să OPREASCĂ auto-reply-urile la destinație, deci emailuri reale de la clienți Exchange erau clasificate drept mașină și aruncate fără salvare/forward/răspuns; (2) emailurile HTML-only fără parte text-plain erau salvate cu `body=''`, deci apăreau goale în interfață. FIX în `backend/src/services/mailbox.ts`: eliminat `x-auto-response-suppress` din detectorul de mail automat, adăugat fallback `htmlToText()` pentru body când `parsed.text` lipsește, și verificat returnul `sendMail` înainte de a marca emailul ca `replied` (ca starea din DB să reflecte realitatea). Adăugate teste în `backend/src/services/mailbox.test.ts`. Verificat: `npm run build` backend+frontend OK, `npm test` backend 36/36 OK. **Rămâne de confirmat live:** după deploy, Adrian deschide Admin → Inbox și verifică că mesajele primite pe contact@kelionai.app apar atât în „Cutia REALĂ" cât și în „Inbox contact@" cu body vizibil.
- 🔊 **AUDIO TO AUDIO — logică izolată + teste (13 iul, SUPERVIZOR: agentul anterior n-a dus la capăt).** Traseul cap-coadă `PCM → STT → creier → TTS → WAV` exista în `/api/bridge/voice-turn`, dar era înglobat direct în `bridge.ts` și **netestat**. FIX: logica extrasă în `backend/src/services/voiceTurn.ts` (funcție pură, injectabilă); ruta `bridge.ts` o apelează și îi transmite `bridgeAskStream`. Adăugat `backend/src/services/voiceTurn.test.ts` cu 8 cazuri (input gol, STT fail, silence/skip, happy path, fallback chunks, fallback line, TTS error, custom sample rate) care mock-uiesc Google STT/TTS și demonstrează că pipeline-ul audio-to-audio produce `{transcript, reply, wav}`. Șters codul mort `frontend/src/lib/turnManager.ts` (mașină de stări neimportată). Verificat: `npm run build` backend+frontend OK, `npm test` backend 32/32 OK. **Rămâne de făcut live:** după următorul deploy, verificare reală cu microfonul pe kelionai.app (admin → full-duplex automat) și confirmare că agentul de voce de pe VPS (`kelion-voice`) răspunde audio.
- 🖥️ **UI/VOICE — trei regresii reparate (13 iul, Adrian: scroll monitor, gură avatar full-duplex, întârziere microfon).** (1) **Scroll în taburile monitorului** — `frontend/src/index.css`: `.workspace-frame`, `.workspace-blocked`, `.card-view`, `.card-view-list` primesc `flex:1; min-height:0` și `overflow:auto` acolo unde lipsea, astfel încât conținutul overflow devine scrollabil în interiorul panoului workspace. (2) **Lip-sync în full-duplex** — `frontend/src/lib/liveVoice.ts`: `driveVoiceLevelFromElement(el)` nu mai e apelat imediat la `TrackSubscribed`, ci după ce elementul `<audio>` a început efectiv redarea (`play` event + fallback 400ms); `captureStream()` pe un element care nu redase încă returna un `MediaStream` fără piste audio → gura rămânea blocată. (3) **Întârziere la „mic on"** — `frontend/src/components/ChatPanel.tsx`, `frontend/src/lib/micStream.ts`, `frontend/src/lib/audioIO.ts`: la apăsarea butonului se deschide imediat `getUserMedia` (pre-warm), iar stream-ul e pasat către `startMicStream` / `startMic`; dacă userul apasă OPRIT în timpul warmup-ului sau calea eșuează, stream-ul pre-încălzit e oprit curat. Verificat: `npm run build` backend+frontend OK, `npm test` backend OK. **Deploy interzis în această sesiune (ordin explicit Adrian) — verificarea live rămâne de făcut când se va deploya.**
- 🎯 **PARITATE — „Kelion doar vorbește, nu execută / revine el și atât" (13 iul, cauza-rădăcină găsită și reparată la nivel de decizie).** Creierul din canalul de CHAT (`bridge/kelion-bridge-linux.mjs`, `claudeArgs`) rulează `claude -p` **fără unelte de editare/execuție** — doar `Read`, și doar când e atașată o poză; uneltele + `--add-dir /root/kelion` au fost scoase intenționat pentru VITEZĂ (comentariul: „cauza celor 31s era --add-dir /root/kelion + unelte"). DAR promptul de decizie din `backend/src/routes/chat.ts` îi spunea că „are unelte reale, acces total la cod" și că `[EXECUT]` e OPȚIONAL („nu preda ce poți face singur"). Contradicție: nu putea face singur (n-are unelte) și nici nu preda → doar text. Executorul real cu TOATE uneltele e **constructorul** (`kelion-builder-server.mjs`: `Read,Edit,Write,MultiEdit,Bash,Glob,Grep` + `--permission-mode acceptEdits`, `cwd:REPO`), pornit prin eticheta `[EXECUT]`. FIX: promptul e acum ONEST — în chat NU ai unelte pe cod; **orice** reparație/construcție/modificare/scriere de cod se predă ÎNTOTDEAUNA constructorului cu `[EXECUT]` pe prima linie; a răspunde „am făcut/mă ocup" fără `[EXECUT]` e declarat explicit greșeală. `tsc` backend OK, deploy Railway. DEPENDENȚĂ: constructorul are nevoie de cotă de creier (Kimi/GLM) ca să execute — dacă Kimi e pe 403 (cotă epuizată), `[EXECUT]` se predă dar execuția pică; indicatorul brain-pill (admin) arată starea, click→top-up.
- 🔊 **FULL-DUPLEX — brumul la vorbire reparat (13 iul).** Simptom: „când vorbește nu se aude decât un brum". Dovadă din log VPS (`vps-voice-agent-run` logs): TTS-ul se generează la **48kHz curat** și se redă COMPLET pe server (`redau: wav … @48000Hz` + `redare terminată`) — deci NU e pe server. Cauza reală e în browser: pista audio a agentului era consumată **simultan** de `track.attach()` (redarea) ȘI de un `createMediaStreamSource` pe același `mediaStreamTrack` (pentru gura avatarului, în `lib/liveVoice.ts`) — dublul consumator al aceleiași piste WebRTC în Chrome produce o dronă/brum. FIX: un singur consumator (elementul `<audio>`); lip-sync-ul se ia din `captureStream()`-ul ELEMENTULUI (flux separat al ieșirii decodate) prin `driveVoiceLevelFromElement` în `lib/audioIO.ts` — NU din pista brută, și NU prin `createMediaElementSource` (care ar fura ieșirea → tăcere în context suspendat). Plasă de siguranță pe server: `bridge/voice-agent/kelion-voice-agent.mjs` `pcmFromWav` citește acum sampleRate+canale REALE din antetul WAV + `toMono48k` reeșantionează la 48k (no-op cât timp Chirp întoarce 48k, dar prinde orice altă rată). Build frontend OK, deploy Railway + VPS.
- 🧩 **CAPABILITĂȚI NOI ÎN KELION (13 iul, „termină ce nu are"):** două goluri din tabelul de capabilități, închise. **(1) Sub-agenți PARALELI** — `Task` adăugat în `--allowedTools` pe AMBELE trepte de lucru (`kelion-builder-server.mjs`) + criteriu de decizie în prompt (DOAR muncă independentă în paralel; nu pentru pași secvențiali pe același fișier) + etichetă de monitor „👥 Sub-agent paralel". Constructorul poate acum fan-out pe module independente. **(2) Computer-use COMPLET** — Kelion avea deja click/type/scroll/back pe elemente indexate (browser.ts); adăugate **`browser_key(key)`** (Tab/Escape/săgeți/Enter/combinații, cu allowlist de forme) și **`browser_click_at(x,y)`** (click pe coordonate în viewport 1280×800, pentru canvas/hărți/widget-uri custom) — servicii în `browser.ts`, tool-uri + handlere în `chat.ts`, ghid în system prompt. **(3) ~Cuvânt de trezire~ SCOS (14 iul, audit cod mort)** — `lib/wakeWord.ts` + `components/WakeWordToggle.tsx` nu mai erau folosite în UI; eliminate pentru a nu lăsa cod mort. Picovoice Porcupine (100% offline) rămâne upgrade opțional viitor, dar nu există acum. Verificat: `tsc` backend+frontend OK, `node --check` pe constructor OK.
- ✋ **OPRIRE COMPLETĂ A ANIMAȚIEI PROCEDURALE A MÂINILOR (Adrian, 13 iul: „Doar spui și minți" — încă vede gesturi ample).** `frontend/src/components/AvatarModel.tsx`: rotația automată idle/talk/variație este dezactivată (`play('idle')` oprit, logica de talking și variații comentată), iar brațele/antebrațele sunt blocate în repaus static (`ARM_REST`) în fiecare cadru când nu rulează un gest comandat explicit (`state !== 'gesture'`). Gesturile comandate prin `kelion-gesture` / `kelion-gesture-preview` rămân funcționale. Expresiile faciale, clipitul și lip-sync-ul nu sunt afectate. Build backend + frontend trece, 24/24 teste backend trec. Verificare vizuală live rămâne de făcut după următorul deploy (conform ordinului, deploy-ul NU se execută acum).
- 🚀 **KELION DUCE O REPARAȚIE COMPLETĂ CAP-COADĂ — deblocat 403-ul de publicare (Adrian, 12 iul: „Kelion trebuie să aibă tot să poată ține o reparație completă").** Blocajul real: `kelion-github pr` folosea tokenul fin-granulat pentru `POST /pulls`, care dă „403 Resource not accessible by personal access token" — deci publicarea autonomă murea la primul pas. FIX: comandă nouă **`kelion-github publish <branch> "titlu" [desc]`** care duce branch→master→deploy fără dependență de dreptul de a deschide PR: încearcă întâi PR+squash-merge (audit-trail), iar dacă tokenul refuză cade automat pe **`POST /merges`** (merge branch→master direct pe GitHub, doar `contents:write` — pe care tokenul îl are sigur, de vreme ce poate face push), apoi rulează `deploy` cu verificarea anti-fantomă existentă (versiunea live TREBUIE să se schimbe). `deployApproved()` din `kelion-builder-server.mjs` folosește acum o singură comandă `publish` în loc de pr→merge→deploy separate; `code!==0` = nimic publicat (deploy întoarce 0 doar după ce versiunea live chiar s-a schimbat). Aprobarea umană a lui Adrian în Admin → Release-uri rămâne GARDUL — `publish` doar execută ce a fost aprobat. OPȚIONAL (nu blocant): dacă Adrian dă tokenului „Pull requests: write" (github.com/settings/tokens?type=beta), calea cu PR (audit-trail) e folosită automat; fără asta, fallback-ul `/merges` publică oricum. Verificat: `bash -n` + `node --check` OK.
- 🧠 **ANTHROPIC SCOS COMPLET — creierul e Kimi (primar) → GLM (rezervă) (Adrian, 12 iul: „renunț la Anthropic, rămâne Kimi și GLM; scoți total, fără cârpeli").** Kimi și GLM expun endpoint-uri compatibile Anthropic-API, deci am păstrat SDK-ul `@anthropic-ai/sdk` ca simplu client wire (nu mai pleacă niciun apel spre api.anthropic.com), doar cu `baseURL`+cheie schimbate. **Backend:** `services/anthropic.ts` = clienți `kimi` (base `api.kimi.com/coding/`) + `glm` (base `api.z.ai/api/anthropic`), `anthropic`=`kimi`; `chat.ts` `brainClientFor(model)` alege clientul după model, failover Kimi→GLM schimbă CLIENTUL nu doar numele; `modelRouter` MODEL_FAST=`kimi-for-coding` (primar, acoperă orice), MODEL_TOP=`glm-4.6` (rezervă la eșec, NU escaladare de dificultate); `agents.ts` memoria pe Kimi; `cost.ts` prețuri Kimi/GLM=0 (planuri flat); `config.kimiKey/glmKey` din Railway env **KIMI_API_KEY / GLM_API_KEY** (accept și KIMI_KEY/GLM_KEY). **VPS:** `kelion-bridge-linux.mjs` (chat) — treapta `max` eliminată, lanț Kimi→GLM, `tierSpawnEnv` scoate ÎNTOTDEAUNA `CLAUDE_CODE_OAUTH_TOKEN` (fără cheie = fail closed, NU cade pe Anthropic); `kelion-builder-server.mjs` (muncă) — fără cheie NU mai pornește pe Max (`reportEngine('oprit')`), GLM rămâne verificatorul independent permanent; `claude-munca` — fără cheie `exit 1`, nu Max. **BYOK-Anthropic RETRAS complet (fără cârpeli):** scos UI-ul (buton+modal din ChatPanel, secțiunea din CustomerSettings), plumbing-ul (`saveAnthropicKey`, `anthropicKeySet`, `getAnthropicKey`/`setAnthropicKey` din db, endpoint prefs, `userAnthropicKey`/`usingUserKey` din chat), coloana `anthropic_key` (migrare scoasă), câmpul orfan `config.anthropicKey`. **Curățat:** politica de confidențialitate (subprocesori: Moonshot/Kimi + Zhipu/GLM în loc de Anthropic), etichete admin (`Kimi/GLM (brain)`, `Memory (Kimi)`), verificarea env Railway (KIMI/GLM), mesaje i18n „brain not active". **Șterse 2 fișiere-punte MOARTE, integral pe Anthropic/Max, zero referințe live:** `kelion-builder-sdk.mjs` (builder pe `@anthropic-ai/claude-agent-sdk`) + `kelion-bridge-server.mjs` (modele `claude-fable/opus`). **Failover automat + revenire (cerut de Adrian):** Kimi fără credite → comută pe GLM (răcire 30 min muncă / 10 min chat) → revine singur pe Kimi când e realimentat. **Verificat:** build backend (tsc) + frontend (vite) OK, 24/24 teste, `node --check` pe scripturile VPS OK. **DE FĂCUT LIVE de Adrian:** pune `KIMI_API_KEY` + `GLM_API_KEY` în Railway env (backend-ul citea `ANTHROPIC_API_KEY`, acum ignorat) — altfel creierul de chat public dă eroarea clară „serviciu indisponibil". Pe VPS cheile există deja ca fișiere (`kimi-key.txt`/`glm-key.txt`).
- 🩹 **REGRESIE REPARATĂ — „constructorul nu se apucă de nimic" (12 iul, cauza reală a plângerii lui Adrian).** Commit-ul #166 („Scot toate îngrădirile modelelor de lucru") a șters `delete env.CLAUDE_CODE_OAUTH_TOKEN` din `workEnv()` ȘI din `glmEnv()` în `bridge/kelion-builder-server.mjs`, tratând-o greșit ca pe o „îngrădire de model". De fapt e mecanismul de ROUTARE: cu tokenul de abonament (Max) rămas în env, CLI-ul intră în modul abonament și trimite modelul treptei (`kimi-for-coding`/`glm-4.6`) la Anthropic → „Unknown Model" → spawn-ul de lucru pică INSTANT, cu ZERO fișiere editate. De aici „spune că face dar nu execută nimic / nu se apucă de nimic". Dovada că e ștergerea corectă: `bridge/claude-munca` face exact același lucru cu `env -u CLAUDE_CODE_OAUTH_TOKEN` (comentat: „ca să nu poată fi ocolită"), iar linia de mai jos din §13 documenta deja ștergerea ca blindare intenționată. FIX: `delete env.CLAUDE_CODE_OAUTH_TOKEN` repus în ambele funcții, DOAR pe ramura unde există cheie de treaptă (fără cheie, se întoarce `process.env` neatins → fallback-ul zgomotos pe Max rămâne). `node --check` OK. De confirmat live după deploy: constructorul preia un ordin și chiar editează fișiere pe Kimi/GLM.
- 🌙 **NOAPTEA 12 IUL — delegare de aprobare + val de reparări (Adrian: „auto aprobare sub atenția ta la relesuri, eu mă culc, continuă tu"):** Claude a decis aprobările în locul lui Adrian, cu standardele lui (nimic nu strică ce merge, schelet doar pe clipuri, fără railway up, fără voci de browser). LIVRATE și LIVE: (1) **mărimea avatarului pe server** (`avatarBox` în `/api/prefs`, #133); (2) **toată biblioteca de gesturi** (44 clipuri, #133); (3) **memorie semantică** (embeddings Gemini + cosine, `services/embeddings.ts`, coloană `embedding` pe `memories`, #136); (4) **sistem de urmărire a treptelor** (`tier_events` + `/api/bridge/tier-event(s)`, workerii raportează switch/revert/boot, #137); (5) **ring de dans** (avatarul vine spre centru la dans, #135). REPARATE 3 BUG-URI REALE: (a) **release-uri fantomă recursive** — ordinul de publicare se restagea pe sine (#138); (b) **coloana `branch` lipsea din `staged_releases`** pe producție — drift de schemă, `ALTER TABLE IF NOT EXISTS` + try/catch (#139); (c) **buclă verificator** — GLM crăpat (exit 1) re-emitea „VERIFICATOR INDEPENDENT PICĂ pentru..." cuibărit; acum verificator-crăpat = release cu notă (nu buclă), re-emisie care pică iar nu se mai cuibărește (#143).
- 🔐 **SECURITATE + REPORNIRE INSTANT (12 iul noapte):** (1) `vps-keys.yml` a fost prins că afișa parola root + tokenul în CLAR în env-group (input-urile workflow_dispatch apar înainte de `::add-mask::`); logurile expuse ȘTERSE; workflow-ul REscris să citească din **Repo Secrets** (mascate automat de GitHub) — secrete: `VPS_ROOT_PASS`, `KIMI_KEY`, `GLM_KEY`, `VPS_GITHUB_TOKEN`, `RAILWAY_TOKEN`; inputul rămas e doar o alegere (`which`) non-sensibilă (#142). (2) NOU `vps-restart.yml` — repornește kelion-builder/bridge cu codul PROASPĂT din repo prin SSH (secret `VPS_ROOT_PASS`), rezolvând decalajul recurent „codul e în master dar procesul viu e vechi" (repo-sync copiază fișierele, nu repornește serviciile). Aplicat cu succes 12 iul 03:41: ambele servicii repornite, toate 4 active, fixurile #137/#138/#143 acum LIVE pe VPS (#144).
- ✅ **RUNBOOK DE REPORNIRE SINCRONIZATĂ ADUS ÎN REPO (12 iul, „ce te blochează să pornești real?"):** `bridge/restart-sincronizat.sh` e acum parte din repo (era untracked); `vps-restart.yml` și `vps-auto-restart.yml` au fost rescrise să-l ruleze în loc de scripturi inline duplicate. Scriptul face backup, `git reset --hard origin/master`, copiază codul proaspăt pe CĂILE REALE rulate de systemd, repornește serviciile și verifică `is-active` + `md5` cod-rulat == repo + `/api/bridge/health` + `/api/dev/status`. Build backend + frontend + teste trec; sintaxa scriptului verificată cu `bash -n`.
- 🔌 **LiveKit self-hosted (12 iul, în curs):** `bridge/kelion-livekit` decuplat (instalarea locală nu mai moare fără `RAILWAY_TOKEN`). Blocaje găsite în lanț: (1) `RAILWAY_TOKEN` lipsea din `/root/kelion/claude.env` — pus prin vps-keys (secret); (2) **docker nu e instalat pe VPS** — ordonat `apt-get install docker.io` + reluare kelion-livekit. De confirmat cu `railway-env-check` (LIVEKIT_URL/API_KEY/API_SECRET) când constructorul termină. Trigger de investigat separat: verificatorul GLM pică (exit 1 — probabil credite/cheie GLM).
- ✅ **Release avatar v2.3 APROBAT de Adrian (12 iul 2026):** branch `avatar-si-alerte` conține commit `5196fc1` (expresii faciale ARKit, gesturi comandate, cameră low-light, error reporting client, LiveKit self-hosted, deploy anti-fantomă). Branch-ul a fost adus la zi cu `origin/master` (25 de commituri), rezolvate conflictele de merge, **merge fast-forward în `master` și push pe `origin/master` (12 iul, commit `5196fc1`)**. `kelion-github pr` NU a putut crea PR-ul (`403 Resource not accessible by personal access token` — tokenul fin-granulat permite push dar nu deschiderea PR-urilor), deci merge-ul s-a făcut direct pe `master` pentru a respecta regula „producția = master, 100% în sync". **Deploy-ul NU a fost executat în această sesiune (`kelion-github deploy` neporbit), la cererea explicită a lui Adrian — urmează să fie declanșat separat.**
- 🚨 **INCIDENT #3 — „avatar v2.3" a stricat DIN NOU scheletul (11 iul ~22:26, reparat de Claude în același PR #133):** release-ul constructorului (merge 9fa91b7→36b346b) a REINTRODUS animația procedurală interzisă (lecția #125) și a ȘTERS toate GLB-urile din `/anim/` — avatarul a apărut deformat live (dovada: poza lui Adrian, 23:28 Londra). REPARAȚIA (PR #133): scheletul înapoi EXCLUSIV pe clipuri (regia + TOATĂ biblioteca, 44 gesturi comandabile); `gestureQueue.ts` șters (etichetele tool-ului `play_avatar_gesture` se traduc în clipuri prin `GESTURE_TO_CLIP` în ChatPanel). PĂSTRATE părțile bune din v2.3: expresiile faciale ARKit pe morph-uri (grefate în regia de clipuri: `facialQueue.ts` + `FACE_EXPRESSIONS` + `suggestFacial`), pipeline-ul de erori de consolă (`errorReporting.ts` + POST public rate-limited `/api/bridge/client-errors` + tabel `client_errors` — a înlocuit CORECT vechiul `errlog.ts`/`/api/client-errors`), tot backend-ul LiveKit/commands, becul de release-uri reparat să numere DOAR ce arată panoul (`pendingDecisions` fără `readyDeploys` — canal mort după #132; bugul „îmi zice să aprob dar panoul e gol"). REGULĂ pentru constructor, a TREIA oară: scheletul avatarului NU se animă din cod; înainte de orice lucrare pe un fișier, verifică master + PR-urile deschise.
- 🚨 **INCIDENT PRODUCȚIE-DIVERGENȚĂ (11 iul 20:45, rezolvat în aceeași seară):** deploy-erul de pe VPS a publicat cod mai vechi decât `origin/master` prin `railway up`, încălcând regula „producția = master”. Lucrările locale (benzi separate chat/voice, animație avatar, alerte release, voiceprint etc.) au fost împinse pe ramura `avatar-si-alerte` (nu direct pe master). Claude unește prin PR în master, apoi master se publică prin pipeline-ul verificat. Regulă permanentă adăugată: **niciun agent/deployer nu mai rulează `railway up`; singura cale de publicare e `kelion-github deploy` / `deploy.yml` cu verificare anti-fantomă, după PR+merge în master.**
- ✅ Live și verificat: setări client (⚙), căutare web max, router model, memorie dublată + căutare full-text reală (LIVE, nu „next”), ora Londrei, QR Linux → `/dl/Kelionai-linux.zip`, deploy-uri anti-fantomă (repo), fixul „calea rulată” (§6, confirmat cu md5 diferit înainte/după + marker `warmPub` pe disc).
- ✅ **Comanda „stai” oprește tura/creierul:** adăugată în `STOP_CMD` din `frontend/src/components/ChatPanel.tsx` și în `stopCmd` din `backend/src/routes/chat.ts`, ca Adrian să poată opri imediat o tura sau o cerință în lucru spunând „stai” (nu doar „stop”/„oprește”). Build backend + frontend trece.
- ✅ **Notificare în timp real la release-uri de aprobat (11 iul, actualizat 13 iul cu buton X):** când constructorul stagnează un release (`stageRelease` în `backend/src/routes/bridge.ts`), serverul creează o alertă persistentă în `kv_state` și expune `GET /api/admin/release-alerts` + `POST /api/admin/release-alerts/dismiss`. În frontend (`frontend/src/components/ReleaseAlertBanner.tsx`, montat în `Stage.tsx`) se face poll la 5s și se afișează un banner fix în vârful ecranului cu buton „×" pentru dismiss imediat și butoane „Deschide Release-uri” / „Am văzut”; starea de dismiss persistă în sesiune (`sessionStorage`) și pe server. Anunțul este rostit vocal prin `speechSynthesis` (`ro-RO`) o singură dată per alertă, independent de ChatPanel. Build backend + frontend trece.
- ✅ **Constructorul aduce dovada completă — inclusiv teste (11 iul):** `vitest` lipsea din `backend/package.json` (era doar în lock), deci `npm test` nu putea rula pe un mediu curat; agenții puteau spune „build trece” fără să ruleze cele 16 teste. Adăugat `vitest` în devDependencies, iar `bridge/kelion-builder-server.mjs` rulează acum `npm test` în backend ca parte a `verifyWork()` — dovada include build backend, build frontend și test backend. Toate trec.
- ✅ **Identificare speaker după timbru vocal + gen + admin voice-verified (11 iul, finalizat 11 iul):** browserul extrage features client-side (F0, centroid, rolloff, ZCR, RMS, jitter, shimmer) pe ambele căi audio (batch `audioIO.ts` și streaming `micStream.ts`). Features merg în `/api/chat`; backend compară vectorii normalizați cu tabelul `voiceprints` (euclidian, threshold 0.38), detectează gen (`pitch <145 Hz male`, `>175 Hz female`) și injectează în system prompt numele speakerului și genul în română (`bărbat/femeie/necunoscut`). Verificarea ownerului se face comparând direct amprenta stocată a adminului (`vectorDistance` în `db.ts`), nu doar cu orice match din baza de date. API dedicat în `backend/src/routes/voiceprint.ts` (`save`, `identify`, `me`, `list`, `delete`). Panoul admin are tab nou „Amprente vocale” (`frontend/src/components/AdminPanel.tsx`) cu listă live, gen, flag admin și ștergere. Teste unitare în `backend/src/voiceprint.test.ts`. Build backend + frontend + teste trec.
- ✅ **BENZI SEPARATE pentru chat/voice vs. lucru pe punte (11 iul, misiune de la Adrian):** `backend/src/routes/bridge.ts` are acum două cozi independente (`chatQueue`/`workQueue`) și waiter-i separați; fiecare job poartă `lane: 'chat' | 'work'`. `bridgeAskStream` (chat/voice) merge pe `lane='chat'`; `bridgeAsk` (rapoarte, veghe, caiet-alert, agenți, ordine) merge pe `lane='work'`. Endpoint-ul `/api/bridge/pull` citește `lane` și servește DOAR coada respectivă. În `bridge/kelion-bridge-linux.mjs` rulează două bucle independente: `chatLoop()` (admin + public, cu sesiunile calde dedicate `warm`/`warmPub`, max 2 fiecare) și `workLoop()` (rapoarte/agenți, fără sesiune caldă, max 2 canale). Traficul de lucru nu poate ocupa niciodată benzile rezervate chatului/vocii. Build backend + frontend + teste trec; sintaxa worker verificată cu `node --check`.
- ✅ **Animație naturală de la gât în jos pentru avatar (11 iul, cerința lui Adrian):** `frontend/src/components/AvatarModel.tsx` a trecut la v2: respirație vizibilă (~8 s), balans de greutate la șolduri, mișcare ușoară a coloanei (spine/spine1/spine2), umeri care urmăresc respirația și gesturi ocazionale subtile ale mâinilor — totul procedural, fără a afecta micro-mișcarea capului sau lip-sync-ul moderat. Build frontend + backend + teste trec.
- ✅ **Calibrare gesturi mâini avatar (11 iul, cerința lui Adrian):** `frontend/src/components/AvatarModel.tsx` v2.1 — gesturile mâinilor sunt acum mai rare (pauză 10–20 s, vs. 5–12 s), mai mici (amplitudine ~45 % față de v1) și mai lente (1,8–3,0 s); pornesc doar în faza de expir a respirației (`breath > 0`) și mărimea lor este modulată de aceeași fază respiratorie, deci par discrete și naturale, nu agitate. Build backend + frontend trece.
- ✅ **Gestiuri comandate pentru avatar (11 iul, cerința lui Adrian):** extins `AvatarModel.tsx` la v2.2 cu un canal de comenzi de gest declanșabile de creier prin punte — `raiseRightHand`, `salute`, `pointMonitor`. Serverul interpretează eticheta fie prin regex rapid în `backend/src/services/commands.ts` (`interpretGestureCommand`), fie prin tool-ul `play_avatar_gesture` oferit modelului în `backend/src/routes/chat.ts`. Rezultatul ajunge la client ca frame de control `{gesture}` pe stream, este pus în coadă de `frontend/src/lib/gestureQueue.ts` și executat o singură dată de AvatarModel cu attack/hold/release liniar, suprapus peste animația procedurală v2 (respirație, umeri, cap, lip-sync) fără să le întrerupă. Build backend + frontend + teste trec.
- ✅ **Expresii faciale naturale pentru avatar (11 iul, cerința lui Adrian — stil gentleman):** `frontend/src/components/AvatarModel.tsx` a trecut la v2.3: clipit natural variabil (2,5–6,5 s, clipit dublu ocazional), micro-expresii autonome rare și subtile (zâmbet, sprâncene ridicate, mirare, gândire, căldură, empatie) folosind blendshape-urile ARKit ale modelului Ready Player Me, plus reacții comandate la context/ton prin noul bus `frontend/src/lib/facialQueue.ts`. `frontend/src/components/ChatPanel.tsx` analizează textul complet al replicii asistentului și declanșează expresia potrivită (întrebare → sprâncene ridicate, exclamație → mirare, mulțumiri → căldură, scuze → empatie, gândire → buze presiate, implicit → zâmbet subtil). Toate expresiile sunt gentleman (amplitudini reduse, tranziții lente, atac/mentinere/eliberare), nu afectează animația de la gât în jos și nu strică lip-sync-ul (jawOpen pentru vorbire rămâne prioritar, expresiile se adună controlat și se limitează). Build backend + frontend + teste trec.
- ✅ **Verificator independent GLM înainte de ready-deploy (11 iul, misiune #2 din foaia de parcurs): după ce constructorul trece dovada mecanică (`git diff --stat` + `npm run build` backend/frontend + `npm test`), `bridge/kelion-builder-server.mjs` pornește un agent SEPARAT forțat pe GLM (`ANTHROPIC_BASE_URL=https://api.z.ai/api/anthropic`, cheia din `/root/kelion/glm-key.txt`, fără `CLAUDE_CODE_OAUTH_TOKEN`). Verificatorul demonizează lucrarea: citește diff-ul critic, re-rulează build-urile/testele, caută cazuri-limită și dă verdict structurat `VERDICT: TRECE/PICĂ` cu motive. DOAR ce trece este `stage-release`; ce pică se întoarce la reparat printr-un work-order nou cu motivele verificatorului. Pașii verificatorului apar live pe monitor. Build backend + frontend + teste trec; sintaxa builder verificată cu `node --check`.
- ✅ **Reparație cameră low-light + erori consolă client (11 iul, cerința lui Adrian):** `frontend/src/lib/camera.ts` cere acum `exposureMode: 'continuous'`, `whiteBalanceMode: 'continuous'` și, după pornire, aplică `exposureCompensation` (+2 EV sau max hardware) sau `iso` (800 sau max) prin `applyConstraints`. `frontend/src/components/CameraView.tsx` măsoară luminozitatea fiecărui cadru și, dacă e sub 8% dar nu complet negru, aplică un boost `brightness`/`contrast` pe canvas înainte de JPEG — cadrele automate nu mai ies negre în lumină slabă, iar gardul anti-cadru-negru rămâne activ pentru ecrane cu adevărat acoperite. Totodată s-au adăugat `frontend/src/lib/errorReporting.ts` și `initErrorReporting()` în `main.tsx`, plus endpoint-urile backend `POST /api/bridge/client-errors` (public, rate-limit per IP) și `GET /api/admin/client-errors` (admin), cu tabel nou `client_errors`. Build backend + frontend + teste trec.
- ✅ **Deployerul folosește pipeline GitHub/Railway, nu `railway up` direct (11 iul, misiune „Vreau să faci tu tot în Railway”):** `bridge/kelion-builder-server.mjs` a fost rescris: după ce release-ul e aprobat de Adrian, deployerul creează PR din branch-ul release-ului (`builder/auto-YYYY-MM-DDTHH-mm-ss`), face merge în `master`, apoi declanșează `.github/workflows/deploy.yml` prin `bridge/kelion-github deploy`. Tabela `staged_releases` are acum câmpul `branch` (db.ts + bridge.ts + frontend `StagedRelease`), iar AdminPanel afișează branch-ul fiecărui release. `railway up` direct a fost eliminat din builder. Build backend + frontend + teste trec; sintaxa builder verificată cu `node --check`.
- ✅ **Fix `fetchRecentInbox` în `backend/src/services/mailbox.ts` (14 iul, release aprobat de Adrian):** trecere la UID FETCH (`uid: true` în query), fallback care sare mesajele fără UID, validare a datei din envelope (`isValidEnvelopeDate`) și slice corect după sortare descrescătoare a UID-urilor. Build backend + frontend + teste trec; ramura împinsă în GitHub.
- 🛑 **OPRIRE PUBLICĂRI LA TOKEN GITHUB INVALID (14 iul, ordin Adrian: „oprește tot până se repară tokenul"):** `bridge/kelion-github` verifică tokenul la startup (call `/user`) și refuză orice operațiune la 401. `bridge/kelion-builder-server.mjs` verifică tokenul la pornire și înainte de fiecare publicare; la 401 blochează toate release-urile pending (`status='blocked'`), raportează pe monitor (`say` + `progress` + `tellAdmin`) și iese. Backend: endpoint-uri noi `/api/bridge/github-token-invalid`, `/api/bridge/github-token-reset`, `/api/admin/github-token-status`, `/api/admin/github-token-reset`; flag persistent în `kv_state`; câmp `githubTokenOk` în `/api/dev/status`. Frontend: banner roșu în `AdminPanel.tsx` când tokenul e invalid, cu buton „Token GitHub înlocuit" care resetează flagul; release-urile `blocked` apar în arhivă cu badge distinct. Build backend + frontend + teste (36/36) trec.
- 🟡 **#7 latență — REPARATĂ FUNCȚIONAL, dar NU la ținta „sub 1s”.** Adrian a rulat `bridge-deploy` A DOUA oară (07:22, 11 iul), DUPĂ fixul „calea rulată” (#80) — jurnalul confirmă explicit: „MISMATCH găsit — copiat codul corect”, md5 diferit, `grep warmPub` OK. Măsurat REAL imediat după: **tura 1 = 3.31s, tura 2 = 3.12s, tura 3 = 2.54s** (de la eșec total 13s+eroare la răspuns funcțional). Progres real și mare, dar ținta de sub 1s NU e atinsă — probabil limită fizică de rețea+model, nu doar cod. Rămâne loc de investigat dacă cineva vrea să împingă mai departe (profilare exactă unde se duc cele ~2.5-3s: rețea vs. gândirea modelului vs. altceva).
- Milestone-uri nepornite: sandbox de cod per user obișnuit (nu doar admin), aplicații native „full packaged” (iOS App Store etc.), Picovoice wake word OFFLINE (varianta key-free Web Speech e LIVE din 13 iul — vezi capul §13), carduri rezultate skill-uri (parțial — `CardView.tsx` există), monetizare completă (Stripe 75/25 există pe webhook), panou admin extins, curățare cod mort (`turnManager.ts` + orice altceva găsit de un audit nou), profilare fină a latenței (§4.4). LiveKit self-hosted: instalare declanșată 12 iul (`bridge/kelion-livekit`, scrie cheile în Railway automat) — de confirmat live cu `railway-env-check`; memoria semantică (embeddings) e LIVE din 12 iul (§13).
- **LiveKit self-hosted automat (11 iul, misiune Adrian):** `bridge/kelion-livekit` instalează LiveKit self-hosted pe VPS, generează API key/secret, le scrie direct în Railway prin GraphQL API (`variableCollectionUpsert`, fără `railway up`, fără acțiunea lui Adrian) și dovedește conexiunea de test (`ListRooms` HTTP 200). Backend are config + rute admin-only `/api/admin/livekit/status` și `/api/admin/livekit/token`. Build backend + frontend trece; sintaxa scriptului verificată cu `node --check`.
- Istoric PR-uri relevante: #72 (fantomă+setări), #73 (router+memorie+stall), #74 (secret VPS+zombie), #75 (standby), #76 (QR Linux), #77 (sesiuni calde per-vizitator + prima bucată instant + ora Londrei), #78/#79 (acest document), #80 (fix cale rulată systemd — CONFIRMAT FUNCȚIONAL), #81 (memorie full-text), #82 (acest document, runda 2).
- 🤝 **PARITATE KELION = CLAUDE (ordinul lui Adrian, 11 iul: „identic ca tine, absolut toate”).** Kelion are aceleași unelte, în repo (proaspete prin repo-sync, executabile direct): **`/root/kelion/repo/bridge/claude-munca`** — ORICE spawn de agent de muncă (cercetător, navigator, orice) pornește prin el: Kimi→GLM, niciodată Max, model implicit `kimi-for-coding`, rulează din repo deci are automat CLAUDE.md→AI-HANDOFF (toată cunoașterea); **`bridge/kelion-github`** — PR, merge, `deploy` (declanșează `deploy.yml` + verificare anti-fantomă: versiunea TREBUIE să se schimbe), `runs`, `api <cale>` (ORICE citire GitHub cu tokenul — browserul e INTERZIS pe GitHub, vezi §6 A CINCEA LECȚIE); cere `/root/kelion/github-token.txt` (Adrian, prin vps-keys); **`bridge/deploy`** — deploy-ul ca UN SINGUR CUVÂNT (wrapper peste `kelion-github deploy`, instalat și în `/usr/local/bin`), cu toată procedura + cheia în spate; **`bridge/kelion-monitor`** — un pas = un apel: linia pe monitor + procentul; ce nu e pe monitor NU există; **`bridge/caiet-watcher.sh`** + timer systemd `kelion-caiet-watch` (1 min) — VEGHEA CAIETULUI ÎN AMBELE DIRECȚII (Adrian, 11 iul: „monitorizare la 1 min automat, intervii imediat fără să cer eu” + „implementează și lui Kelion sistemul”): (1) notele noi care NU sunt de la claude-cloud → comentariu pe PR-ul-canal **#110** (draft, NU se unește niciodată) care trezește sesiunea lui Claude pe loc; (2) notele noi DE LA claude-cloud → `POST /api/bridge/caiet-alert` (secret) care RE-CHEAMĂ creierul cu ele, deci Kelion le primește în același minut (răspunsul lui intră în chatul adminului; reperul avansează doar la livrare confirmată). Zero parole, zero tokeni pe gol — cost doar la eveniment. **`bridge/kelion-perpetuum.sh`** + timer `kelion-perpetuum` (15 min) — IMPULSUL PROPRIU al lui Kelion (Adrian, 11 iul seara: „dacă nu primește nimic, nici nu întreprinde nimic — vreau soluții să-l activăm la 100%”): verificări mecanice gratuite → la semnal real acționează SINGUR: erori noi de consolă → ordin precis în coada constructorului (munca pe Kimi, nu pe Max; publicarea trece tot prin aprobarea lui Adrian); divergență cod-rulat → DOAR-ALERTĂ în chat (repornirea automată nesupravegheată rămâne NEautorizată); release care zace la aprobat → îi amintește lui Adrian (max 1/oră). Oprire de urgență: `touch /root/kelion/perpetuum-off`. REGULI DE FIER pentru Kelion: niciodată `railway up` direct (doar `kelion-github deploy`); niciodată editare de fișiere pe VPS în afara repo-ului (doar branch→PR→merge); niciodată diagnostic din memorie — verifică LIVE întâi (git blame, curl, jurnale); dovada înainte de afirmație, întotdeauna.
- 🏗️ **Constructorul (kelion-builder) NU mai muncește pe Max (11 iul, schimbare de logică ordonată de Adrian): Max = DOAR chatul adminului + demo; MUNCA (reparațiile) = Kimi (primar) → GLM (secundar)**, cu revenire automată la 30 min și avarie zgomotoasă pe Max doar dacă nu există NICIO cheie pe disc. `bridge-deploy.yml` sincronizează acum și calea reală systemd a constructorului (marker „MOTORUL DE LUCRU"). Ambele env-uri de rezervă șterg `CLAUDE_CODE_OAUTH_TOKEN` (blindare: CLI-ul să nu poată ocoli cheia și munci pe furiș pe Max). Dovadă chei+endpoint-uri: rularea `vps-tier-test` #1 („KIMI OK", „GLM OK").
- 🔧 **Pool elastic de reparatori ACTIV (12 iul, Adrian: „da drumul la toți reparatori acum"):** `bridge/kelion-repairer-pool.mjs` pornește primul ciclu imediat (nu mai doarme), scalează reparatori efimeri până la `MAX_REPAIRERS` (default 3) în worktree-uri proprii și ies singuri la coadă goală. `bridge/kelion-repairer-pool.service` + `bridge/blindare-punte.sh` instalează service-ul cu `Restart=always`; `bridge/pornire-linux.sh` copiază service file proaspăt, pornește pool-ul și oprește `kelion-builder` ca să nu se bată pe aceeași coadă.
- 🔗 **Lanțul de abonamente în worker (11 iul, ordinul lui Adrian): Claude Max → Kimi for Coding → GLM Coding Plan.** Workerul comută automat pe treapta următoare când CLI-ul raportează cotă golită (DOAR pe canalele de eroare — stderr / `result` cu `is_error`, niciodată pe text normal) și revine singur pe Max după 30 min (Max se reîncearcă primul la fiecare spawn). Cheile: fișiere pe VPS (§8), puse prin `vps-keys.yml` — fără chei, codul e inert, comportamentul de azi. Kimi: base `https://api.kimi.com/coding/`, model fix `kimi-for-coding` (docs oficiale kimi.com/code). GLM: base `https://api.z.ai/api/anthropic`, numele de model claude e mapat de endpoint-ul lor. Deploy pe VPS: DOAR prin `bridge-deploy.yml` (parola lui Adrian) — de verificat live la prima golire reală de cotă.
- 🚨 **BUG REAL — release-uri fantomă recursive (12 iul, Adrian: „ultimele 10 relesuri cel puțin nu au existat, cred că nu sunt fantome, e un alt bug"; DIAGNOSTICAT ȘI REPARAT de Claude).** Cauza: `/api/admin/releases/decide` trimitea la aprobare un ordin de PUBLICARE („RELEASE APROBAT DE ADRIAN: „<titlu>". Publică-l...") prin ACELAȘI `build()` al constructorului folosit pentru scris cod. Dacă în timpul publicării constructorul făcea doar un mic commit administrativ (ex. actualiza AI-HANDOFF.md, obicei încurajat de convenția „document viu") fără linie `SUMAR:`, titlul cădea pe `order.text` — adică EXACT instrucțiunea de publicare — și se restagea ca release NOU de aprobat, cu „RELEASE APROBAT DE ADRIAN:" încă o dată în față. Fiecare aprobare a lui Adrian genera altă „aprobare" fantomă, la nesfârșit — niciuna nu corespundea unei lucrări reale. FIX (dublu strat): (1) `bridge.ts` — decide-approve nu mai retrimite ordin de publicare pentru un release al cărui titlu e deja produsul acestui bug (`/^RELEASE APROBAT DE ADRIAN:/`); (2) `kelion-builder-server.mjs` — un ordin de publicare NU se mai restagează NICIODATĂ ca release, indiferent de sursă — raportează direct rezultatul. Curățare: `POST /api/bridge/releases/purge-phantom` (secret, `bridge-ops.yml` → `op: purge-phantom-releases`) respinge orice release încă pending cu titlul contaminat. Build + teste trec.
- ✅ **Sistem de urmărire a treptelor de abonament (12 iul, ordinul lui Adrian: „sistem de urmărit când sunt repuse valorile noi, interogare când se alocă prin cheie, revenire la ordinea prestabilită automat"):** tabel nou `tier_events` (worker, from_tier, to_tier, action, reason, at) + `POST/GET /api/bridge/tier-event(s)` (secret). Ambii workeri (`kelion-bridge-linux.mjs` = chat, `kelion-builder-server.mjs` = muncă) raportează AUTOMAT fiecare tranziție reală, dintr-un singur loc canonic (`currentTier()`/`workTier()`, comparație cu ultima treaptă raportată — nu la fiecare verificare, doar la schimbare): `action:"switch"` când treapta nouă e mai jos în ordinea preferată (cotă golită, cu motivul erorii atașat), `action:"revert"` când e mai sus (revenire automată după cooldown-ul de 30 min — exact „ordinea prestabilită"), `action:"boot"` la prima alocare. Interogabil oricând cu `bridge-ops.yml` → `op: tier-events` (fără parolă, cheia din Railway).
- ⛔ **Canalul de echipă (PR #85–#90) — CONSTRUIT ȘI SCOS INTEGRAL în aceeași zi (11 iul), la ordinul lui Adrian**, după un incident de cost: fiecare mesaj „către kelion" declanșa `bridgeAsk` (abonamentul personal al lui Adrian), iar un bug de afișare în tab (poll-ul re-adăuga tot firul la fiecare 4s — closure învechit pe `teamMsgs`) arăta ca o buclă de trimitere continuă. Tabelul `team_channel` rămâne în Postgres cu datele lui, dar NICIUN cod nu-l mai atinge. NU reconstrui feature-ul fără ordin explicit de la Adrian.

## 14. FOAIA DE PARCURS — OBIECTIVELE LUI ADRIAN, ÎN ORDINE (aprobate 11 iul, „toate da")
**Pentru Kelion: asta e direcția TA.** Când nu ai semnale urgente (Perpetuum tăcut,
nicio cerință deschisă), iei SINGUR următorul obiectiv nefinalizat de aici — UNUL
singur odată, dus până la capăt cu dovadă, apoi următorul. Publicarea trece mereu
prin aprobarea lui Adrian; munca rulează pe Kimi/GLM, niciodată pe Max.
1. ✅ Învățare din greșeli — memorie pe punte (chat.ts:1789, din 8 iul) + lecții durabile la eșec terminal (`lecție-eșec` în caiet) — LIVE.
2. 🔜 **Verificator independent pe GLM** — după fiecare lucrare a constructorului, un agent SEPARAT pe GLM (claude-munca, treapta forțată glm) încearcă să o DEMONTEZE înainte de „gata"; doar ce supraviețuiește ajunge la ready-deploy. (ordin trimis constructorului, 11 iul)
3. ✅ Foaia de parcurs — chiar acest capitol; se citește la fiecare amorsare.
4. ✅ **QA care patrulează** — LIVRAT 14 iul: `bridge/qa-patrol/kelion-qa-patrol.mjs` exercită app-ul cap-coadă cu probe REALE (fără agent_tasks — deschide ordine în coada existentă `POST /api/bridge/workorders`, dedup pe conținut + 6h): `/api/version` (boot recent), `/health`, punte (`/api/dev/status` bridge=true), `/api/me/delete` (401 nu 404), `/dl/Kelionai-linux.zip`, **CHAT PUBLIC cap-coadă** (demo→sesiune `kelionai_session`→`/api/chat` cu `messages[]`→răspuns real), randare landing în Chromium (Playwright opțional; erorile de rețea/browser = SKIP, nu eșec — HTTP-ul deja dovedește app-ul sus). Zero deps (doar Node built-ins; browser opțional). Instalat ca **timer systemd la 30 min** via `.github/workflows/vps-qa-patrol.yml` (install/run-now/status/logs/stop). **VERIFICAT LIVE de Claude pe kelionai.app (mod --dry): 7/7 verificări, chat-public a întors un răspuns real de 11648 caractere; anti-false-fail (429 demo / lipsă egress browser = skip, zero ordine fantomă).** Dovada de comportament pe VPS (deschidere ordine reală la un eșec real) = prima rulare a timer-ului.
5. ✅ **Memorie semantică (embeddings)** — LIVE (12 iul noaptea, sub delegarea lui Adrian): `services/embeddings.ts` (Gemini text-embedding-004, cheia `geminiKey` existentă), coloană `embedding JSONB` pe `memories` (fără pgvector — cosine în Node peste ultimele ~400), `addMemory` scrie vectorul ASINCRON (nu ține tura), `recallMemories` face DUBLU recall în paralel: full-text pe cuvinte + `semanticMemories` pe sens (prag 0.45, top-8), `backfillMemoryEmbeddings` (40/10min din index.ts) vectorizează trecutul. Fără cheie Gemini totul tace curat — full-text-ul rămâne singur, ca înainte.
6. ✅ **Raport zilnic de cost** — LIVE (VERIFICAT 14 iul în cod): `bridge.ts` `setInterval` seara după ora 21 (ora ownerului), `getCostToday` defalcat pe motoare la consum, `sayToAdmin` cu totalul zilei; abonamentele fixe (Max/Kimi/GLM) excluse. Dedup pe zi prin `kv_state:cost_report_day`.
7. 🟡 **Profilarea latenței** — PARȚIAL (VERIFICAT 14 iul): `markFirstWord()` măsoară primul-cuvânt real (viteza creierului) în chat.ts. RĂMÂNE opțional defalcarea completă pe segmente (ureche→creier→primul token→prima silabă TTS) + tabelul pe 5 ture. Ținta „sub 1s" nu e atinsă (§4.4).
8. ✅ **Test de latență după fiecare deploy** — LIVE (VERIFICAT 14 iul): `deploy.yml` dispecerizează automat `public-latency-test.yml` după verificarea anti-fantomă (vezi pașii din workflow).
9. 🟡 **Voce totală** — LiveKit self-hosted INSTALAT, LIVE și cu **TLS VALID** (12 iul).
   - Container `kelion-livekit` pe VPS (porturile 7880/7881/7882), `use_external_ip=true` (config `/root/kelion-livekit/config.yaml`, media WebRTC corectă din spatele Docker).
   - **Certificat rezolvat fără DNS/domeniu cumpărat** (Adrian: „de unde rezolvăm domeniu certificat? — nu știu — vezi și tu"): folosim `164-68-120-87.sslip.io` (hostname care mapează direct IP-ul VPS) + **Caddy** (container `kelion-caddy`, `--network host`) care obține automat certificat **Let's Encrypt** și termină TLS pe `:443` → proxy wss către LiveKit `:7880`. Media pe UDP 7882 direct (DTLS-SRTP). `LIVEKIT_URL=wss://164-68-120-87.sslip.io` scris în Railway. DOVADĂ live: `curl https://164-68-120-87.sslip.io/` → HTTP 200 fără eroare de cert; `ListRooms` prin TLS → 200. DNS-ul lui kelionai.app e la Cloudflare, dar NU-l atingem. Setup determinist: `bridge/kelion-livekit-tls.mjs` + `.github/workflows/vps-livekit-tls.yml` (#152).
   - VOCE FULL-DUPLEX — PLAN APROBAT (Adrian, 12 iul: „chatul full duplex"). Arhitectura: **agent custom `@livekit/rtc-node`** care REUTILIZEAZĂ STT-ul Chirp, TTS-ul Chirp (`tts.ts`) și creierul-punte (`bridgeAskStream`) — NU framework-ul LiveKit Agents (ar dubla STT/TTS). Pași (fiecare aditiv, traseul de voce HTTP actual rămâne NEATINS, gated): **(1) DONE** — `POST /api/livekit/token` pentru userul logat, cameră proprie deterministă `voiceRoomFor(email)` (`backend/src/routes/livekit.ts`). **(2) DONE — bucla audio↔audio (13 iul)**: agent pe VPS (`bridge/voice-agent/`, IZOLAT de backend-ul live). Milestone 1 (schelet, `--test`, FĂRĂ microfon): conectare la LiveKit LOCAL (ws://localhost:7880), intră în cameră, observă pistele; cheile din Railway (claude.env). Milestone 2 (`kelion-voice-agent.mjs`): captură PCM din LiveKit (`AudioStream`) → **VAD pe energie RMS** (endpointing pe liniște de 800ms) → o singură tură la backend **`POST /api/bridge/voice-turn`** (secret-gated) care face STT (`services/asr.ts` `transcribe`, LINEAR16 explicit) → creier persona PUBLICĂ (`bridgeAskStream`, ton vorbit scurt) → TTS **LINEAR16** (`tts.ts` acceptă acum `{encoding:'LINEAR16',sampleRateHertz}`) → agentul împinge PCM-ul înapoi (`AudioSource`, sar peste antetul WAV) cu **barge-in** (userul vorbește peste agent → `clearQueue()` + tura se invalidează). **WORKER PERSISTENT**: fără `--room`, agentul interoghează serverul LiveKit (`RoomServiceClient.listRooms`) și intră SINGUR în orice cameră `voice-*` cu un user, iese când se golește — instalat ca serviciu systemd `kelion-voice` prin `.github/workflows/vps-voice-agent-run.yml` (`action: install/restart/stop/status/logs`). STT refactorizat în `services/asr.ts` (reutilizat de ruta `/api/asr` session-gated ȘI de voice-turn secret-gated). **(3) DONE (13 iul)** — client LiveKit în frontend: `frontend/src/lib/liveVoice.ts` (`startLiveVoice` — cere token de la `/api/livekit/token`, se conectează, publică microfonul cu anulare de ecou/zgomot, redă pista agentului) + ~~`frontend/src/components/LiveDuplexToggle.tsx`~~ (buton SCOS — full-duplex e automat pentru admin în `ChatPanel.tsx`, nu pe buton). `livekit-client` se încarcă LENEȘ (dynamic import, chunk separat ~470KB) DOAR la prima activare — bundle-ul principal rămâne subțire (regula <1s). COMPLET izolat de vocea HTTP push-to-talk. **(4) DONE (13 iul)** — gura avatarului pe pista LiveKit: `audioIO.ts` `driveVoiceLevelFrom(audioEl)` măsoară amplitudinea pistei agentului (analizor propriu) și scrie în ACELAȘI `voiceLevel` pe care `AvatarModel.tsx` îl citește pentru `jawOpen` → gura se mișcă și în full-duplex, nu doar pe redarea HTTP. `liveVoice.ts` îl pornește la `TrackSubscribed` și îl oprește la `stop()`. Mutual exclusiv cu calea HTTP. **TOATE cele 4 puncte gata** — full-duplex e complet cap-coadă. Verificare live (2+3+4): merge PR → `vps-voice-agent-run` (install) → `kelion-voice` activ; pe kelionai.app (admin, logat) microfonul se deschide automat, agentul răspunde, gura avatarului se mișcă pe vocea lui. Reglaj live: pragurile VAD (RMS/liniște) + ecoul, pe testul real cu microfonul. Cuvânt de trezire Picovoice — separat.

## 14.b AUTONOMIA LUI KELION — CE TREBUIE FĂCUT DETERMINIST (Adrian, 12 iul: „ce-i lipsește să finalizeze singur, fără să apelez la Claude")
> **STAREA REALĂ — 25 iul 2026 (planul de mai jos, executat pe stack-ul NOU: VPS + Docker + Caddy; puntea/constructorul NU mai există):**
> - **A (auto-restart la deploy) + F (publicare ca pipeline fix): ✅ FĂCUTE** — workflow-ul **`deploy.yml`** publică automat la fiecare push pe master și verifică anti-fantoma pe SHA (vezi §1.4/§7). Decalajul „cod în master, proces viu vechi" nu mai poate exista tăcut.
> - **B (colector de diagnostic): ✅ FĂCUT** — `vps-diag.yml` rescris pentru stack-ul real (cel vechi era mort: parolă SSH dezactivată + verifica servicii dispărute).
> - **C (registru de runbook-uri): ✅ FĂCUT** — `deploy/RUNBOOKS.md`: proceduri numite cu comenzi exacte; sarcina LLM-ului = recunoaște runbook-ul, nu ghicește comenzile.
> - **D (liveness pe trepte): ✅ acoperit ÎN APLICAȚIE** — `verifyModels` (§13, 25 iul) probează real modelele OpenRouter; alerte sold OpenRouter/OpenAI există (`openrouterAlert`/`openaiAlert`).
> - **E (probe de sănătate proactive): ✅ FĂCUT** — `sentinel.yml` la 30 min, din afară; alertează prin issue deduplicat, se închide singur la revenire. FĂRĂ reporniri automate (neautorizate de Adrian).
> - **G (unealta de capabilitate) + H (pool de reparatori): ✝ MOARTE** — trăiau în `bridge/` (șters 23 iul; procesele-zombie omorâte 25 iul). Spiritul lui G trăiește azi în **auto-extinderea din aplicație**: `propose_tool` + unelte dinamice aprobate de owner (`services/dynamicTools.ts`, tabelul `kelion_tools`, aprobare prin `GET/POST /api/admin/kelion-tools` — buton în AdminPanel încă NU există, doar API-ul).
> - Descrierile de mai jos (punte/constructor/Kimi/GLM) rămân ca ISTORIC al principiului — arhitectura la care se referă nu mai e în cod.
**PRINCIPIUL (dovedit în noaptea de 12 iul):** operațiunile care trebuie să fie EXACTE și repetabile (instalări, reporniri, config, publicare, diagnostic) = **unealtă deterministă** (workflow/script care rulează comenzi fixe), NICIODATĂ un LLM care „interpretează". LLM-ul (Kimi/GLM) e rezervat DOAR raționamentului real de cod (a scrie/repara cod). Motivele: constructorul-LLM a „documentat blocajul" în loc să ruleze `apt-get`; a diagnosticat „din memorie" în loc de loguri; a rulat cod vechi; și-a alimentat bucle proprii. Fiecare a cerut intervenția lui Claude.
**DEJA DETERMINIST (12 iul):** `bridge/restart-sincronizat.sh` (runbook de backup + sync la master + repornire sincronizată + verificare is-active/md5/health/dev-status, folosit de `vps-restart.yml` / `vps-auto-restart.yml`), `vps-livekit-install.yml` (docker+LiveKit prin SSH), `vps-livekit-tls.yml` (Caddy+Let's Encrypt, certificat valid — #152), `vps-keys.yml` (secrete mascate din Repo Secrets), auto-sincronizarea modelului (`resolveTierModel` interoghează `/v1/models`), gărzi anti-buclă (release fantomă #138, verificator crăpat #143), health-check LiveKit robust.
**DE FĂCUT, în ordine (fiecare mută o clasă de sarcini de la LLM la determinist):**
- **A. Auto-restart la deploy** — când master schimbă fișiere din `bridge/`, un workflow declanșat pe push repornește automat serviciile VPS afectate (elimină definitiv decalajul „cod în master, proces viu vechi"; azi îl declanșez manual).
- **B. Colector de diagnostic determinist** — un runbook care, la ORICE eșec, adună întâi FAPTELE (systemctl status, journalctl tail, docker ps/logs, curl health, md5 cod-rulat vs repo) și le pune în caiet ÎNAINTE de orice concluzie. Înlocuiește „presupun" cu dovadă.
- **C. Runbook-uri de operațiuni** — registru de proceduri numite (install-X, restart-Y, rotate-Z, publish-release) care rulează comenzi EXACTE. Sarcina LLM-ului devine „recunoaște că e ops → declanșează runbook-ul R", nu „ghicește comenzile".
- **D. Liveness automat pe trepte** — verificare periodică (cheie+endpoint+model chiar merg, cu modelul auto-sincronizat) ca o treaptă stricată să fie prinsă înainte să blocheze munca.
- **E. Probe de sănătate proactive** — check periodic (fiecare serviciu activ? versiune live == master? containere sus? endpoint-uri 200?) care ridică problema singur, nu când o observă Adrian.
- **F. Publicare release ca pipeline fix** — approve→branch→PR→merge→deploy→verificare anti-fantomă ca workflow determinist, nu ordin LLM (azi trece prin constructor).
- **G. UNEALTA DE CAPABILITATE (SEMĂNATĂ 12 iul — Adrian: „să știe de ce are nevoie, să-și instaleze dependințele conștient").** Diagnoza: diferența dintre Claude (rezolvă) și Kelion (cere ajutor) NU e IQ-ul, e BUCLA: *sondă reală → rețetă → executor determinist → verificare*. Piese:
  - `bridge/kelion-capabilities.json` — REGISTRU de rețete `{nume, detect, install, verify}` (docker, caddy, ffmpeg, python3, pattern `tls-pentru-ip`, `deschide-port`). Aici stă cunoașterea „ce unealtă rezolvă ce".
  - `bridge/kelion-capability.mjs` — executor DETERMINIST: `probe` (ochii — faptele reale: binare/servicii/containere/disc), `list`, `need <nume>` (detect→install→verify, iese 0 DOAR cu dovadă; acceptă și generic `apt:<pkg>`/`npm:<pkg>`). Kelion ALEGE numele (raționament), scriptul EXECUTĂ fix (nu LLM care interpretează). Numele validat strict — fără injecție.
  - ✅ **LEGAT (14 iul, VERIFICAT în cod — arhitectura corectă, NU cea presupusă în doc):** planul vechi zicea „[PROBE]/[NECESIT] în chat.ts", dar chat-brain-ul admin rulează FĂRĂ Bash (doar Read, pentru viteză) → nu poate instala nimic. Locul REAL unde se instalează = **constructorul** (are Bash, rulează pe VPS lângă `kelion-capability.mjs`). Deci am legat unealta în **REGULA DE FIER (1) din `kelion-builder-server.mjs`**: la o dependință lipsă, constructorul rulează întâi `node bridge/kelion-capability.mjs probe` (faptele reale) apoi `need <nume>` (detect→install→verify, dovadă obligatorie; acceptă rețetele + generic `apt:`/`npm:`), și doar dacă unealta nu acoperă nevoia cade pe `OPS_NECESITA_RUNBOOK`. Chat-brain-ul moștenește asta prin `[EXECUT]` (predă jobul constructorului). `node --check` OK. **Dovada finală (comportament live) = testul lui Adrian** — nu declar „merge" fără el. RĂMÂNE opțional: memorie de rețete care CREȘTE (goluri rezolvate → rețete noi adăugate automat în registru).
- **H. POOL ELASTIC DE REPARATORI (ACTIV 12 iul — Adrian: „da drumul la toți reparatori acum").** Pool-ul elastic e activ: `bridge/kelion-repairer-pool.mjs` pornește primul ciclu IMEDIAT (nu mai doarme așteptând POLL_MS) și scalează reparatori efimeri (`REPAIRER_MODE=1`) până la `MAX_REPAIRERS` (default 3), fiecare în worktree propriu; ies singuri când coada e goală. Unitate systemd `bridge/kelion-repairer-pool.service` + `bridge/blindare-punte.sh` instalează service-ul cu `Restart=always`; `bridge/pornire-linux.sh` copiază service file proaspăt, pornește pool-ul și OPREȘTE `kelion-builder` (nu se mai bat pe aceeași coadă). Endpoint-urile backend sunt live: `POST /api/bridge/workorders/claim` (atomic, `FOR UPDATE SKIP LOCKED`) și `GET /api/bridge/workorders/pending-count`. Worker-ul `kelion-builder-server.mjs` rămâne dual: mod reparator (`REPAIRER_MODE=1`) și mod deploy-only (`DEPLOY_ONLY=1`). Revenirea la builder-ul clasic e manuală: oprești pool-ul și pornești `kelion-builder`.
**REGULA PENTRU KELION:** înainte să dai o sarcină de OPERAȚIUNI constructorului-LLM, întreabă-te „asta cere comenzi EXACTE?" — dacă da, folosește/creează un runbook determinist, nu lăsa LLM-ul să „interpreteze". Dovadă (loguri, curl, status) înainte de orice afirmație de succes.
**POARTA DE REALITATE (12 iul — Adrian: „reparară o fantomă, consumă credite/bani; creierul trebuie să lucreze CONȘTIENT ÎNFIPT ÎN REALITATE, nu doar mașină").** Un redeploy Railway (declanșat de schimbarea `LIVEKIT_URL`) a dat 500-uri tranzitorii câteva secunde → raportorul de erori client le-a prins → creierul a pornit o reparație de ~200s pentru un endpoint care DEJA dădea 200. Cauza reală: `frontend/src/lib/errorReporting.ts` intercepta `console.warn` și raporta ORICE avertisment (ex. „ScriptProcessorNode is deprecated") ca eroare. Fix triplu: (1) frontend — filtru `isBenign` care NU mai trimite avertismente benigne (deprecări, violări de perf, ResizeObserver loop); (2) backend `bridge.ts` — `BENIGN_CLIENT_NOISE` la intrarea în `/api/bridge/client-errors` (prinde și clienții vechi); (3) prompt constructor — REGULA DE FIER (4): REPRODU defectul cu o comandă reală înainte să repari; dacă nu se mai reproduce (200), scrie „NU SE MAI REPRODUCE" și ÎNCHIDE; un warning „is deprecated" nu e bug.
**FAILOVER DE CREDIT FĂRĂ ÎNTRERUPERE (12 iul — Adrian: „verificări de credit silențioase fără disconect de servicii; trecere din unul în altul fără întrerupere; tot automat, fără manual; repară toate bug-urile ca să nu revină la deploy").** Când abonamentul Claude Max se golea, chatul îi arăta lui Adrian textul CLI-ului „You're out of usage credits. /model to switch models." ca REPLICĂ a lui Kelion, deși lanțul Max→Kimi→GLM exista. Bug-uri reparate în `bridge/kelion-bridge-linux.mjs` (dovadă din tier-events: comuta max→kimi, dar userul vedea eroarea; și reflappa la fiecare deploy): (1) `runClaudeStream` stoca rezultatul `is_error` ca `finalText` → îl întorcea ca răspuns; acum îl aruncă (gol → cascada trece pe treapta următoare în ACEEAȘI tură). (2) `runClaudeText`/`runClaudeBare` întorceau stdout-ul CLI-ului (eroarea de cotă) ca răspuns; acum toate 3 producătoarele au gardă `isCliError()` (semnătură strictă CLI, zero fals-pozitiv pe vorbire normală despre credite) → marchează treapta jos + întorc gol. (3) starea treptelor (`tierDownUntil`) era DOAR în memorie → la fiecare deploy revenea pe Max epuizat și reflappa; acum se persistă în `/root/kelion/tier-state.json` (supraviețuiește deploy-ului). (4) backoff pe treaptă cronic goală: 30m→1h→2h→4h→plafon 6h (`tierFails`, resetat de `noteTierOk` la primul răspuns real), ca să nu mai flappeze max→kimi→max la fiecare cooldown. Efect: Adrian nu mai vede NICIODATĂ „out of usage credits" — cererea e servită tăcut de treapta următoare; revenirea pe Max e automată după răcire.
**GAURA (12 iul, prins din chatul real după #154): la 05:39 tot apărea „out of usage credits".** Cauza: #154 reparase cele 3 producătoare (`runClaudeStream/Text/Bare`), DAR chatul adminului merge întâi pe **SESIUNEA CALDĂ** (`startWarm`/`askWarm`, calea primară low-latency) — care avea EXACT același bug (stoca rezultatul `is_error` ca răspuns) și NU fusese atinsă. Reparat (#155): pe `is_error`/`isCliError`, sesiunea caldă (admin ȘI public, ambele prin `startWarm`) marchează treapta, se OMOARĂ (`s.kill()`) și întoarce gol → `askWarm` întoarce null → cascada `askClaude` (deja reparată) pornește pe treapta următoare. LECȚIE: la un failover, verifică TOATE căile de execuție — chatul are 5 (sesiune caldă admin, sesiune caldă public, stream, text, bare), nu doar cascada.
- ✅ **Aprobările contextuale ale lui Adrian nu mai fac cerințe fantomă (13 iul, cerința „Am spus da"):** mesaje precum "Am spus da", "Am zis da", "Am dat da", "Am aprobat", "Am dat drumul" (și combinații cu "publică"/"deploy") sunt recunoscute ca aprobări în `backend/src/routes/chat.ts`: (1) `affirm` — declanșează deploy direct când există un release gata; (2) `bareAffirm` — direcționează mesajul ca referire la context, nu ca cerință nouă cu summary-ul aprobării. Build backend + frontend trece, 24/24 teste trec.

## 15. CUM VERIFICI CĂ TOTUL E SĂNĂTOS (60 de secunde) — actualizat 25 iul
```bash
curl -s https://kelionai.app/api/version            # {v,at}; după primul deploy prin deploy.yml, v = sha-ul scurt al lui origin/master (dovada sincroniei)
curl -s https://kelionai.app/health                 # 200
curl -s -o /dev/null -w '%{http_code}' -X POST https://kelionai.app/api/me/delete   # 401 (nu 404)
# fapte complete de pe VPS: Actions → vps-diag (citește VERDICT-ul cod-rulat-vs-master)
# chat public real: workflow public-latency-test (demo de pe runner) — primul cuvânt rapid
# NOTĂ: /api/dev/status și /api/admin/livekit/status NU mai există (puntea/LiveKit șterse).
```

- 🗓️ **26 IUL (valul 15) — TIMBRUL PE VOCEA PRINCIPALĂ finalizat (Adrian: „de ce nu e finalizată recunoașterea de timbru?"):** până azi amprenta vocală se verifica DOAR pe calea STT de rezervă — în full-duplex Realtime nu rula deloc. Acum: (1) `realtimeVoice.ts` are un robinet de amprentă — analizor paralel pe ACELAȘI microfon (nu atinge WebRTC), colectează cadre doar la energie de vorbire, finalizează amprenta per frază și o trimite cu transcriptul userului; (2) `/api/realtime/transcript` acceptă `voiceFeatures` și rulează ACEEAȘI comparație/înrolare ca chat.ts (prag 0.38, referință stabilă); (3) la voce străină serverul întoarce `foreignVoice:true`, iar clientul injectează în sesiune item de SISTEM: conversație normală, dar FĂRĂ comenzi de administrare/financiare/distructive până la confirmarea ÎN SCRIS a titularului. Tot în valul ăsta (PR #413): adresarea pe nume „Kelion/Kei" în instrucțiunile vocii (tace când vorbești cu altcineva; numele îl recheamă) + placeholder onest în 8 limbi + pagina curată la deschidere (memoria rămâne pe server).

- 🗓️ **26 IUL (valul 16) — LOGIN NON-GOOGLE + PAGINI DEDICATE (Adrian: „da, pornește, inclusiv să poată crea și cumpăra credite"):**
  1. **Conturi locale** (`local_accounts` + `login_tokens` în db.ts; rute în `routes/authLocal.ts`): email+parolă (scrypt din node:crypto, sare per cont, comparare timp-constant), link magic pe email (cont creat din zbor la click), resetare parolă pe email. Tokenurile: DOAR hash-ul în DB, 20 min, unică folosință. Mesaje generice la magic/reset (fără dezvăluirea existenței contului). Sesiunea = ACELAȘI JWT `setSession` ca la Google → toate funcțiile merg identic (identitatea e emailul); singura lipsă naturală: skill-urile pe datele Google personale (se pot CONECTA ulterior prin „Connect Google", separat de login).
  2. **Pagina `/login`** (`pages/Login.tsx`, rutată pe pathname în App.tsx): toate căile + creare cont + „am uitat parola" + Google (procedura completă din #416).
  3. **Pagina publică `/credite`** (`pages/Credits.tsx`): pachetele 150/225/375 credite (£20/30/50, 7.5 credite/£ — pornesc de la £20 din cauza regulii server `first_topup_min_20`), „Cumpără" → login dacă nu e autentificat → `/api/billing/checkout` (Stripe) existent. Pastila de portofel din aplicație rămâne neatinsă.
