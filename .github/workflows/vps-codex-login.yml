name: VPS Constructor OpenCode preflight

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  measured-read-only-preflight:
    uses: ./.github/workflows/private-ai-status-proof.yml
    secrets: inherit
