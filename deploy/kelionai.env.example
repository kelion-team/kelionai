# ── Env aplicație Kelionai (VPS) — copiază în /root/kelion/kelionai.env ──────
# Legendă:  [AM] = deja primit în conversație (le pune Claude prin set-keys.sh)
#           [ADRIAN] = doar tu le ai (din vechiul setup Google)
#           [AUTO] = se poate genera / valoare implicită

PORT=8080
NODE_ENV=production

# ── Creier (Gemini-only — OpenRouter/OpenAI EXTIRPATE, 3 aug) ────────────────
GEMINI_API_KEY=                # [ADRIAN] CREIERUL UNIC (chat+work+top) + vedere + traduceri + imagini
# MODELUL NU SE MAI POATE SCHIMBA DIN ENV (regulă ultra-decisă, 6 aug): sursa unică
# e MODEL_UNIC_DEFAULT din config.ts, iar toate treptele sunt getteri pe ea. Vechile
# BRAIN_CHAT/WORK/TOP_MODEL au fost SCOASE din cod — nu mai au niciun efect. Lacătul
# (verifica-gemini.mjs + lacat.test.ts) respinge orice PR care reintroduce un env de model.
GITHUB_TOKEN=                  # [ADRIAN] fin-granulat, DOAR repo kelionai, Actions:write — mâinile lui Kelion pe runbook-uri (run_runbook)

# ── Login Google (OBLIGATORII, doar tu le ai) ────────────────────────────────
GOOGLE_CLIENT_ID=              # [ADRIAN] Google Cloud Console → OAuth client
GOOGLE_CLIENT_SECRET=          # [ADRIAN]
GOOGLE_REDIRECT_URI=https://kelionai.app/auth/google/login/callback  # [ADRIAN] verifică calea reală
SESSION_SECRET=                # [AUTO] orice string lung aleator (ex: openssl rand -hex 32)

# ── Bază de date (OBLIGATORIU) ───────────────────────────────────────────────
# Postgres rulează pe VPS (127.0.0.1:5432). Ai nevoie de user/parolă/nume bază.
DATABASE_URL=postgres://USER:PAROLA@127.0.0.1:5432/kelion?sslmode=disable  # [ADRIAN]

# ── Payments (Revolut + Enable Banking; Stripe is fully OUT — 31 Jul) ────────
REVOLUT_PAY_LINK=              # [ADRIAN] Revolut checkout/payment link for credit purchases
ENABLE_BANKING_APP_ID=         # [ADRIAN] Enable Banking Control Panel → API applications
ENABLE_BANKING_PRIVATE_KEY_B64=# [ADRIAN] private key, base64, one line (base64 -w0 key.pem)
# ENABLE_BANKING_ACCOUNT_UID=  # optional: filled automatically after the PSD2 consent
BILLING_CURRENCY=gbp
USER_SHARE=0.75                # 75% credits to the user / 25% admin fund
CREDIT_VALUE=0.1

# ── Voce + skill-uri Google ──────────────────────────────────────────────────
GOOGLE_SERVICE_ACCOUNT_JSON=   # [ADRIAN] Chirp 3 (auz + gură) — fără el vocea nu are rezervă (OpenAI extirpat)
GOOGLE_TTS_API_KEY=            # [ADRIAN] alternativă TTS pe cheie (opțional)
SERPER_API_KEY=               # [ADRIAN] căutare web (singurul motor)
GOOGLE_MAPS_KEY=              # [ADRIAN] hărți (opțional)

# ── Mail contact@ (opțional) ─────────────────────────────────────────────────
MAIL_USER=contact@kelionai.app
MAIL_PASS=                    # [ADRIAN]
ADMIN_EMAIL=adrianenc11@gmail.com

# ── LiveKit (rulează pe VPS; cheile din setup-ul livekit) ────────────────────
LIVEKIT_URL=ws://127.0.0.1:7880
LIVEKIT_API_KEY=              # [ADRIAN] din configul livekit de pe VPS
LIVEKIT_API_SECRET=           # [ADRIAN]

FRONTEND_ORIGIN=https://kelionai.app
