[package]
name = "kelionai-desktop"
version = "1.0.0"
description = "Kelionai — personal AI assistant"
authors = ["AE Studio"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-deep-link = "2"
tauri-plugin-opener = "2"
tauri-plugin-single-instance = { version = "2", features = ["deep-link"] }
keyring = { version = "=4.1.5", default-features = false, features = ["v1"] }
serde_json = "1"

[profile.release]
# Bundle local minim; rețeaua este folosită numai de API-ul first-party.
opt-level = "s"
lto = true
codegen-units = 1
strip = true
